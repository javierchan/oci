# Project Audit Report

**Generated:** 2026-07-10 13:05 CST
**Repository:** https://github.com/javierchan/oci.git
**Selected codebase:** `OCI DIS Blueprint`
**Execution and write location:** `/Users/javierchan/Documents/GitHub/oci/OCI DIS Blueprint`
**Branch:** `main`
**Commit:** `38cfb30 fix(ci): restore OCI DIS quality gates`
**Auditor:** Codex (automated)

---

## Executive Summary

The Phase 1 product baseline is feature-complete: all documented milestones M1-M24 have implementation evidence, the working tree is clean, the OpenAPI contract is synchronized, the 42 deterministic calc-engine parity tests pass locally, and the GitHub Actions run for the current commit completed successfully with 68 API tests plus lint and type checks.

The application is not yet ready for direct production deployment. Three critical gaps remain:

1. Identity and project authorization still trust client-authored actor headers and there are no user, organization, or project-membership models.
2. The repository provides a strong local Docker environment but no production deployment, secret, network, backup, or release contract.
3. The effective root GitHub Actions workflow omits calc-engine parity tests, the production web build, web unit tests, E2E, dependency audit enforcement, migrations against PostgreSQL, and container scanning.

Four additional implementation areas remain partial: durable object storage and observability are configured but not wired; Service Product Library phases 3-5 are not fully consumed by backend rules; browser regression coverage is narrow and does not assert terminal job success; and repository contracts contain stale workspace, CI, and runtime references.

Recommended stance: the product is suitable for governed local/internal use and continued implementation, but production release should remain blocked until M1-M15 in this report are complete.

## Repository Profile

| Metric | Evidence |
|---|---|
| Git state | `main`, clean, synchronized commit `38cfb30` |
| Codebase size | 230 Python/TypeScript source files, 52,312 lines |
| API contract | 78 OpenAPI paths, 100 operations; artifact check passed |
| Database migrations | 13 revisions, single Alembic head `20260607_0013` |
| Python tests | 110 collected: 68 API and 42 calc-engine |
| Web tests | 19 focused unit tests and 2 Playwright tests |
| Runtime services | 7 Compose services: db, redis, api, worker, beat, web, minio |
| History | 307 repository commits; newest 2026-07-03; oldest 2026-01-07 |
| Recent activity | 1 commit since 2026-07-03; 1 commit in the prior seven-day window |
| Debt markers | No actionable TODO/FIXME/HACK/PLACEHOLDER markers in application source |

`graphify` was not installed. The audit did not install it because audit mode prohibits package installation; relationships were traced directly from imports, models, routes, tests, workflows, and runtime configuration.

## Milestone Status

| Milestone | Status | Evidence | Gap or deviation |
|---|---|---|---|
| M1 Schema + Migrations | Complete | 13 Alembic revisions; one head; governance and project models present | Current audit could not run migration upgrade against live PostgreSQL because Docker was unavailable |
| M2 Import Engine | Complete | Import service and importer parity tests present | Durable production object storage is not used |
| M3 Catalog Grid API | Complete | Catalog router/service and API tests present | Project-scoped authorization is absent |
| M4 Calculation Engine | Complete | 42 calc-engine tests passed locally | Effective CI does not execute these tests |
| M5 Dashboard API | Complete | Dashboard service/schema/UI and tests present | Service Product freshness is not consumed |
| M6 Justification Narratives | Complete | Deterministic narrative service and approval tests present | Production identity boundary remains absent |
| M7 Exports | Complete | XLSX/JSON/PDF/brief code and tests present | Export artifacts are written to local filesystem |
| M8 Admin + Governance | Complete | Admin routes, services, audit paths, and tests present | Admin identity is client-authored |
| M9 Capture Wizard | Complete | Five-step capture routes/components present | Full browser regression not in CI |
| M10 Dependency Map | Complete | Graph service, schemas, UI, drill-through present | Full visual regression not in CI |
| M11 Navigation + Theme | Complete | Shared navigation/theme implementation documented and present | Accessibility automation is absent |
| M12 Lineage + Template | Complete | Lineage and template export implementation/tests present | Import lifecycle and retention are not production-defined |
| M13 Integration Canvas | Complete | SVG canvas and governed save semantics present | Backend rules still rely on static mappings and assumptions |
| M14 Map Interaction | Complete | Pan/zoom/filter/focus implementation present | Keyboard and viewport coverage is not a blocking CI gate |
| M15 UX P0 | Complete | Canvas, pagination, and error handling implemented | Effective CI no longer carries the full five gates promised by the milestone |
| M16 UX P1 | Complete | Data accuracy, dashboard, graph direction, and filters implemented | No current live browser validation in this audit |
| M17 UX P2 | Complete | Layout and polish changes documented and present | Visual regression is manual/report-based |
| M18 Import Fidelity | Complete | Header/alias/unit tests present | PostgreSQL integration path is not exercised by CI |
| M19 Reference Data 2.0 | Complete | Seed and reference API tests present | Missing shared-schema package contradicts documented contract |
| M20 Canvas Intelligence | Complete | Combination and interoperability helpers/tests present | Normalized Service Product rules are not the backend source of truth |
| M21 Assumption Parity | Complete | Calc assumptions and parity tests present | Technical service limits remain inside `AssumptionSet` |
| M22 QA Confidence | Complete | QA and dashboard confidence tests present | Service metadata freshness is not propagated end to end |
| M23 Pattern Coverage | Complete | Pattern support matrix, seed coverage, QA/export paths present | No current live benchmark run in this audit |
| M24 Synthetic Lab | Complete | API/service/worker tests and prior bounded smoke reports present | Existing Playwright test stops at `pending` and static detail rendering |

### Progress Metrics

- Documented product milestones: 24
- Complete by implementation evidence: 24
- Partial or not started within M1-M24: 0
- Definition-of-done items: 6
- Verified with current or current-commit evidence: 5
- Not revalidated live: 1 (API/browser smoke; Docker daemon unavailable)
- New remediation milestones in this report: 20 open

## Milestone Index

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [M1](#m1--establish-trusted-identity) | Establish Trusted Identity | Critical | open |
| [M2](#m2--enforce-project-authorization) | Enforce Project Authorization | Critical | open |
| [M3](#m3--validate-auth-boundaries) | Validate Auth Boundaries | Critical | open |
| [M4](#m4--harden-production-configuration) | Harden Production Configuration | Critical | open |
| [M5](#m5--define-production-deployment) | Define Production Deployment | Critical | open |
| [M6](#m6--prove-release-and-recovery) | Prove Release And Recovery | Critical | open |
| [M13](#m13--consolidate-effective-ci) | Consolidate Effective CI | Critical | open |
| [M14](#m14--enforce-complete-quality-gates) | Enforce Complete Quality Gates | Critical | open |
| [M15](#m15--add-release-security-gates) | Add Release Security Gates | Critical | open |
| [M7](#m7--adopt-durable-object-storage) | Adopt Durable Object Storage | High | open |
| [M8](#m8--wire-operational-telemetry) | Wire Operational Telemetry | High | open |
| [M9](#m9--validate-multi-instance-operations) | Validate Multi-Instance Operations | High | open |
| [M10](#m10--consume-service-product-rules) | Consume Service Product Rules | High | open |
| [M11](#m11--separate-business-assumptions) | Separate Business Assumptions | High | open |
| [M12](#m12--close-verification-productization) | Close Verification Productization | High | open |
| [M16](#m16--cover-primary-browser-workflows) | Cover Primary Browser Workflows | High | open |
| [M18](#m18--validate-terminal-job-behavior) | Validate Terminal Job Behavior | High | open |
| [M17](#m17--add-visual-and-accessibility-gates) | Add Visual And Accessibility Gates | Medium | open |
| [M19](#m19--remove-stale-repository-contracts) | Remove Stale Repository Contracts | Medium | open |
| [M20](#m20--synchronize-operational-documentation) | Synchronize Operational Documentation | Medium | open |

## Verification Results

| Check | Result | Notes |
|---|---|---|
| Git status | PASS | Clean worktree before report creation |
| GitHub Actions run 28674026763 | PASS | Current commit `38cfb30`; 68 API tests, Ruff, mypy, OpenAPI, TypeScript, ESLint |
| Calc-engine pytest | PASS | 42 passed locally |
| Ruff | PASS | `apps/api/app` and calc-engine source clean |
| OpenAPI sync | PASS | Committed artifact is up to date |
| ESLint | PASS | No warnings or errors locally |
| Alembic graph | PASS | Single head `20260607_0013` |
| npm audit | FAIL | 5 vulnerabilities: 2 low, 1 moderate, 2 high; all report a fix available |
| Local API pytest | ENVIRONMENT BLOCKED | Host venv is stale and lacks installed `aiosqlite`; requirements and clean CI include it |
| Local mypy | ENVIRONMENT BLOCKED | Host venv lacks installed mypy; requirements and clean CI include it |
| Local web unit tests | ENVIRONMENT BLOCKED | Host `node_modules` lacks optional Rollup native package; clean CI installs it but does not run tests |
| Local web build | ENVIRONMENT BLOCKED | Host SWC fallback attempted a sandbox-denied cache write; effective CI does not run build |
| Local isolated type check | ENVIRONMENT BLOCKED | Concurrent failed build invalidated generated `.next/types`; clean CI type check passed |
| Docker and live API/browser probes | NOT RUN | Docker daemon socket was unavailable; audit did not start or restart Docker |

The host-environment failures above are not classified as product defects because clean CI installs the declared dependencies successfully. They do expose reproducibility drift in the local checkout and reinforce the need for one canonical containerized validation command.

## Pending Tasks

### CRITICAL

- Real identity and project-scoped RBAC: `apps/web/lib/api.ts`, `apps/api/app/services/authz.py`, and `apps/api/app/models/` trust caller-supplied roles and have no membership model.
- Production deployment contract: `docker-compose.yml`, `apps/api/Dockerfile`, and `apps/api/app/core/config.py` remain local/development oriented.
- Effective CI completeness: `../.github/workflows/oci-dis-blueprint-quality.yml` omits mandatory parity, build, web test, E2E, migration, audit, and image gates.

### INCOMPLETE

- Durable storage and observability: storage and OTLP settings/dependencies exist, but imports/exports use local paths and telemetry is not initialized.
- Service Product rule consumption: normalized models and verification exist, while canvas/recalc still consume hardcoded mappings and `AssumptionSet` limits.
- Browser regression: only the Synthetic Lab has Playwright coverage, and job success is not awaited.
- Repository contract cleanup: missing workspaces and three divergent CI definitions remain referenced.

### DEFERRED

- Live source-system connectors, commercial pricing dashboard, contract quotes, and portfolio analytics remain explicitly outside Phase 1. Re-entry should occur only after the production foundation and a new approved PRD milestone.

## Finding Remediation Plans

### Finding: Client-authored identity and non-project-scoped RBAC
- Classification: `CRITICAL`
- Impact: Direct production exposure would allow callers to assert roles without a verified identity or project membership boundary.
- Area: Security, governance, all mutating workflows
- Evidence:
  - `apps/web/lib/api.ts` - `adminHeaders()` sends `web-admin` and `Admin` from browser code.
  - `apps/api/app/services/authz.py` - authorization normalizes role strings but verifies no token or membership.
  - `apps/api/app/models/` - no User, Organization, or ProjectMembership model exists.
- Issue: The current role layer is suitable for a trusted local demo only.
- Remediation Milestones:
  - `M1` - Establish Trusted Identity (`Critical`, `open`)
  - `M2` - Enforce Project Authorization (`Critical`, `open`)
  - `M3` - Validate Auth Boundaries (`Critical`, `open`)

### Finding: No production deployment and secret-management contract
- Classification: `CRITICAL`
- Impact: The app cannot be deployed safely or reproducibly outside local Docker Desktop.
- Area: Deployment, secrets, networking, release operations
- Evidence:
  - `docker-compose.yml` - development API/worker targets, hot mounts, published data-service ports, local credentials, and `minio/minio:latest`.
  - `apps/api/app/core/config.py` - `SECRET_KEY` defaults to `change-me-in-production` without a production fail-fast validator.
  - `apps/api/Dockerfile` - production stage runs as root and carries build dependencies from the base/deps chain.
- Issue: Local runtime is mature, but production infrastructure and safeguards are undefined.
- Remediation Milestones:
  - `M4` - Harden Production Configuration (`Critical`, `open`)
  - `M5` - Define Production Deployment (`Critical`, `open`)
  - `M6` - Prove Release And Recovery (`Critical`, `open`)

### Finding: Storage and telemetry abstractions are configured but not implemented
- Classification: `INCOMPLETE`
- Impact: Multi-instance deployments can lose or fail to find uploads/exports, and operators lack metrics, traces, and actionable alerts.
- Area: Imports, exports, synthetic jobs, operations
- Evidence:
  - `apps/api/app/services/import_service.py` - uploads are written with `Path.write_bytes`.
  - `apps/api/app/services/export_service.py` - `EXPORT_ROOT = Path("uploads/exports")` and artifacts are local files.
  - `apps/api/app/core/config.py` - storage and `OTLP_ENDPOINT` settings exist but are not consumed by a storage or telemetry adapter.
- Issue: Configuration advertises production capabilities that runtime code does not use.
- Remediation Milestones:
  - `M7` - Adopt Durable Object Storage (`High`, `open`)
  - `M8` - Wire Operational Telemetry (`High`, `open`)
  - `M9` - Validate Multi-Instance Operations (`High`, `open`)

### Finding: Service Product Library phases 3-5 remain partial
- Classification: `INCOMPLETE`
- Impact: Accepted product findings do not consistently become the rule source used by canvas, volumetry, AI review, dashboard, and exports.
- Area: Service Product Library and Verification Agent
- Evidence:
  - `docs/architecture/service-product-library-and-verification-agent-plan.md` - phases 3-5 remain unchecked and define unfinished rule consumption and assumption split.
  - `apps/api/app/services/canvas_interoperability.py` - static `TOOL_TO_SERVICE_ID`, backend compatibility sets, and `Assumptions` limits remain authoritative.
  - `apps/api/app/services/recalc_service.py` - `_to_assumptions()` still reads service constraints from `AssumptionSet`.
  - `apps/api/app/services/ai_review_service.py`, `export_service.py`, and `dashboard_service.py` - no normalized Service Product rule/freshness query path is present.
- Issue: The library and agent exist, but the application has two sources of truth for service rules.
- Remediation Milestones:
  - `M10` - Consume Service Product Rules (`High`, `open`)
  - `M11` - Separate Business Assumptions (`High`, `open`)
  - `M12` - Close Verification Productization (`High`, `open`)

### Finding: Effective CI is green while mandatory gates are absent
- Classification: `CRITICAL`
- Impact: A merge can regress workbook parity, production web build, web behavior, migrations, or dependency security while CI remains green.
- Area: CI/CD and release quality
- Evidence:
  - `../.github/workflows/oci-dis-blueprint-quality.yml` - executes API pytest, Ruff, mypy, OpenAPI, TypeScript, and ESLint only.
  - `.github/workflows/oci-dis-blueprint-quality.yml` and `infra/ci/github-actions.yml` - fuller but non-effective duplicate definitions include calc and web tests.
  - GitHub run 28674026763 - successful with 68 API tests; the 42 calc tests and 19 web tests were not run.
  - `npm audit --package-lock-only --json` - 5 vulnerabilities, including production-path `form-data` via Axios and dev-path Vite issues; no audit step blocks CI.
  - `apps/api/app/tests/conftest.py` - API tests use SQLite metadata creation rather than PostgreSQL plus Alembic migration execution.
- Issue: The displayed quality signal does not satisfy the repository's own definition of done.
- Remediation Milestones:
  - `M13` - Consolidate Effective CI (`Critical`, `open`)
  - `M14` - Enforce Complete Quality Gates (`Critical`, `open`)
  - `M15` - Add Release Security Gates (`Critical`, `open`)

### Finding: Browser, visual, and accessibility regression coverage is narrow
- Classification: `INCOMPLETE`
- Impact: Core catalog, canvas, topology, import, AI review, and responsive regressions can reach main undetected.
- Area: Frontend quality and user workflows
- Evidence:
  - `apps/web/e2e/admin-synthetic.spec.ts` - only two Synthetic Lab tests exist.
  - The create flow asserts `pending` and static detail sections but does not poll for `completed` or `cleaned_up`.
  - Effective CI does not install or run Playwright and has no accessibility or screenshot-diff gate.
- Issue: Prior manual visual audits are valuable evidence, but they are not regression-capable release gates.
- Remediation Milestones:
  - `M16` - Cover Primary Browser Workflows (`High`, `open`)
  - `M17` - Add Visual And Accessibility Gates (`Medium`, `open`)
  - `M18` - Validate Terminal Job Behavior (`High`, `open`)

### Finding: Repository contracts contain stale or duplicated definitions
- Classification: `INCOMPLETE`
- Impact: Engineers can update a non-effective workflow or rely on packages and runtime counts that do not exist.
- Area: Maintainability and traceability
- Evidence:
  - `package.json` - declares `packages/ui` and `packages/shared-schema`; neither directory exists.
  - `AGENTS.md` and `docs/architecture/system-overview.md` - still describe `packages/shared-schema` as active.
  - `../.github/workflows/oci-dis-blueprint-quality.yml`, `.github/workflows/oci-dis-blueprint-quality.yml`, and `infra/ci/github-actions.yml` - three workflow definitions, with the effective root file missing gates found in the copies.
  - `README.md` - validation snapshot says six containers while Compose defines seven services.
- Issue: Documentation and duplicate automation no longer describe one canonical implementation.
- Remediation Milestones:
  - `M19` - Remove Stale Repository Contracts (`Medium`, `open`)
  - `M20` - Synchronize Operational Documentation (`Medium`, `open`)

## M1 — Establish Trusted Identity

**Tier:** Critical
**Status:** open

### Problem

The API trusts caller-controlled actor headers.

### Root Cause

Phase 1 implemented role simulation without an authenticated principal boundary.

### Target State

- A verified server-side principal is created from OIDC/JWT or a formally trusted signed gateway contract.
- Direct client role assertion is impossible in production.

### Files Most Likely To Change

- `apps/api/app/core/config.py` - identity-provider and production validation settings.
- `apps/api/app/services/authz.py` - authenticated principal dependency and policy entry points.
- `apps/web/lib/api.ts` - remove hardcoded actor headers.

### Action Plan

1. Select and document the identity boundary and token/gateway trust model.
2. Implement token or signed-header validation and a typed principal object.
3. Remove browser-authored role headers and reject unverified production requests.
4. Add audit attributes for subject, tenant, and authentication method.

### Exit Criteria

- Anonymous and forged-role requests fail in production mode.
- Valid signed identities reach allowed routes with stable actor IDs.

## M2 — Enforce Project Authorization

**Tier:** Critical
**Status:** open

### Problem

Global role strings do not enforce organization or project ownership.

### Root Cause

No user, organization, or project-membership domain model exists.

### Target State

- Membership and role assignments are persisted and project-scoped.
- Service-layer policies guard every read and mutation.

### Files Most Likely To Change

- `apps/api/app/models/` - identity and membership models.
- `apps/api/migrations/versions/` - identity/RBAC schema migration.
- `apps/api/app/services/authz.py` - project policy evaluation.

### Action Plan

1. Add organization, user, and project-membership schema with auditable role assignment.
2. Introduce service-layer `require_project_permission` policies.
3. Apply policies across projects, imports, catalog, exports, governance, AI, and service verification.
4. Add tenant-isolation query constraints and audit events.

### Exit Criteria

- Cross-project access is denied even when a valid user has a role in another project.
- All mutating service paths have positive and negative policy tests.

## M3 — Validate Auth Boundaries

**Tier:** Critical
**Status:** open

### Problem

Production authorization cannot be trusted without adversarial validation.

### Root Cause

Current tests validate role labels rather than identity, membership, and token failure modes.

### Target State

- Authentication and authorization tests cover every role and project boundary.
- Deployment prevents bypassing the trusted identity layer.

### Files Most Likely To Change

- `apps/api/app/tests/` - auth matrix and isolation tests.
- Production ingress/IaC files - direct API isolation.
- `docs/` - identity and incident runbooks.

### Action Plan

1. Build an endpoint/permission matrix for Viewer, Analyst, Architect, and Admin.
2. Add expired, malformed, wrong-issuer, wrong-audience, and forged-role tests.
3. Validate direct API bypass is blocked in the production topology.
4. Document role lifecycle and access-revocation procedures.

### Exit Criteria

- Auth matrix tests are blocking CI gates.
- A deployment smoke proves the API cannot be reached without the trusted identity path.

## M4 — Harden Production Configuration

**Tier:** Critical
**Status:** open

### Problem

Unsafe local defaults can survive into production.

### Root Cause

Settings are permissive and do not enforce environment-specific invariants.

### Target State

- Production startup fails on default secrets, local credentials, broad CORS, or missing storage/auth configuration.
- Production images run as non-root with minimal dependencies.

### Files Most Likely To Change

- `apps/api/app/core/config.py` - production validators.
- `apps/api/Dockerfile` - non-root minimal runtime.
- `.env.example` - explicit local-only defaults and production requirements.

### Action Plan

1. Add production-mode configuration validation with actionable failures.
2. Split runtime and build packages and add a non-root API user.
3. Pin infrastructure image versions and define environment-specific CORS/host policy.
4. Add image metadata and health/readiness contracts.

### Exit Criteria

- Production image fails fast with any local default.
- API and worker images pass non-root and minimal-package checks.

## M5 — Define Production Deployment

**Tier:** Critical
**Status:** open

### Problem

Docker Compose is the only executable topology.

### Root Cause

No target cloud deployment or IaC milestone has been implemented.

### Target State

- Versioned IaC/manifests define ingress, compute, managed PostgreSQL/Redis/object storage, networking, and secrets.
- Migrations and workers have controlled rollout semantics.

### Files Most Likely To Change

- `infra/` - production IaC and environment overlays.
- `apps/api/Dockerfile` and `apps/web/Dockerfile` - release images.
- `docs/architecture/` - production topology ADR.

### Action Plan

1. Select the OCI runtime target and document the production topology.
2. Implement separate environment overlays and managed dependencies.
3. Add TLS ingress, private networking, secret-store integration, and egress policy.
4. Define migration job, worker/beat ownership, and zero-downtime rollout order.

### Exit Criteria

- A clean environment can be provisioned from versioned code.
- Deployment smoke reaches health/readiness and one governed workflow through TLS.

## M6 — Prove Release And Recovery

**Tier:** Critical
**Status:** open

### Problem

Backup, restore, rollback, and release behavior are not executable.

### Root Cause

Operations are documented as future work rather than automated contracts.

### Target State

- Releases, migrations, rollback, backup, restore, and disaster recovery are rehearsed.
- Recovery objectives and data-retention policies are explicit.

### Files Most Likely To Change

- `infra/` - backup, restore, and rollout automation.
- `docs/runbooks/` - release and incident procedures.
- `.github/workflows/` - deployment and recovery validation.

### Action Plan

1. Define RPO/RTO, retention, and artifact lifecycle requirements.
2. Automate backup and isolated restore verification.
3. Add migration preflight, rollback decision points, and release evidence capture.
4. Execute and document a staging recovery drill.

### Exit Criteria

- A backup restores to a clean environment with catalog and audit integrity verified.
- Release and rollback runbooks are exercised and timestamped.

## M7 — Adopt Durable Object Storage

**Tier:** High
**Status:** open

### Problem

Uploads and exports depend on local container filesystems.

### Root Cause

Storage settings and boto3 were added without a storage service abstraction.

### Target State

- All durable artifacts use a typed storage adapter with MinIO locally and OCI Object Storage in production.
- Database records store stable object keys and metadata, not local paths.

### Files Most Likely To Change

- `apps/api/app/services/` - new object storage service.
- `apps/api/app/services/import_service.py` and `export_service.py` - adapter integration.
- `apps/api/app/models/` and migrations - object metadata/lifecycle fields.

### Action Plan

1. Define storage interface, object-key convention, checksum, and retention metadata.
2. Migrate import, export, and synthetic artifact writes to the adapter.
3. Add presigned or streamed download authorization.
4. Backfill or explicitly expire existing local-path artifacts.

### Exit Criteria

- Two API instances can create and retrieve the same artifact.
- Storage tests pass against MinIO and the production adapter contract.

## M8 — Wire Operational Telemetry

**Tier:** High
**Status:** open

### Problem

Operators cannot measure or trace failed imports, jobs, exports, or AI/verification calls.

### Root Cause

Telemetry dependencies and settings are present but not initialized.

### Target State

- Structured logs, traces, metrics, and correlation IDs cover HTTP and asynchronous jobs.
- Alerts map to user-visible failures and runbooks.

### Files Most Likely To Change

- `apps/api/app/main.py` - instrumentation initialization.
- `apps/api/app/workers/` - Celery telemetry.
- `apps/api/app/core/` - logging and telemetry configuration.

### Action Plan

1. Initialize structured logging and OpenTelemetry from environment settings.
2. Propagate correlation IDs from HTTP through Celery and audit events.
3. Add latency, error, queue-depth, stale-job, and storage metrics.
4. Define alerts and dashboards for critical workflows.

### Exit Criteria

- One import and one async job are traceable end to end.
- Injected failures emit correlated logs, metrics, and alerts.

## M9 — Validate Multi-Instance Operations

**Tier:** High
**Status:** open

### Problem

Local single-host behavior does not prove horizontal runtime correctness.

### Root Cause

Filesystem state and process-local assumptions have not been tested under multiple instances.

### Target State

- API, worker, and scheduler ownership are safe under scaling and restarts.
- Idempotency and cleanup are deterministic.

### Files Most Likely To Change

- `apps/api/app/workers/` - task idempotency and locking.
- `apps/api/app/services/` - durable job state.
- Production test manifests - scaled smoke topology.

### Action Plan

1. Inventory process-local and filesystem state.
2. Add idempotency keys, scheduler singleton policy, and stale-job recovery.
3. Run concurrent import/recalc/export/verification scenarios across multiple workers.
4. Validate rolling restart and duplicate-delivery behavior.

### Exit Criteria

- Scaled staging tests produce no duplicate catalog mutations or lost artifacts.
- Worker restart resumes or safely terminates every governed job.

## M10 — Consume Service Product Rules

**Tier:** High
**Status:** open

### Problem

Normalized service rules are not the backend source of truth.

### Root Cause

The library was layered beside existing static canvas and assumption logic for backward compatibility.

### Target State

- Canvas, QA, recalc input assembly, AI review, dashboard, and exports consume a versioned rule bundle.
- Calc-engine remains pure and receives explicit immutable inputs.

### Files Most Likely To Change

- `apps/api/app/services/service_product_service.py` - rule-bundle API.
- `apps/api/app/services/canvas_interoperability.py` and `recalc_service.py` - injected rules.
- `apps/api/app/services/ai_review_service.py`, `dashboard_service.py`, `export_service.py` - freshness/version evidence.

### Action Plan

1. Define a typed immutable service-rule bundle and version identifier.
2. Load active limits/interoperability from normalized tables in the service layer.
3. Inject the bundle into canvas, recalc, AI, dashboard, and export paths.
4. Add regression tests proving accepted findings change governed behavior only after approval.

### Exit Criteria

- No production rule path reads duplicated static service limits.
- Snapshots and exports identify the exact service-rule version used.

## M11 — Separate Business Assumptions

**Tier:** High
**Status:** open

### Problem

Client assumptions and hard service constraints remain mixed.

### Root Cause

Compatibility with historical snapshots preserved the original `AssumptionSet` shape.

### Target State

- Assumptions contain only unknown client/business variables.
- Historical snapshots remain reproducible through a versioned compatibility shim.

### Files Most Likely To Change

- `apps/api/app/models/governance.py` and migrations - assumption schema/version metadata.
- `apps/api/app/services/recalc_service.py` - split input assembly.
- `apps/web/app/admin/assumptions/` - business-only UX.

### Action Plan

1. Classify every current assumption key as business, service rule, or historical compatibility.
2. Migrate service keys into normalized limits and produce a migration report.
3. Implement snapshot compatibility resolution without mutating historical records.
4. Update UI, exports, and documentation labels.

### Exit Criteria

- New assumption sets contain no hard Oracle service limits.
- Historical snapshot recalculation tests remain deterministic.

## M12 — Close Verification Productization

**Tier:** High
**Status:** open

### Problem

Verification execution exists, but production policy and freshness consumption are incomplete.

### Root Cause

Phase 2 was completed before identity, deployment, and rule-consumption foundations.

### Target State

- Allowlist, scheduling, approvals, egress, stale alerts, and evidence retention are production-governed.
- No internet-derived change bypasses human review.

### Files Most Likely To Change

- `apps/api/app/services/service_product_service.py` - policy and retention.
- `apps/api/app/workers/celery_app.py` - governed schedule.
- `docs/runbooks/` - verification operations.

### Action Plan

1. Resolve open decisions for evidence storage, schedule defaults, tenancy overrides, and fetch adapters.
2. Bind verification jobs and findings to authenticated actors and project/tenant policy.
3. Expose freshness and open-finding impact in dependent workflows.
4. Add runbook and production egress tests.

### Exit Criteria

- Scheduled and manual jobs obey allowlist, quota, identity, and retention policy.
- Accepted findings update one audited rule bundle and all consumers surface its version.

## M13 — Consolidate Effective CI

**Tier:** Critical
**Status:** open

### Problem

Three workflow definitions diverge, and only the repository-root workflow executes.

### Root Cause

The codebase moved inside a parent Git repository without retiring or generating nested workflow copies.

### Target State

- One canonical effective workflow or one generated source with a drift check.
- Engineers cannot edit an inert CI file believing it is active.

### Files Most Likely To Change

- `../.github/workflows/oci-dis-blueprint-quality.yml` - effective workflow.
- `.github/workflows/oci-dis-blueprint-quality.yml` and `infra/ci/github-actions.yml` - retire or convert to templates.
- `README.md` - canonical validation command.

### Action Plan

1. Declare the root workflow canonical and inventory unique steps in the copies.
2. Merge required gates into the effective workflow.
3. Remove redundant tracked copies or generate them with an enforced equality check.
4. Document ownership and local parity commands.

### Exit Criteria

- Exactly one authoritative CI definition exists.
- A deliberate failure in each required gate fails the effective workflow.

## M14 — Enforce Complete Quality Gates

**Tier:** Critical
**Status:** open

### Problem

Current CI omits required parity, build, unit, and browser tests.

### Root Cause

The restored root workflow selected a fast subset after prior CI failures.

### Target State

- CI enforces calc-engine parity, API tests, OpenAPI, lint, typing, production web build, web unit tests, and bounded E2E.
- Generated type artifacts are isolated from concurrent build/type processes.

### Files Most Likely To Change

- `../.github/workflows/oci-dis-blueprint-quality.yml` - complete jobs and dependencies.
- `apps/web/package.json` - stable CI scripts.
- Test configuration - deterministic service dependencies.

### Action Plan

1. Add explicit `packages/calc-engine/src/tests` execution.
2. Add `npm run build`, `npm test`, and bounded Playwright jobs.
3. Run build and type generation sequentially or in isolated workspaces.
4. Publish concise test and browser artifacts on failure.

### Exit Criteria

- CI reports 110 Python tests plus 19 web unit tests and the approved E2E suite.
- Production build failure blocks merge.

## M15 — Add Release Security Gates

**Tier:** Critical
**Status:** open

### Problem

Known dependency findings and unscanned images do not block release.

### Root Cause

Dependency installation reports audit data but no policy evaluates it; Python and images have no scanner.

### Target State

- Fixable high vulnerabilities fail CI, with time-bound audited exceptions.
- Python dependencies and release images have lock/SBOM/scan evidence.
- PostgreSQL migrations run in CI.

### Files Most Likely To Change

- `package-lock.json` and dependency manifests - patched dependency graph.
- Python constraints/lock artifact - reproducible transitive graph.
- `../.github/workflows/oci-dis-blueprint-quality.yml` - audit, SBOM, image, migration gates.

### Action Plan

1. Update the lockfile to remove the current five npm findings, prioritizing production-path `form-data`.
2. Add npm and Python vulnerability policy with an exception ledger.
3. Build and scan production images and emit SBOMs.
4. Run Alembic upgrade and API integration smoke against PostgreSQL 16 in CI.

### Exit Criteria

- No unapproved high/critical dependency or image vulnerability remains.
- A clean PostgreSQL database reaches the single Alembic head and passes smoke tests.

## M16 — Cover Primary Browser Workflows

**Tier:** High
**Status:** open

### Problem

Most user-facing workflows have no automated browser coverage.

### Root Cause

Playwright was introduced narrowly for Synthetic Lab validation.

### Target State

- Projects, import, catalog/drawer tabs, detail/canvas, topology, dashboard, governance, AI review fallback, and exports have bounded E2E coverage.
- Tests use deterministic seeded data and stable roles.

### Files Most Likely To Change

- `apps/web/e2e/` - workflow suites and fixtures.
- `apps/web/playwright.config.ts` - projects, retries, artifacts, viewports.
- `apps/api/scripts/` - bounded deterministic seed/setup.

### Action Plan

1. Define critical journeys and reusable deterministic fixtures.
2. Add desktop coverage for create/read/update/govern/export flows.
3. Add mobile smoke for overflow-prone catalog, canvas, topology, and service pages.
4. Capture console, failed requests, and screenshots as assertions/artifacts.

### Exit Criteria

- Every critical journey has at least one success and one failure-path test.
- Browser console errors and unexpected 4xx/5xx fail the suite.

## M17 — Add Visual And Accessibility Gates

**Tier:** Medium
**Status:** open

### Problem

Manual screenshots do not prevent visual or accessibility regressions.

### Root Cause

No baseline screenshot or automated accessibility policy exists.

### Target State

- Stable main surfaces have desktop/mobile/light/dark visual baselines.
- WCAG-oriented automated checks cover keyboard, names, contrast, and overflow.

### Files Most Likely To Change

- `apps/web/e2e/` - visual and accessibility specs.
- `apps/web/playwright.config.ts` - snapshot configuration.
- CI workflow - baseline comparison and artifacts.

### Action Plan

1. Select stable screens and deterministic viewport/theme states.
2. Add screenshot comparisons with reviewed thresholds.
3. Add accessibility scans and keyboard navigation assertions.
4. Define baseline update review policy.

### Exit Criteria

- Unapproved visual diffs and serious accessibility violations block merge.
- Core screens have zero global horizontal overflow at supported viewports.

## M18 — Validate Terminal Job Behavior

**Tier:** High
**Status:** open

### Problem

Synthetic E2E passes before the async job outcome is known.

### Root Cause

The test stops at `202 pending` and static detail-page rendering.

### Target State

- Browser tests poll bounded jobs to `completed` or `cleaned_up` and validate counts.
- Failed terminal states are clear and tested.

### Files Most Likely To Change

- `apps/web/e2e/admin-synthetic.spec.ts` - terminal polling/assertions.
- Synthetic job UI - actionable failure detail if needed.
- CI environment - worker/beat dependencies.

### Action Plan

1. Add bounded API polling with explicit timeout and terminal-state parsing.
2. Assert expected smoke counts and cleanup evidence.
3. Add a controlled failed-job UI test.
4. Include cleanup so CI leaves no durable synthetic residue.

### Exit Criteria

- E2E cannot pass while the created job later fails.
- Success and failure terminal contracts are both verified.

## M19 — Remove Stale Repository Contracts

**Tier:** Medium
**Status:** open

### Problem

Manifests and docs reference missing packages and inert automation copies.

### Root Cause

Repository cleanup removed components without reconciling every contract.

### Target State

- Workspace manifests list only real packages.
- Shared schema ownership is explicit: restored as a real package or removed from contracts.
- No legacy CI definition remains ambiguous.

### Files Most Likely To Change

- `package.json` and `package-lock.json` - workspace list.
- `AGENTS.md` and architecture docs - package ownership.
- CI copies - retirement or generation.

### Action Plan

1. Decide whether shared schemas and UI packages should exist or remain app-local.
2. Reconcile workspace and lockfile entries with that decision.
3. Remove or archive inert workflow definitions.
4. Add a repository-shape check for missing declared workspaces.

### Exit Criteria

- Every declared workspace exists and installs from a clean checkout.
- Search finds no active contract referring to retired packages or CI files.

## M20 — Synchronize Operational Documentation

**Tier:** Medium
**Status:** open

### Problem

Runtime counts, validation commands, and Service Product phase status are stale.

### Root Cause

Documentation was updated per feature pass rather than reconciled after production cleanup.

### Target State

- README, AGENTS, architecture plans, and runbooks match the effective runtime and CI.
- Completed and pending Service Product phases are explicit.

### Files Most Likely To Change

- `README.md` - runtime and validation snapshot.
- `AGENTS.md` - repository layout and definition of done commands.
- `docs/architecture/service-product-library-and-verification-agent-plan.md` - phase status.

### Action Plan

1. Reconcile service count, package layout, and canonical CI commands.
2. Mark each Service Product phase with evidence-backed status.
3. Link production runbooks and ownership boundaries.
4. Add documentation drift review to release closeout.

### Exit Criteria

- A clean-clone operator can follow one documented path to reproduce every gate.
- Documentation contains no known contradictions with manifests or effective CI.

## Debt Markers

- No TODO/FIXME/HACK/PLACEHOLDER markers were found in application source.
- Three `pass` statements are intentional: abstract `Base`, and irreversible migration downgrades.
- Legacy compatibility paths remain for old canvas CSV values and historical Spanish justification records; these are explicit compatibility code, not placeholders.
- The material debt is architectural and operational rather than unfinished stub code.

## Recommended Next Actions

1. Execute M1-M6 to establish trusted identity and a production deployment boundary.
2. Execute M13-M15 before release to make the complete definition of done and security posture enforceable.
3. Execute M7-M12 to establish durable operations and eliminate dual Service Product rule ownership.
4. Execute M16, M18, M17, M19, and M20 for browser depth, visual/accessibility coverage, and repository consistency.

## Dependency Drift

- npm audit currently reports 5 fixable vulnerabilities: 2 low, 1 moderate, and 2 high.
- The production dependency path includes `form-data@4.0.5` through `axios@1.17.0`; the advisory reports a fix is available.
- Vite, esbuild, Babel, and js-yaml findings are development-tool paths, but still require lockfile maintenance and CI policy.
- Python top-level packages are exactly pinned in `requirements.txt`, but transitive versions are not locked and no Python vulnerability scanner or SBOM gate exists.
- Infrastructure images are not scanned in CI, and MinIO uses the mutable `latest` tag.

## Audit Limitations

- Docker Desktop was not running, so current live API, PostgreSQL migration, worker, storage, browser, and visual probes were not executed.
- The local Python venv and Node installation are stale relative to the lock/manifests; clean CI evidence was used where appropriate and environmental failures were not promoted to product defects.
- No source files, migrations, configuration, or runtime state were modified during this audit.
