# Project Audit Report

**Generated:** 2026-04-15 13:33 CST
**Repository:** https://github.com/javierchan/oci.git
**Branch:** codex/codex-active-work
**Commit:** `aa0c602 docs: refresh project audit report`
**Auditor:** Codex (automated)

---

## Executive Summary

This audit covers the `OCI DIS Blueprint` codebase only, not the sibling `pricing/` project in the same Git repository.

Execution location and write location resolved to the same path in this run:

- Execution context: `/Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint`
- Branch-backed checkout used for report output: `/Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint`

Reason: `git worktree list --porcelain` shows `codex/codex-active-work` materialized only in `/Users/javierchan/.codex/worktrees/b840/oci`, so this linked worktree is the only branch checkout available for that branch.

The codebase is much healthier than the earlier `main`-based audit. The API surface now exposes the missing catalog, graph, lineage, governance, export, dashboard, and audit endpoints; the local Docker stack is healthy; parity tests, Ruff, mypy, TypeScript, and ESLint are all green; and live probes show real project, catalog, snapshot, justification, export, and audit data flowing through the mounted application.

Current summary:

- Total milestones assessed: 15
- Complete: 13
- Partial: 2
- Not started: 0
- In progress: 0
- Critical blockers: 1

Definition-of-done metrics:

- Documented checklist items in `AGENTS.md` (M1-M8): 38
- Items verified: 36
- Items with gaps: 2

Key findings:

- The import and recalculation worker tasks exist in `apps/api/app/workers/`, but the mounted routers still execute those operations synchronously instead of dispatching through Celery as specified in `AGENTS.md`.
- The branch being audited is not clean: four tracked files are modified locally, so the exact validated state is not yet reproducible from the audited commit.
- Aside from those two execution-path deviations and the dirty worktree state, the codebase is functionally complete and live API probes succeed across the major milestone areas.

---

## Repository Profile

Selected codebase root: `OCI DIS Blueprint/`

Project profile:

- Backend: FastAPI in `apps/api/`
- Frontend: Next.js 14 / TypeScript in `apps/web/`
- Calc engine: pure Python in `packages/calc-engine/`
- Docker stack: `docker-compose.yml`
- Migrations: `apps/api/migrations/` and `apps/api/alembic.ini`
- Tests: `packages/calc-engine/src/tests`

Repo-shape detection:

```text
Python:
apps/api/requirements.txt
Node:
apps/web/package.json
package.json
Docker:
docker-compose.yml
Migrations:
apps/api/alembic.ini
apps/api/migrations
Tests:
packages/calc-engine/src/tests
CI:
```

Shell note:

- The active shell is `zsh`.
- Repo-shape checks were run under `bash -lc 'shopt -s nullglob; ...'` to avoid unmatched-glob failures.

Repository summary:

```text
oldest=2026-01-07
newest=2026-04-14
commits=227
shortlog:
status:
 M apps/api/app/routers/recalculate.py
 M apps/api/app/schemas/volumetry.py
 M apps/api/app/services/recalc_service.py
 M docs/progress.md
stash:
worktrees:
worktree /Users/javierchan/Documents/GitHub/oci
HEAD 155b2036e0dd687521645b16595303fc84f411ce
branch refs/heads/vscode/codex-active-work

worktree /Users/javierchan/.codex/worktrees/7ea6/oci
HEAD 6990ea595854c3cdd8faf67948cef375a07f7a23
detached

worktree /Users/javierchan/.codex/worktrees/b840/oci
HEAD aa0c60233b29295de00e2090fe67cb944b313685
branch refs/heads/codex/codex-active-work

source_files=122
cloc:
```

Interpretation:

- `git status --short` is not clean at audit time, so the validated application state includes uncommitted local changes beyond commit `aa0c602`.
- `git shortlog` returned no visible output in this environment.
- `cloc` was unavailable or produced no output in this environment.

Recent activity:

```text
Commits in prior 7 days: 2
Commits in last 7 days: 38
```

Debt markers:

```text
./apps/api/app/routers/projects.py:62:    # TODO: partial update + audit
```

---

## Milestone Status

### M1 — Schema + Migrations
**Status:** ✅ Complete

Evidence:

- Migration files exist in `apps/api/migrations/versions/20260413_0001_initial_schema.py`, `apps/api/migrations/versions/20260414_0002_prompt_template_versions.py`, and `apps/api/migrations/versions/20260414_0003_add_is_system_to_pattern_definitions.py`.
- Live governance and reference probes succeed:
  - `patterns=17`
  - `dictionary_frequency=13`
  - `default_assumption=1.0.0`
- Correct database tables are present in the live PostgreSQL container, including `pattern_definitions`, `dictionary_options`, and `assumption_sets`.

Gaps:

- None confirmed.

Deviation:

- None.

### M2 — Import Engine
**Status:** ⚠ Partial

Evidence:

- Import service implementation exists in `apps/api/app/services/import_service.py`.
- Celery worker exists in `apps/api/app/workers/import_worker.py`.
- Live import probes succeed:
  - `import_batches=1 latest_status=completed latest_loaded=144`
  - `source_rows_total=170 page_rows=5`
- Calc-engine parity tests pass: `26 passed in 0.07s`.

Gaps:

- `apps/api/app/routers/imports.py` calls `import_service.process_import(...)` directly inside the request path instead of dispatching to `app.workers.import_worker.process_import_task`.

Deviation:

- The route returns HTTP `202 Accepted` while doing synchronous in-request processing, which diverges from the queued worker design documented in `AGENTS.md`.

### M3 — Catalog Grid API
**Status:** ✅ Complete

Evidence:

- Catalog service exists in `apps/api/app/services/catalog_service.py`.
- OpenAPI exposes the full catalog surface:
  - `/api/v1/catalog/{project_id}`
  - `/api/v1/catalog/{project_id}/bulk-patch`
  - `/api/v1/catalog/{project_id}/{integration_id}`
  - `/api/v1/catalog/{project_id}/{integration_id}/lineage`
- Live catalog probe succeeds: `catalog_total=144 page_rows=50`.
- Live lineage probe succeeds: `column_names=True import_filename=parity_import.xlsx`.

Gaps:

- None confirmed.

Deviation:

- None.

### M4 — Calculation Engine
**Status:** ⚠ Partial

Evidence:

- Recalculation service exists in `apps/api/app/services/recalc_service.py`.
- Celery worker exists in `apps/api/app/workers/recalc_worker.py`.
- Live volumetry probes succeed:
  - `volumetry_snapshots=3 latest=af099596-b20d-44fd-8045-3abbab6bd9ec`
  - `scoped_status=completed snapshot_id=a14f0a54-344d-4b51-922e-99d152e7d07c`
- Consolidated metrics endpoint responds with the expected shape:
  - `['data_integration', 'functions', 'oic', 'queue', 'streaming']`

Gaps:

- `apps/api/app/routers/recalculate.py` still invokes `recalc_service.recalculate_project(...)` directly instead of dispatching to `app.workers.recalc_worker.recalculate_project_task`.

Deviation:

- The route is documented and typed like an asynchronous job trigger but currently performs synchronous recalculation in the API process.

### M5 — Dashboard API
**Status:** ✅ Complete

Evidence:

- Dashboard service exists in `apps/api/app/services/dashboard_service.py`.
- OpenAPI exposes:
  - `/api/v1/dashboard/{project_id}/snapshots`
  - `/api/v1/dashboard/{project_id}/snapshots/{snapshot_id}`
- Live probes succeed:
  - `dashboard_snapshots=3 latest=fc29e7ce-c66e-47ce-a7a3-a8aa43fa0b62`
  - `dashboard_mode=technical risks=0`

Gaps:

- None confirmed.

Deviation:

- None.

### M6 — Justification Narratives
**Status:** ✅ Complete

Evidence:

- Narrative service exists in `apps/api/app/services/justification_service.py`.
- OpenAPI exposes both template governance and record actions under `/api/v1/justifications`.
- Live justification probe succeeds:
  - `justification_state=draft`
  - `narrative_keys=['evidence', 'methodology_blocks', 'override_text', 'qa_reasons', 'qa_status', 'summary']`

Gaps:

- None confirmed.

Deviation:

- None.

### M7 — Exports
**Status:** ✅ Complete

Evidence:

- Export service exists in `apps/api/app/services/export_service.py`.
- Template route is exposed: `/api/v1/exports/template/xlsx`.
- Live export probes succeed:
  - `template_bytes=6313`
  - `HTTP/1.1 200 OK`
  - `export_status=completed format=json`
  - `xlsx_export_status=completed filename=a51bb83a-110b-4226-94f0-d2f590e3cd1d-af099596-b20d-44fd-8045-3abbab6bd9ec.xlsx`
  - `pdf_export_status=completed filename=a51bb83a-110b-4226-94f0-d2f590e3cd1d-af099596-b20d-44fd-8045-3abbab6bd9ec.pdf`

Gaps:

- None confirmed.

Deviation:

- None.

### M8 — Admin + Governance
**Status:** ✅ Complete

Evidence:

- Governance routes are registered:
  - `/api/v1/patterns/`
  - `/api/v1/dictionaries/`
  - `/api/v1/assumptions/`
- Service implementations exist in `apps/api/app/services/reference_service.py`.
- Live probes succeed:
  - `patterns=17`
  - `dictionary_frequency=13`
  - `default_assumption=1.0.0`

Gaps:

- None confirmed.

Deviation:

- None.

### M9 — Integration Capture Wizard
**Status:** ✅ Complete

Evidence:

- The frontend contract in `apps/web/lib/api.ts` is backed by registered backend routes:
  - `/api/v1/catalog/{project_id}/systems`
  - `/api/v1/catalog/{project_id}/duplicates`
  - `/api/v1/catalog/{project_id}/estimate`
  - `POST /api/v1/catalog/{project_id}`
- The capture-related UI exists in `apps/web/app/projects/[projectId]/capture/` and `apps/web/components/capture-*.tsx`.

Gaps:

- None confirmed.

Deviation:

- The capture flow depends on the same synchronous import/recalculation architecture noted in M2 and M4, but no separate M9-specific gap was confirmed.

### M10 — System Dependency Map
**Status:** ✅ Complete

Evidence:

- Graph service exists in `apps/api/app/services/graph_service.py`.
- Graph UI exists in `apps/web/app/projects/[projectId]/graph/page.tsx` and `apps/web/components/integration-graph.tsx`.
- Live graph probe succeeds: `graph_nodes=2 graph_edges=1`.

Gaps:

- None confirmed.

Deviation:

- None.

### M11 — Navigation + Theme
**Status:** ✅ Complete

Evidence:

- Theme and navigation artifacts exist in:
  - `apps/web/components/breadcrumb.tsx`
  - `apps/web/components/theme-toggle.tsx`
  - `apps/web/lib/use-theme.ts`
  - `apps/web/app/layout.tsx`
  - `apps/web/app/globals.css`
- Frontend checks are clean:
  - TypeScript: exit code 0
  - ESLint: exit code 0

Gaps:

- None confirmed.

Deviation:

- None.

### M12 — Source Lineage + Template
**Status:** ✅ Complete

Evidence:

- Lineage artifacts exist in `apps/api/app/schemas/catalog.py` and `apps/api/app/services/catalog_service.py`.
- Template generation exists in `apps/api/app/services/export_service.py`.
- Live probes succeed:
  - `column_names=True import_filename=parity_import.xlsx`
  - `template_bytes=6313`

Gaps:

- None confirmed.

Deviation:

- None.

### M13 — Integration Design Canvas
**Status:** ✅ Complete

Evidence:

- Design canvas component exists in `apps/web/components/integration-canvas.tsx`.
- The detail flow wiring exists in `apps/web/app/projects/[projectId]/catalog/[integrationId]/page.tsx`.
- Frontend quality gates remain green after the latest backend cleanup.

Gaps:

- None confirmed.

Deviation:

- None.

### M14 — Map Pan + Visual Improvements
**Status:** ✅ Complete

Evidence:

- Graph interaction artifacts exist in:
  - `apps/web/components/graph-controls.tsx`
  - `apps/web/components/integration-graph.tsx`
  - `apps/web/app/projects/[projectId]/graph/page.tsx`
- Frontend checks remain green and the live graph endpoint responds successfully.

Gaps:

- None confirmed.

Deviation:

- None.

### Browser QA — Bug Fixes + UX Enhancements
**Status:** ✅ Complete

Evidence:

- Browser QA remediation is documented in `docs/progress.md`.
- Supporting code artifacts exist across the web app, including:
  - `apps/web/components/system-autocomplete.tsx`
  - `apps/web/components/capture-wizard.tsx`
  - `apps/web/app/projects/[projectId]/catalog/[integrationId]/page.tsx`
  - `apps/web/app/projects/page.tsx`
  - `apps/web/app/admin/page.tsx`
- Current frontend gates are clean:
  - TypeScript: exit code 0
  - ESLint: exit code 0

Gaps:

- None confirmed.

Deviation:

- None.

---

## Milestone Index

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [M1](#m1--align-import-dispatch-contract) | Align Import Dispatch Contract | High | open |
| [M2](#m2--route-imports-through-celery-worker-lifecycle) | Route Imports Through Celery Worker Lifecycle | High | open |
| [M3](#m3--validate-async-import-observability) | Validate Async Import Observability | Medium | open |
| [M4](#m4--align-recalculation-dispatch-contract) | Align Recalculation Dispatch Contract | High | open |
| [M5](#m5--route-recalculation-through-celery-worker-lifecycle) | Route Recalculation Through Celery Worker Lifecycle | High | open |
| [M6](#m6--validate-async-recalculation-job-flow) | Validate Async Recalculation Job Flow | Medium | open |
| [M7](#m7--checkpoint-the-audited-branch-state) | Checkpoint The Audited Branch State | Critical | open |
| [M8](#m8--rerun-release-audit-on-clean-head) | Rerun Release Audit On Clean HEAD | Medium | open |

---

## Verification Results

### Backend

```text
./.venv/bin/python -m pytest --tb=short -q 2>&1 | tail -40
..........................                                               [100%]
26 passed in 0.07s
```

```text
./.venv/bin/python -m ruff check . 2>&1 | tail -20
All checks passed!
```

```text
./.venv/bin/python -m mypy apps/api/app --ignore-missing-imports --no-error-summary 2>&1 | tail -10
```

### Frontend

```text
cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | tail -30
```

```text
cd apps/web && npx eslint . --ext .ts,.tsx --max-warnings 0 2>&1 | tail -20
```

### Docker / Live Stack

```text
docker compose ps 2>&1
NAME                       IMAGE                    COMMAND                  SERVICE   CREATED        STATUS                  PORTS
ocidisblueprint-api-1      ocidisblueprint-api      "uvicorn app.main:ap…"   api       24 hours ago   Up 24 hours (healthy)   0.0.0.0:8000->8000/tcp, [::]:8000->8000/tcp
ocidisblueprint-db-1       postgres:16-alpine       "docker-entrypoint.s…"   db        38 hours ago   Up 38 hours (healthy)   0.0.0.0:5432->5432/tcp, [::]:5432->5432/tcp
ocidisblueprint-minio-1    minio/minio:latest       "/usr/bin/docker-ent…"   minio     38 hours ago   Up 38 hours (healthy)   0.0.0.0:9000-9001->9000-9001/tcp, [::]:9000-9001->9000-9001/tcp
ocidisblueprint-redis-1    redis:7-alpine           "docker-entrypoint.s…"   redis     38 hours ago   Up 38 hours (healthy)   0.0.0.0:6379->6379/tcp, [::]:6379->6379/tcp
ocidisblueprint-web-1      ocidisblueprint-web      "docker-entrypoint.s…"   web       24 hours ago   Up 24 hours             0.0.0.0:3000->3000/tcp, [::]:3000->3000/tcp
ocidisblueprint-worker-1   ocidisblueprint-worker   "celery -A app.worke…"   worker    38 hours ago   Up 38 hours             8000/tcp
```

```text
docker compose ps --format json 2>/dev/null | python3 -c "..."
```

```text
curl -sf http://localhost:8000/health 2>/dev/null && echo "API: UP" || echo "API: DOWN"
{"status":"ok","version":"1.0.0"}API: UP
```

```text
curl -sf http://localhost:8000/openapi.json 2>/dev/null | python3 -c "..."
Registered endpoints: 46 paths, 64 operations
```

### Database

Template probe from the audit skill:

```text
docker compose exec -T db psql -U postgres -d dis_blueprint -c "SELECT schemaname, tablename, n_live_tup AS rows FROM pg_stat_user_tables ORDER BY tablename;"
psql: error: connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed: FATAL:  role "postgres" does not exist
```

Corrected probe for this codebase's actual credentials:

```text
docker compose exec -T db psql -U dis -d oci_dis -c "SELECT schemaname, relname, n_live_tup AS rows FROM pg_stat_user_tables ORDER BY relname;" | head -30
 schemaname |         relname          | rows 
------------+--------------------------+------
 public     | alembic_version          |    1
 public     | assumption_sets          |    2
 public     | audit_events             |  261
 public     | catalog_integrations     |  891
 public     | dashboard_snapshots      |   14
 public     | dictionary_options       |   40
 public     | import_batches           |   15
 public     | justification_records    |   14
 public     | pattern_definitions      |   17
 public     | projects                 |   10
 public     | prompt_template_versions |    3
 public     | source_integration_rows  |  986
 public     | volumetry_snapshots      |   22
(13 rows)
```

### Key Live Probes

```text
projects=8
catalog_total=144 page_rows=50
patterns=17
dictionary_frequency=13
default_assumption=1.0.0
graph_nodes=2 graph_edges=1
volumetry_snapshots=3 latest=af099596-b20d-44fd-8045-3abbab6bd9ec
dashboard_snapshots=3 latest=fc29e7ce-c66e-47ce-a7a3-a8aa43fa0b62
dashboard_mode=technical risks=0
column_names=True import_filename=parity_import.xlsx
justification_state=draft narrative_keys=['evidence', 'methodology_blocks', 'override_text', 'qa_reasons', 'qa_status', 'summary']
template_bytes=6313
HTTP/1.1 200 OK
import_batches=1 latest_status=completed latest_loaded=144
source_rows_total=170 page_rows=5
export_status=completed format=json
xlsx_export_status=completed filename=a51bb83a-110b-4226-94f0-d2f590e3cd1d-af099596-b20d-44fd-8045-3abbab6bd9ec.xlsx
pdf_export_status=completed filename=a51bb83a-110b-4226-94f0-d2f590e3cd1d-af099596-b20d-44fd-8045-3abbab6bd9ec.pdf
audit_total=149 page_events=3 first_event=recalculation
scoped_status=completed snapshot_id=a14f0a54-344d-4b51-922e-99d152e7d07c
```

### OpenAPI Registered Paths

```text
/api/v1/assumptions/
/api/v1/assumptions/default
/api/v1/assumptions/{version}
/api/v1/assumptions/{version}/default
/api/v1/audit/{project_id}
/api/v1/catalog/{project_id}
/api/v1/catalog/{project_id}/bulk-patch
/api/v1/catalog/{project_id}/duplicates
/api/v1/catalog/{project_id}/estimate
/api/v1/catalog/{project_id}/graph
/api/v1/catalog/{project_id}/systems
/api/v1/catalog/{project_id}/{integration_id}
/api/v1/catalog/{project_id}/{integration_id}/lineage
/api/v1/dashboard/{project_id}/snapshots
/api/v1/dashboard/{project_id}/snapshots/{snapshot_id}
/api/v1/dictionaries/
/api/v1/dictionaries/{category}
/api/v1/dictionaries/{category}/{option_id}
/api/v1/exports/template/xlsx
/api/v1/exports/{project_id}/jobs/{job_id}
/api/v1/exports/{project_id}/jobs/{job_id}/download
/api/v1/exports/{project_id}/json
/api/v1/exports/{project_id}/pdf
/api/v1/exports/{project_id}/xlsx
/api/v1/imports/{project_id}
/api/v1/imports/{project_id}/{batch_id}
/api/v1/imports/{project_id}/{batch_id}/rows
/api/v1/justifications/templates
/api/v1/justifications/templates/{version}
/api/v1/justifications/templates/{version}/default
/api/v1/justifications/{project_id}
/api/v1/justifications/{project_id}/{integration_id}
/api/v1/justifications/{project_id}/{integration_id}/approve
/api/v1/justifications/{project_id}/{integration_id}/override
/api/v1/patterns/
/api/v1/patterns/{pattern_id}
/api/v1/projects/
/api/v1/projects/{project_id}
/api/v1/projects/{project_id}/archive
/api/v1/recalculate/{project_id}
/api/v1/recalculate/{project_id}/jobs/{job_id}
/api/v1/recalculate/{project_id}/scoped
/api/v1/volumetry/{project_id}/snapshots
/api/v1/volumetry/{project_id}/snapshots/{snapshot_id}
/api/v1/volumetry/{project_id}/snapshots/{snapshot_id}/consolidated
/health
```

---

## Pending Tasks

### CRITICAL

- `git status --short` / milestone traceability — release readiness — the branch has uncommitted changes in `apps/api/app/routers/recalculate.py`, `apps/api/app/schemas/volumetry.py`, `apps/api/app/services/recalc_service.py`, and `docs/progress.md`, so the audited state is not reproducible from commit `aa0c602`.

### INCOMPLETE

- `apps/api/app/routers/imports.py` — M2 — import upload executes synchronously in the request path instead of dispatching to the existing Celery worker.
- `apps/api/app/routers/recalculate.py` — M4 — recalculation endpoints execute synchronously in the API process instead of dispatching to the existing Celery worker.

### DEFERRED

- Dependency upgrades across Python and web toolchains — maintenance — significant drift exists, but the current stack is green and upgrades should happen in a dedicated compatibility wave rather than as part of this release audit.

---

## Finding Remediation Plans

### Finding: Import API bypasses the worker path
- Classification: `INCOMPLETE`
- Impact: Delivery and operational scalability risk; import behavior diverges from the documented queued architecture.
- Area: M2 — Import Engine
- Evidence:
  - `apps/api/app/routers/imports.py` — route calls `import_service.process_import(...)` directly.
  - `apps/api/app/workers/import_worker.py` — `process_import_task` exists but is not used by the router.
  - Live probe — `import_batches=1 latest_status=completed latest_loaded=144` shows the import path is functionally working.
- Issue: The import milestone is functionally complete but not wired to Celery as specified, so the runtime contract and the documented architecture still diverge.
- Remediation Milestones:
  - `M1` — Align Import Dispatch Contract (`High`, `open`): define the exact queued response model and import status lifecycle the API should expose.
  - `M2` — Route Imports Through Celery Worker Lifecycle (`High`, `open`): replace direct service execution with task dispatch and persisted batch state transitions.
  - `M3` — Validate Async Import Observability (`Medium`, `open`): verify batch polling, failure handling, and operator-visible import progress.

### Finding: Recalculation API bypasses the worker path
- Classification: `INCOMPLETE`
- Impact: Delivery and operational scalability risk; recalculation behavior diverges from the documented job-based architecture.
- Area: M4 — Calculation Engine
- Evidence:
  - `apps/api/app/routers/recalculate.py` — route calls `recalc_service.recalculate_project(...)` directly.
  - `apps/api/app/workers/recalc_worker.py` — `recalculate_project_task` exists but is not used by the router.
  - Live probe — `scoped_status=completed snapshot_id=a14f0a54-344d-4b51-922e-99d152e7d07c` confirms the feature works, but synchronously.
- Issue: Recalculation is implemented and validated, but the API still runs the heavy work inside the request cycle instead of using the already-present worker infrastructure.
- Remediation Milestones:
  - `M4` — Align Recalculation Dispatch Contract (`High`, `open`): define the job and snapshot lookup semantics the routes should expose.
  - `M5` — Route Recalculation Through Celery Worker Lifecycle (`High`, `open`): dispatch full and scoped recalculations through Celery and persist job-state transitions.
  - `M6` — Validate Async Recalculation Job Flow (`Medium`, `open`): verify UI polling, snapshot creation, and error recovery against the queued model.

### Finding: Audited branch state is not reproducible from HEAD
- Classification: `CRITICAL`
- Impact: Release and audit traceability risk; the validated state cannot be reconstructed from the audited commit alone.
- Area: Repository hygiene / release readiness
- Evidence:
  - `git status --short` — four modified tracked files are present at audit time.
  - `git log --oneline -1` — current commit is `aa0c602 docs: refresh project audit report`, which does not include the audited local modifications.
- Issue: The branch contains validated but uncommitted changes, so a downstream engineer or CI job checking out the audited commit will not see the same state described in this report.
- Remediation Milestones:
  - `M7` — Checkpoint The Audited Branch State (`Critical`, `open`): isolate, validate, and commit the current local changes into reproducible history.
  - `M8` — Rerun Release Audit On Clean HEAD (`Medium`, `open`): rerun the audit once the branch is clean so the report points to a committed state.

---

## M1 — Align Import Dispatch Contract

**Tier:** High
**Status:** open

### Problem

The import router advertises an asynchronous `202 Accepted` workflow, but it performs workbook ingestion synchronously inside the request handler.

### Root Cause

The import service and import Celery worker both exist, but the mounted route in `apps/api/app/routers/imports.py` was wired to the service directly instead of to the worker-backed lifecycle.

### Target State

- `POST /api/v1/imports/{project_id}` returns a stable queued job or batch status contract.
- Import batch status transitions reflect queued, running, completed, and failed states consistently across API and UI.

### Files Most Likely To Change

- `apps/api/app/routers/imports.py` — request/response and dispatch behavior
- `apps/api/app/services/import_service.py` — batch state transitions and serialization
- `apps/api/app/schemas/imports.py` — queued lifecycle response fields
- `apps/web/lib/api.ts` — client expectations if response shape changes

### Action Plan

1. Define the canonical import lifecycle contract and align route status codes, response payloads, and batch-state semantics with that model.
2. Update the router to create the batch, persist the upload, and dispatch to `process_import_task` instead of running the import synchronously.
3. Ensure batch polling and failure-state serialization remain usable from the existing import history UI.
4. Update any affected frontend client typings or polling assumptions.

### Exit Criteria

- `POST /api/v1/imports/{project_id}` no longer performs workbook parsing in the request cycle.
- Batch status can be polled to observe queued or running transitions before completion.
- Existing import history and row listing routes still work after a queued import completes.

## M2 — Route Imports Through Celery Worker Lifecycle

**Tier:** High
**Status:** open

### Problem

Even with a defined queued contract, the actual execution path still needs to move from the API process to the worker process.

### Root Cause

The current implementation uses the service layer directly in the API route, bypassing Redis/Celery despite worker infrastructure already being present.

### Target State

- Imports run in the Celery worker.
- The API process is limited to validation, upload persistence, and job dispatch.

### Files Most Likely To Change

- `apps/api/app/routers/imports.py` — worker dispatch
- `apps/api/app/workers/import_worker.py` — task payload and error handling
- `apps/api/app/core/config.py` — if job-routing settings need alignment
- `apps/api/app/services/import_service.py` — persisted state transitions

### Action Plan

1. Replace direct `process_import(...)` invocation with Celery task submission and initial batch state persistence.
2. Ensure the worker updates batch status and failure details through the same serialization path used by the API.
3. Add or update tests around worker-dispatched imports and error handling.
4. Verify that upload persistence paths remain valid for the worker process.

### Exit Criteria

- Import tasks are visible in Celery worker logs during execution.
- The API route returns before the import is completed.
- Failed imports persist actionable `error_details` without leaving orphaned batch state.

## M3 — Validate Async Import Observability

**Tier:** Medium
**Status:** open

### Problem

After rerouting imports to Celery, the system still needs operator-visible feedback and validation coverage for the async lifecycle.

### Root Cause

The current happy-path validation was performed against synchronous execution, so async-specific observability has not been proven yet.

### Target State

- Import status, row counts, and failure information are observable and trustworthy in the UI and API.
- Validation artifacts demonstrate the queued workflow end-to-end.

### Files Most Likely To Change

- `apps/web/app/projects/[projectId]/import/page.tsx` — polling or status presentation if needed
- `apps/web/lib/api.ts` — client polling helpers if needed
- `docs/progress.md` — validation evidence once complete

### Action Plan

1. Run an end-to-end queued import using a known workbook or fixture and capture the status transitions.
2. Verify that the import history page shows batch state correctly while the worker is processing.
3. Validate failure handling with a controlled bad import or simulated worker exception path.
4. Document the verified async import behavior in progress tracking docs.

### Exit Criteria

- An import can be observed moving through queued or running to completed.
- Import history and row inspection remain correct after async completion.
- Validation evidence for the queued path is recorded in repository docs.

## M4 — Align Recalculation Dispatch Contract

**Tier:** High
**Status:** open

### Problem

The recalculation router exposes job-like endpoints but executes recalculation immediately in the API process.

### Root Cause

The service layer and worker task were both implemented, but the route wiring stopped at direct service invocation.

### Target State

- Recalculation routes expose a consistent queued job model for full and scoped runs.
- Snapshot lookup semantics remain stable after execution moves to the worker.

### Files Most Likely To Change

- `apps/api/app/routers/recalculate.py` — route behavior and response models
- `apps/api/app/schemas/volumetry.py` — job contract schemas
- `apps/api/app/services/recalc_service.py` — job lookup semantics and state persistence
- `apps/web/components/recalculate-button.tsx` — UI expectations if response timing changes

### Action Plan

1. Decide the canonical job contract for full and scoped recalculation, including queued, running, completed, and failed states.
2. Align route response models and status codes with that contract.
3. Preserve existing snapshot lookup behavior so the dashboard and volumetry screens remain stable.
4. Update any frontend assumptions that currently depend on immediate snapshot availability.

### Exit Criteria

- Recalculation route responses describe job state rather than immediate completion when work is queued.
- Job polling remains able to resolve the final snapshot identifier.
- No existing dashboard or volumetry page breaks because of the contract update.

## M5 — Route Recalculation Through Celery Worker Lifecycle

**Tier:** High
**Status:** open

### Problem

The recalculation workload still runs synchronously in the API process despite an available Celery worker implementation.

### Root Cause

`apps/api/app/routers/recalculate.py` invokes `recalc_service.recalculate_project(...)` directly instead of dispatching to `recalculate_project_task`.

### Target State

- Full and scoped recalculation run through Celery.
- The API process is limited to job submission and status lookup.

### Files Most Likely To Change

- `apps/api/app/routers/recalculate.py` — Celery dispatch
- `apps/api/app/workers/recalc_worker.py` — task payload and metadata
- `apps/api/app/services/recalc_service.py` — state persistence for queued runs

### Action Plan

1. Update the router to enqueue recalculation tasks and return job descriptors instead of immediate snapshots.
2. Ensure the worker persists snapshot metadata needed by `GET /jobs/{job_id}` and the existing volumetry endpoints.
3. Extend validation coverage for full and scoped recalculation under worker execution.
4. Confirm audit events still emit correctly for queued recalculation completion.

### Exit Criteria

- Recalculation tasks execute in the worker process.
- `GET /api/v1/recalculate/{project_id}/jobs/{job_id}` resolves a real queued-job lifecycle.
- Snapshot creation and audit events remain correct after the dispatch change.

## M6 — Validate Async Recalculation Job Flow

**Tier:** Medium
**Status:** open

### Problem

Once recalculation is worker-backed, the API and UI need proof that polling and downstream snapshot consumption still work.

### Root Cause

Current validation covers synchronous recalculation success, not the queued lifecycle described in the project plan.

### Target State

- The recalculation UI and API can observe queued job progression to a completed snapshot.
- Dashboard and volumetry screens behave correctly after queued recalculation completes.

### Files Most Likely To Change

- `apps/web/components/recalculate-button.tsx` — user feedback and polling behavior
- `apps/web/app/projects/[projectId]/page.tsx` — dashboard refresh behavior
- `docs/progress.md` — validation evidence

### Action Plan

1. Run a full queued recalculation and verify job polling through to snapshot availability.
2. Confirm the dashboard and volumetry pages reflect the new snapshot after completion.
3. Test a scoped recalculation to ensure the scoped job model is also valid.
4. Record the validated async lifecycle in progress documentation.

### Exit Criteria

- Full and scoped recalculation can be observed from submission through completion.
- The UI refreshes correctly once the snapshot becomes available.
- Documentation captures the verified queued recalculation flow.

## M7 — Checkpoint The Audited Branch State

**Tier:** Critical
**Status:** open

### Problem

The audit validated a branch state that is not yet committed, so the report cannot be reproduced from the referenced commit.

### Root Cause

Recent recalculation cleanup and progress updates remain local modifications at audit time.

### Target State

- The branch is clean.
- The audited code and docs state is fully represented in git history.

### Files Most Likely To Change

- `apps/api/app/routers/recalculate.py` — current local cleanup
- `apps/api/app/schemas/volumetry.py` — current local cleanup
- `apps/api/app/services/recalc_service.py` — current local cleanup
- `docs/progress.md` — current local documentation update

### Action Plan

1. Review the current local modifications and confirm they belong to the intended branch scope.
2. Run the relevant verification commands again against the staged changes.
3. Commit the validated changes in logical units with clear messages.
4. Confirm `git status --short` is clean before any release or merge action.

### Exit Criteria

- `git status --short` returns no changes.
- The validated recalculation cleanup exists in committed history.
- The branch can be checked out elsewhere and matches the audited state.

## M8 — Rerun Release Audit On Clean HEAD

**Tier:** Medium
**Status:** open

### Problem

This audit is useful, but it references a dirty worktree and therefore cannot serve as the final release-readiness artifact.

### Root Cause

The audit was correctly run after local validation, but before the branch was checkpointed into clean history.

### Target State

- A clean-head audit exists and references a committed, reproducible state.
- Any remaining gaps are limited to genuine product or architecture decisions.

### Files Most Likely To Change

- `docs/reports/audit-report-*.md` — fresh clean-head audit output
- `docs/progress.md` — optional summary of audit rerun

### Action Plan

1. After M7 completes, rerun the audit from the clean branch head.
2. Compare the new report against this one and verify that the dirty-worktree blocker is gone.
3. Update progress documentation or release notes to reference the clean-head audit.
4. Use the fresh audit as the handoff artifact for the next execution wave.

### Exit Criteria

- A new audit report references a clean `git status --short`.
- The report commit hash matches the actual audited code state.
- Remaining open findings are limited to active engineering work, not branch hygiene.

---

## Debt Markers

Confirmed debt markers outside tests:

```text
./apps/api/app/routers/projects.py:62:    # TODO: partial update + audit
```

Notes:

- The earlier router-placeholder debt called out in the previous audit is no longer present for this branch.
- Current debt is narrow and concentrated in project update/audit completeness plus the remaining worker-dispatch drift.

---

## Recommended Next Actions

1. Commit the current local recalculation cleanup and `docs/progress.md` update so the audited branch state becomes reproducible.
2. Decide whether the release contract is truly asynchronous for import and recalculation; if yes, work M1-M6 in order and route both paths through Celery.
3. Rerun this audit on a clean branch head after checkpointing so the release artifact points to committed history.
4. Keep dependency upgrades in a separate maintenance wave after the worker-dispatch alignment is complete.

---

## Dependency Drift

Python package drift:

```text
Package           Version Latest  Type
----------------- ------- ------- -----
aiosqlite         0.20.0  0.22.1  wheel
alembic           1.13.1  1.18.4  wheel
asyncpg           0.29.0  0.31.0  wheel
celery            5.4.0   5.6.3   wheel
coverage          7.5.1   7.13.5  wheel
fastapi           0.111.0 0.135.3 wheel
greenlet          3.0.3   3.4.0   wheel
httpx             0.27.0  0.28.1  wheel
Mako              1.3.10  1.3.11  wheel
openpyxl          3.1.2   3.1.5   wheel
packaging         26.0    26.1    wheel
pandas            2.2.2   3.0.2   wheel
psycopg2-binary   2.9.9   2.9.11  wheel
pydantic          2.7.1   2.13.1  wheel
pydantic_core     2.18.2  2.46.1  wheel
pydantic-settings 2.2.1   2.13.1  wheel
pytest            8.2.0   9.0.3   wheel
pytest-asyncio    0.23.6  1.3.0   wheel
```

Frontend package drift:

```text
Package                Current    Wanted   Latest  Location                           Depended by
@types/node           20.19.39  20.19.39   25.6.0  node_modules/@types/node           web@npm:@oci-dis/web@1.0.0
@types/react           18.3.28   18.3.28  19.2.14  node_modules/@types/react          web@npm:@oci-dis/web@1.0.0
@types/react-dom        18.3.7    18.3.7   19.2.3  node_modules/@types/react-dom      web@npm:@oci-dis/web@1.0.0
@vitejs/plugin-react     4.7.0     4.7.0    6.0.1  node_modules/@vitejs/plugin-react  web@npm:@oci-dis/web@1.0.0
eslint                  8.57.1    8.57.1   10.2.0  node_modules/eslint                web@npm:@oci-dis/web@1.0.0
eslint-config-next      14.2.3    14.2.3   16.2.3  node_modules/eslint-config-next    web@npm:@oci-dis/web@1.0.0
lucide-react           0.383.0   0.383.0    1.8.0  node_modules/lucide-react          web@npm:@oci-dis/web@1.0.0
next                    14.2.3    14.2.3   16.2.3  node_modules/next                  web@npm:@oci-dis/web@1.0.0
postcss                  8.5.9    8.5.10   8.5.10  node_modules/postcss               web@npm:@oci-dis/web@1.0.0
react                   18.3.1    18.3.1   19.2.5  node_modules/react                 web@npm:@oci-dis/web@1.0.0
react-dom               18.3.1    18.3.1   19.2.5  node_modules/react-dom             web@npm:@oci-dis/web@1.0.0
recharts                2.15.4    2.15.4    3.8.1  node_modules/recharts              web@npm:@oci-dis/web@1.0.0
tailwind-merge           2.6.1     2.6.1    3.5.0  node_modules/tailwind-merge        web@npm:@oci-dis/web@1.0.0
tailwindcss             3.4.19    3.4.19    4.2.2  node_modules/tailwindcss           web@npm:@oci-dis/web@1.0.0
typescript               5.9.3     5.9.3    6.0.2  node_modules/typescript            web@npm:@oci-dis/web@1.0.0
vitest                   1.6.1     1.6.1    4.1.4  node_modules/vitest                web@npm:@oci-dis/web@1.0.0
zod                    3.25.76   3.25.76    4.3.6  node_modules/zod                   web@npm:@oci-dis/web@1.0.0
zustand                  4.5.7     4.5.7   5.0.12  node_modules/zustand               web@npm:@oci-dis/web@1.0.0
```

Assessment:

- Drift is meaningful but not currently breaking the validated local stack.
- Dependency upgrades should be handled in a dedicated compatibility wave after the worker-dispatch and reproducibility issues are resolved.
