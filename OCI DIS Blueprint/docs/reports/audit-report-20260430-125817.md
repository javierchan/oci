# Project Audit Report

**Generated:** 2026-04-30 12:58 America/Mexico_City
**Repository:** local git repository at `/Users/javierchan/.codex/worktrees/b840/oci`
**Selected Codebase:** `OCI DIS Blueprint`
**Execution Location:** `/Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint`
**Report Destination:** `/Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint/docs/reports/audit-report-20260430-125817.md`
**Branch:** `codex/codex-active-work`
**Commit:** `a06875a58e0089dc470068875f12a16007ca590f`
**Auditor:** Codex (automated)

---

## Executive Summary

The repository documents OCI DIS Blueprint as complete through M24, and the local stack is running with one populated demo project. However, the audit found that delivery readiness is not clean yet. The current working tree contains an uncommitted, validated recovery for the catalog/graph route, several API routers still return hardcoded empty payloads, backend tests fail, backend type checking fails, and the committed OpenAPI artifact is stale.

Documented milestone status is 24/24 complete. Verification-adjusted status is 18/24 clean, 6/24 partial, 0 not started, and 0 in progress. The gap is not primarily frontend polish; it is backend API completeness, release hygiene, and contract validation.

Critical blockers:

1. The recent graph/catalog router recovery is present only as uncommitted working tree changes.
2. Dashboard, audit, assumptions, and volumetry router groups are still stubbed despite persisted data existing in PostgreSQL.
3. Backend test failures confirm real regressions in audit event querying and recalculation input construction.

Recommended next action: execute remediation milestones in order, starting with committing or otherwise formalizing the validated graph/catalog recovery, then replacing stub routers with service-backed implementations.

## Repository Profile

- Product: OCI DIS Blueprint, an API-first web application for importing, governing, calculating, and exporting OCI integration catalogs.
- Stack: Next.js/TypeScript web app, FastAPI/Python API, PostgreSQL, Redis/Celery, MinIO, and a pure Python calculation engine.
- Branch root: `/Users/javierchan/.codex/worktrees/b840/oci`
- Codebase path: `OCI DIS Blueprint`
- Main checkout also exists at `/Users/javierchan/Documents/GitHub/oci` on branch `main`.
- Current branch checkout is a linked worktree for `codex/codex-active-work`; it is the only materialized checkout for this branch, so this report was written here per the skill rule.
- First commit date: `2026-01-07`
- Latest commit date: `2026-04-30`
- Commit count: `292`
- Commits in last 7 days: `16`
- Commits in prior 7 days: `3`
- Source files counted for Python/TypeScript/TSX excluding dependencies: `204`

Current git status:

```text
## codex/codex-active-work...origin/codex/codex-active-work
 M apps/api/app/routers/catalog.py
 M apps/web/app/projects/[projectId]/graph/page.tsx
```

Diff stat for the uncommitted runtime recovery:

```text
 OCI DIS Blueprint/apps/api/app/routers/catalog.py  | 207 ++++++++++++++++++---
 .../web/app/projects/[projectId]/graph/page.tsx    |  38 +++-
 2 files changed, 215 insertions(+), 30 deletions(-)
```

Runtime data baseline:

```text
                  id                  |                  name                  | catalog_rows 
--------------------------------------+----------------------------------------+--------------
 20a23466-dd52-4d5a-a3f5-1bc66f659c78 | OCI DIS Blueprint Demo Enterprise 2026 |          480
(1 row)
```

Database table counts:

```text
        table_name         | count 
---------------------------+-------
 catalog_integrations      |   480
 source_integration_rows   |   516
 justification_records     |   480
 audit_events              |   707
 dashboard_snapshots       |     3
 volumetry_snapshots       |     3
 synthetic_generation_jobs |     1
(7 rows)
```

## Milestone Status

The repository source of truth documents M1 through M24 as complete in `README.md` and `AGENTS.md`. Verification found partial implementation or release-readiness gaps in the milestones that depend on catalog routing, recalculation, snapshot APIs, audit APIs, assumptions APIs, and graph release state.

| Product Milestone | Documented Status | Verification Status | Notes |
|---|---:|---:|---|
| M1 - Schema + Migrations | Complete | Complete | Migrations and persisted runtime tables are present. |
| M2 - Import Engine | Complete | Complete | Synthetic/imported rows exist and catalog has 480 integrations. |
| M3 - Catalog Grid API | Complete | Partial | Catalog router recovery exists only as uncommitted worktree changes. |
| M4 - Calculation Engine | Complete | Partial | Recalculation test fails on `IntegrationInput(selected_pattern=...)`. |
| M5 - Dashboard API | Complete | Partial | Dashboard router returns empty snapshots while DB has 3 dashboard snapshots. |
| M6 - Justification Narratives | Complete | Complete | 480 justification records exist in DB. |
| M7 - Exports | Complete | Complete with contract risk | Export functionality was not the failing area, but OpenAPI artifact is stale. |
| M8 - Admin + Governance | Complete | Partial | Audit and assumptions routers are hardcoded stubs. |
| M9 - Integration Capture Wizard | Complete | Complete | No current audit blocker found. |
| M10 - System Dependency Map | Complete | Partial | Graph route renders in the working tree but depends on uncommitted router recovery. |
| M11 - Navigation + Theme | Complete | Complete | No current audit blocker found. |
| M12 - Source Lineage + Template | Complete | Complete | No current audit blocker found. |
| M13 - Integration Design Canvas | Complete | Complete | No current audit blocker found in this audit scope. |
| M14 - Map Pan + Visual Improvements | Complete | Complete | No current audit blocker found in this audit scope. |
| M15 - UX Overhaul P0 | Complete | Complete with release risk | Invalid route and graph fixes depend on current uncommitted changes. |
| M16 - UX Overhaul P1 | Complete | Partial | Graph data route depends on the uncommitted recovery. |
| M17 - UX Overhaul P2 | Complete | Complete | No current audit blocker found. |
| M18 - Workbook Import Fidelity | Complete | Complete | Runtime catalog data indicates workbook-backed import path is populated. |
| M19 - Governed Reference Data 2.0 | Complete | Complete | Patterns and dictionaries APIs return populated data. |
| M20 - Canvas Intelligence | Complete | Complete | No current audit blocker found. |
| M21 - Volumetry Assumption Parity | Complete | Partial | Assumptions router returns empty data and `/default` returns `{}`. |
| M22 - QA Coverage + Confidence Signals | Complete | Complete | No current audit blocker found. |
| M23 - Pattern Coverage 03-17 | Complete | Complete | Pattern endpoint returns 17 patterns. |
| M24 - Admin Synthetic Lab | Complete | Complete with typing debt | Synthetic project exists, but mypy reports synthetic-service typing errors. |

Progress metrics:

- Total formal milestones: 24
- Verification-clean milestones: 18
- Partial milestones: 6
- Not started: 0
- In progress: 0
- Definition-of-done gates checked in this audit: pytest, ruff, mypy, web tsc, web eslint, OpenAPI artifact check, Docker runtime, live API probes.
- Gates passing: ruff, web tsc, web eslint, Docker runtime, live API health, npm production audit.
- Gates failing: backend pytest, backend mypy, OpenAPI artifact check.

## Milestone Index

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [M1](#m1--commit-and-release-catalog-graph-recovery) | Commit And Release Catalog Graph Recovery | Critical | open |
| [M2](#m2--restore-service-backed-snapshot-and-governance-routers) | Restore Service-Backed Snapshot And Governance Routers | Critical | open |
| [M3](#m3--repair-recalculation-input-contract) | Repair Recalculation Input Contract | High | open |
| [M4](#m4--regenerate-and-gate-api-contract) | Regenerate And Gate API Contract | High | open |
| [M5](#m5--close-backend-typing-and-ci-hygiene) | Close Backend Typing And CI Hygiene | Medium | open |
| [M6](#m6--schedule-dependency-maintenance-wave) | Schedule Dependency Maintenance Wave | Medium | deferred |

## Verification Results

### Docker And Live Services

```text
NAME                       IMAGE                    COMMAND                  SERVICE   CREATED        STATUS                  PORTS
ocidisblueprint-api-1      ocidisblueprint-api      "uvicorn app.main:ap..."   api       17 hours ago   Up 17 hours (healthy)   0.0.0.0:8000->8000/tcp, [::]:8000->8000/tcp
ocidisblueprint-db-1       postgres:16-alpine       "docker-entrypoint.s..."   db        2 weeks ago    Up 3 days (healthy)     0.0.0.0:5432->5432/tcp, [::]:5432->5432/tcp
ocidisblueprint-minio-1    minio/minio:latest       "/usr/bin/docker-ent..."   minio     2 weeks ago    Up 3 days (healthy)     0.0.0.0:9000-9001->9000-9001/tcp, [::]:9000-9001->9000-9001/tcp
ocidisblueprint-redis-1    redis:7-alpine           "docker-entrypoint.s..."   redis     2 weeks ago    Up 3 days (healthy)     0.0.0.0:6379->6379/tcp, [::]:6379->6379/tcp
ocidisblueprint-web-1      ocidisblueprint-web      "docker-entrypoint.s..."   web       17 hours ago   Up 17 hours             0.0.0.0:3000->3000/tcp, [::]:3000->3000/tcp
ocidisblueprint-worker-1   ocidisblueprint-worker   "celery -A app.worke..."   worker    17 hours ago   Up 17 hours             8000/tcp
```

```text
{"status":"ok","version":"1.0.0"}API: UP
WEB: 307
```

`WEB: 307` is a redirect response from `/`, not a service-down signal.

### OpenAPI Runtime Shape

```text
Registered endpoints: 51 paths, 66 operations
/api/v1/catalog/{project_id}/graph: present
/api/v1/audit/{project_id}: present
/api/v1/dashboard/{project_id}/snapshots: present
/api/v1/volumetry/{project_id}/snapshots: present
/api/v1/assumptions/: present
```

### Live API Probes

```text
audit: status=200 keys=['events', 'total']
  total=0
  events=list[0]
dashboard_snapshots: status=200 keys=['snapshots']
  snapshots=list[0]
volumetry_snapshots: status=200 keys=['snapshots']
  snapshots=list[0]
assumptions: status=200 keys=['assumption_sets']
  assumption_sets=list[0]
assumptions_default: status=200 keys=[]
catalog_graph: status=200 keys=['edges', 'meta', 'nodes']
  nodes=list[72]
  edges=list[338]
  meta=dict keys=['brands', 'business_processes', 'edge_count', 'integration_count', 'node_count']
```

The graph endpoint is healthy in the current working tree. The audit, dashboard, volumetry, and assumptions endpoints are present but return empty/stub payloads despite persisted records.

### Backend Quality Gates

`cd apps/api && ./.venv/bin/python -m pytest --tb=short -q`:

```text
......................FF..............                                   [100%]
=================================== FAILURES ===================================
_____________________ test_project_patch_emits_audit_event _____________________
app/tests/test_projects_api.py:49: in test_project_patch_emits_audit_event
    assert audit_payload["total"] == 1
E   assert 0 == 1
_______ test_recalculation_includes_low_latency_data_integration_warning _______
app/tests/test_recalc_design_warnings.py:86: in test_recalculation_includes_low_latency_data_integration_warning
    snapshot = await recalc_service.recalculate_project(
app/services/recalc_service.py:209: in recalculate_project
    row_inputs.append(_integration_input_with_overrides(row, executions_day))
app/services/recalc_service.py:108: in _integration_input_with_overrides
    base = _integration_input(row)
app/services/recalc_service.py:55: in _integration_input
    return IntegrationInput(
E   TypeError: IntegrationInput.__init__() got an unexpected keyword argument 'selected_pattern'
=========================== short test summary info ============================
FAILED app/tests/test_projects_api.py::test_project_patch_emits_audit_event
FAILED app/tests/test_recalc_design_warnings.py::test_recalculation_includes_low_latency_data_integration_warning
2 failed, 36 passed in 2.17s
```

`cd apps/api && ./.venv/bin/python -m ruff check .`:

```text
All checks passed!
```

`cd apps/api && ./.venv/bin/python -m mypy app --ignore-missing-imports --no-error-summary`:

```text
app/services/synthetic_service.py:1628: note:     def int(str | bytes | bytearray, /, base: SupportsIndex) -> int
app/services/synthetic_service.py:1629: error: No overload variant of "int" matches argument type "object"  [call-overload]
app/services/synthetic_service.py:1629: note: Possible overload variants:
app/services/synthetic_service.py:1629: note:     def int(str | Buffer | SupportsInt | SupportsIndex | SupportsTrunc = ..., /) -> int
app/services/synthetic_service.py:1629: note:     def int(str | bytes | bytearray, /, base: SupportsIndex) -> int
app/services/synthetic_service.py:1716: error: Argument "old_value" to "emit" has incompatible type "dict[str, str]"; expected "dict[str, object] | None"  [arg-type]
app/services/synthetic_service.py:1716: note: "dict" is invariant -- see https://mypy.readthedocs.io/en/stable/common_issues.html#variance
app/services/synthetic_service.py:1716: note: Consider using "Mapping" instead, which is covariant in the value type
app/services/synthetic_service.py:1743: error: Argument "old_value" to "emit" has incompatible type "dict[str, str | dict[Any, Any] | None]"; expected "dict[str, object] | None"  [arg-type]
app/services/synthetic_service.py:1743: note: "dict" is invariant -- see https://mypy.readthedocs.io/en/stable/common_issues.html#variance
app/services/synthetic_service.py:1743: note: Consider using "Mapping" instead, which is covariant in the value type
app/services/synthetic_service.py:1788: error: No overload variant of "int" matches argument type "object"  [call-overload]
app/services/synthetic_service.py:1788: note: Possible overload variants:
app/services/synthetic_service.py:1788: note:     def int(str | Buffer | SupportsInt | SupportsIndex | SupportsTrunc = ..., /) -> int
app/services/synthetic_service.py:1788: note:     def int(str | bytes | bytearray, /, base: SupportsIndex) -> int
app/services/synthetic_service.py:1821: error: Argument "old_value" to "emit" has incompatible type "dict[str, str]"; expected "dict[str, object] | None"  [arg-type]
app/services/synthetic_service.py:1821: note: "dict" is invariant -- see https://mypy.readthedocs.io/en/stable/common_issues.html#variance
app/services/synthetic_service.py:1821: note: Consider using "Mapping" instead, which is covariant in the value type
app/services/catalog_service.py:355: error: Argument "allowed_core_tools" to "normalize_canvas_design" has incompatible type "set[str]"; expected "Sequence[str]"  [arg-type]
app/services/catalog_service.py:356: error: Argument "allowed_overlay_tools" to "normalize_canvas_design" has incompatible type "set[str]"; expected "Sequence[str]"  [arg-type]
```

### Frontend Quality Gates

`cd apps/web && npx tsc --noEmit --skipLibCheck` passed with no output.

`cd apps/web && npx eslint . --ext .ts,.tsx --max-warnings 0` passed with no output.

`cd apps/web && npm audit --omit=dev --json`:

```text
{'info': 0, 'low': 0, 'moderate': 0, 'high': 0, 'critical': 0, 'total': 0}
```

### API Artifact Check

`cd apps/api && ./.venv/bin/python scripts/export_openapi.py --check`:

```text
OpenAPI artifact is out of date: /Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint/docs/api/openapi.yaml
```

## Pending Tasks

### CRITICAL

- `apps/api/app/routers/catalog.py` and `apps/web/app/projects/[projectId]/graph/page.tsx` - validated graph/catalog recovery is uncommitted and therefore not part of branch HEAD release state.
- `apps/api/app/routers/dashboard.py`, `apps/api/app/routers/audit.py`, `apps/api/app/routers/assumptions.py`, `apps/api/app/routers/volumetry.py` - endpoints return hardcoded empty payloads while DB contains dashboard snapshots, volumetry snapshots, audit events, and governed assumption data.

### INCOMPLETE

- `apps/api/app/services/recalc_service.py` - recalculation passes `selected_pattern` to `IntegrationInput`, but the calc-engine constructor no longer accepts that keyword.
- `docs/api/openapi.yaml` - generated OpenAPI artifact is out of date with the running application.
- `apps/api/app/services/synthetic_service.py` and `apps/api/app/services/catalog_service.py` - backend mypy gate fails on object-to-int narrowing, audit payload dict variance, and `set[str]` vs `Sequence[str]`.
- `.github/workflows/` - no active CI workflow files were found, so quality gates are not automatically enforced in the repository.

### DEFERRED

- Dependency maintenance - Python and frontend dependency drift is significant but not an immediate runtime blocker.
- `docs/.DS_Store` - local ignored artifact exists under docs; it is not tracked, but it should be removed from the working directory during hygiene cleanup.

## Finding Remediation Plans

### Finding: Uncommitted catalog and graph runtime recovery

- Classification: `CRITICAL`
- Impact: Release integrity and demo stability.
- Area: M3 Catalog Grid API, M10 System Dependency Map, M16 Surface Completeness.
- Evidence:
  - `git status --short` - `apps/api/app/routers/catalog.py` and `apps/web/app/projects/[projectId]/graph/page.tsx` are modified.
  - `git diff --stat` - 215 insertions and 30 deletions across the recovery files.
  - Live API probe - `/api/v1/catalog/{project_id}/graph` returns 72 nodes and 338 edges in the working tree.
- Issue: The working tree contains the fix for the recent graph crash, but branch HEAD is still not the validated source state.
- Remediation Milestones:
  - `M1` - Commit And Release Catalog Graph Recovery (`Critical`, `open`): formalize the validated recovery in source control and rerun the route validation.
  - `M4` - Regenerate And Gate API Contract (`High`, `open`): ensure any route shape change is reflected in `docs/api/openapi.yaml`.

### Finding: Service-backed API endpoints are still stubbed

- Classification: `CRITICAL`
- Impact: Product API completeness, dashboard correctness, audit trail visibility, governed assumptions, and volumetry retrieval.
- Area: M5 Dashboard API, M8 Admin + Governance, M21 Volumetry Assumption Parity.
- Evidence:
  - `apps/api/app/routers/dashboard.py` - `list_dashboard_snapshots` returns `{"snapshots": []}`.
  - `apps/api/app/routers/audit.py` - `list_audit_events` returns `{"events": [], "total": 0}`.
  - `apps/api/app/routers/assumptions.py` - list/default/version endpoints return empty payloads.
  - `apps/api/app/routers/volumetry.py` - snapshot endpoints return empty payloads.
  - DB probe - `audit_events=707`, `dashboard_snapshots=3`, `volumetry_snapshots=3`.
  - Live API probe - corresponding endpoints return empty lists or `{}`.
- Issue: The API surface advertises completed capabilities but several routers bypass service/data layers and return placeholder payloads.
- Remediation Milestones:
  - `M2` - Restore Service-Backed Snapshot And Governance Routers (`Critical`, `open`): replace stub responses with real service-backed implementations.
  - `M4` - Regenerate And Gate API Contract (`High`, `open`): refresh OpenAPI after restoring router shapes.
  - `M5` - Close Backend Typing And CI Hygiene (`Medium`, `open`): add regression tests and CI coverage so stubs cannot silently return.

### Finding: Recalculation input contract regression

- Classification: `INCOMPLETE`
- Impact: Calculation correctness and regression test stability.
- Area: M4 Calculation Engine, M20 Canvas Intelligence, M21 Volumetry Assumption Parity.
- Evidence:
  - `apps/api/app/services/recalc_service.py:55` - failing path constructs `IntegrationInput`.
  - `app/tests/test_recalc_design_warnings.py::test_recalculation_includes_low_latency_data_integration_warning` - fails with `TypeError: IntegrationInput.__init__() got an unexpected keyword argument 'selected_pattern'`.
- Issue: API recalculation service and calc-engine input model are out of sync.
- Remediation Milestones:
  - `M3` - Repair Recalculation Input Contract (`High`, `open`): align service payload construction with the calc-engine model and preserve warning semantics.
  - `M5` - Close Backend Typing And CI Hygiene (`Medium`, `open`): include focused test coverage for the corrected contract.

### Finding: OpenAPI artifact is stale

- Classification: `INCOMPLETE`
- Impact: API contract reliability and downstream client generation.
- Area: API contract and release readiness.
- Evidence:
  - `apps/api/scripts/export_openapi.py --check` - reports `docs/api/openapi.yaml` is out of date.
  - Runtime OpenAPI reports 51 paths and 66 operations.
- Issue: The committed OpenAPI document does not match the current application route contract.
- Remediation Milestones:
  - `M4` - Regenerate And Gate API Contract (`High`, `open`): regenerate and verify `docs/api/openapi.yaml`.
  - `M5` - Close Backend Typing And CI Hygiene (`Medium`, `open`): enforce the check in CI.

### Finding: Backend type checking fails

- Classification: `INCOMPLETE`
- Impact: Maintainability and strict typing compliance.
- Area: M24 Synthetic Lab, catalog canvas normalization, backend coding rules.
- Evidence:
  - `apps/api/app/services/synthetic_service.py` - mypy reports `int(object)` and audit payload variance errors.
  - `apps/api/app/services/catalog_service.py:355-356` - passes `set[str]` where `Sequence[str]` is expected.
- Issue: Backend code violates the repository's typing discipline and leaves type-sensitive paths unchecked.
- Remediation Milestones:
  - `M5` - Close Backend Typing And CI Hygiene (`Medium`, `open`): narrow types, adjust audit payload typing, and align sequence inputs.

### Finding: No active CI workflow found

- Classification: `INCOMPLETE`
- Impact: Regression prevention and production readiness.
- Area: DevOps and quality gates.
- Evidence:
  - `find .github -maxdepth 3 -type f` - returned no workflow files.
  - Current local gates already show failures that CI would catch.
- Issue: Quality gates depend on manual execution and can drift from documented completion.
- Remediation Milestones:
  - `M5` - Close Backend Typing And CI Hygiene (`Medium`, `open`): add or restore CI workflow coverage for backend tests, ruff, mypy, frontend tsc, frontend eslint, and OpenAPI check.

### Finding: Dependency drift needs a controlled maintenance wave

- Classification: `DEFERRED`
- Impact: Security, framework supportability, and future migration cost.
- Area: Dependency management.
- Evidence:
  - Python outdated packages include `fastapi 0.111.0 -> 0.136.1`, `celery 5.4.0 -> 5.6.3`, `httpx 0.27.0 -> 0.28.1`, and `openpyxl 3.1.2 -> 3.1.5`.
  - Frontend outdated packages include `next 15.5.15 -> 16.2.4`, `react 18.3.1 -> 19.2.5`, `eslint 8.57.1 -> 10.2.1`, `tailwindcss 3.4.19 -> 4.2.4`, and `zod 3.25.76 -> 4.4.1`.
  - `npm audit --omit=dev` reports zero vulnerabilities.
- Issue: Dependency drift is broad enough to require a planned migration rather than opportunistic updates.
- Remediation Milestones:
  - `M6` - Schedule Dependency Maintenance Wave (`Medium`, `deferred`): define upgrade batches and re-entry criteria after critical runtime gaps are closed.

### Finding: Local docs artifact exists

- Classification: `DEFERRED`
- Impact: Low hygiene risk.
- Area: Repository cleanliness.
- Evidence:
  - `test -e docs/.DS_Store` - file exists.
  - `git ls-files docs/.DS_Store` - no tracked output; `.gitignore` already includes `.DS_Store`.
- Issue: An ignored local artifact exists under docs but is not part of source control.
- Remediation Milestones:
  - `M5` - Close Backend Typing And CI Hygiene (`Medium`, `open`): remove local ignored artifacts during closeout and optionally add a repository hygiene check.

## M1 — Commit And Release Catalog Graph Recovery

**Tier:** Critical
**Status:** open

### Problem

The working tree contains the validated fix for catalog and graph routing, but branch HEAD does not. This creates a false sense of stability: the running app can be correct while the committed branch remains unsafe to deploy or merge.

### Root Cause

The graph crash fix restored service-backed catalog router behavior and frontend normalization, but those changes remain local modifications.

### Target State

- The graph/catalog recovery is committed or otherwise formally accepted into the branch.
- `/api/v1/catalog/{project_id}` and `/api/v1/catalog/{project_id}/graph` both return real data from the committed source state.
- The graph page renders without runtime overlay from the committed branch.

### Files Most Likely To Change

- `apps/api/app/routers/catalog.py` - service-backed catalog and graph route implementation.
- `apps/web/app/projects/[projectId]/graph/page.tsx` - defensive graph response normalization.

### Action Plan

1. Review the uncommitted diff for accidental scope creep and confirm it only covers catalog/router and graph-page normalization.
2. Rerun focused validation: catalog list API, graph API, web tsc, targeted eslint, ruff for `catalog.py`, and graph page browser smoke.
3. Commit or otherwise preserve the validated recovery according to the repository's release workflow.
4. Re-check `git status --short` and confirm no unrelated source changes remain.

### Exit Criteria

- `git status --short` has no uncommitted source changes for the graph/catalog fix.
- `/api/v1/catalog/{project_id}/graph` returns non-empty `nodes` and `edges`.
- Graph page renders without runtime overlay for the demo project.
- Focused backend/frontend gates pass for the touched files.

## M2 — Restore Service-Backed Snapshot And Governance Routers

**Tier:** Critical
**Status:** open

### Problem

Several public API groups are present in OpenAPI but return hardcoded empty payloads. This breaks dashboard snapshot retrieval, volumetry snapshot retrieval, audit event visibility, and governed assumption access.

### Root Cause

Router modules for dashboard, audit, assumptions, and volumetry still contain placeholder implementations instead of delegating to service/repository code.

### Target State

- Dashboard snapshots API returns persisted dashboard snapshots for the project.
- Volumetry snapshots API returns persisted snapshot metadata, row results, and consolidated metrics.
- Audit API returns the 707 persisted audit events with filters and pagination.
- Assumptions API returns governed assumption sets and a valid default payload.

### Files Most Likely To Change

- `apps/api/app/routers/dashboard.py` - replace stubs with service calls.
- `apps/api/app/routers/audit.py` - query audit events through service/repository layer.
- `apps/api/app/routers/assumptions.py` - fix route order and return governed assumption data.
- `apps/api/app/routers/volumetry.py` - return snapshot list/detail/rows/consolidated data.
- `apps/api/app/services/*` - add or reuse service functions as needed.
- `apps/api/app/tests/*` - add regression tests for non-empty runtime data and empty-state behavior.

### Action Plan

1. Inspect existing models and services for dashboard, volumetry, audit, and assumptions before adding new logic.
2. Replace router stubs with thin router functions that delegate to service-layer queries.
3. Preserve pagination, filtering, and response-shape compatibility with OpenAPI and frontend consumers.
4. Add regression tests that prove existing persisted data is surfaced and empty projects still return safe empty responses.
5. Rerun backend pytest and live API probes against the demo project.

### Exit Criteria

- `GET /api/v1/audit/{project_id}` returns total greater than zero for the demo project.
- `GET /api/v1/dashboard/{project_id}/snapshots` returns the 3 persisted dashboard snapshots.
- `GET /api/v1/volumetry/{project_id}/snapshots` returns the 3 persisted volumetry snapshots.
- `GET /api/v1/assumptions/` and `/default` return governed assumption data.
- Backend pytest passes with no failing tests.

## M3 — Repair Recalculation Input Contract

**Tier:** High
**Status:** open

### Problem

The recalculation service fails when constructing calc-engine `IntegrationInput` objects because it passes an unsupported `selected_pattern` keyword.

### Root Cause

The API service and calc-engine input model drifted during canvas/pattern intelligence work. Pattern semantics likely moved to a different field or data structure, but recalc_service still uses the older constructor shape.

### Target State

- Recalculation input construction matches the calc-engine public model.
- Low-latency Data Integration warning behavior remains covered.
- Recalculation tests pass and runtime recalculation can complete for the demo project.

### Files Most Likely To Change

- `apps/api/app/services/recalc_service.py` - align constructor usage and warning inputs.
- `packages/calc-engine/src/engine/*` - inspect only; change only if the contract should intentionally expand.
- `apps/api/app/tests/test_recalc_design_warnings.py` - preserve or update expected warning semantics after code correction.

### Action Plan

1. Inspect `IntegrationInput` definition and current recalc service mapping.
2. Remove or remap `selected_pattern` to the supported field without losing pattern-aware warnings.
3. Add or adjust focused tests for the low-latency Data Integration warning path.
4. Rerun backend pytest and a live recalculation smoke if safe for the demo data.

### Exit Criteria

- `app/tests/test_recalc_design_warnings.py::test_recalculation_includes_low_latency_data_integration_warning` passes.
- Full backend pytest passes after router remediation.
- Recalculation no longer raises `TypeError` from `IntegrationInput`.

## M4 — Regenerate And Gate API Contract

**Tier:** High
**Status:** open

### Problem

The committed OpenAPI artifact is out of date with the runtime application. This undermines generated clients, API documentation, and contract review.

### Root Cause

Route changes were made without regenerating `docs/api/openapi.yaml`, and no CI gate currently enforces the check.

### Target State

- `docs/api/openapi.yaml` exactly matches the app's exported OpenAPI schema.
- The OpenAPI check is part of release validation.
- Router fixes in M1-M3 are reflected in the contract.

### Files Most Likely To Change

- `docs/api/openapi.yaml` - regenerated artifact.
- `apps/api/scripts/export_openapi.py` - inspect only unless the check script is incorrect.
- `.github/workflows/*` - CI gate location if workflows are restored.

### Action Plan

1. Complete route and recalculation fixes before regenerating the artifact.
2. Run `cd apps/api && ./.venv/bin/python scripts/export_openapi.py` to refresh `docs/api/openapi.yaml`.
3. Run `cd apps/api && ./.venv/bin/python scripts/export_openapi.py --check` and confirm it passes.
4. Include the OpenAPI check in CI or documented release gates.

### Exit Criteria

- `apps/api/scripts/export_openapi.py --check` exits successfully.
- `docs/api/openapi.yaml` is updated only by deterministic export output.
- OpenAPI runtime endpoint count and artifact are aligned.

## M5 — Close Backend Typing And CI Hygiene

**Tier:** Medium
**Status:** open

### Problem

Backend type checking fails and the repository has no active CI workflow files. This allows regressions to accumulate until manual audits discover them.

### Root Cause

Recent synthetic and catalog-service additions introduced type ambiguity, and quality gates are documented but not enforced automatically.

### Target State

- Backend mypy passes.
- Quality gates are enforceable in CI or an equivalent repository workflow.
- Ignored local artifacts are cleaned up.

### Files Most Likely To Change

- `apps/api/app/services/synthetic_service.py` - object narrowing and audit payload typing.
- `apps/api/app/services/catalog_service.py` - sequence/set typing alignment.
- `.github/workflows/*` - restore or add CI workflow definitions.
- `docs/reports/*` - optional progress or closeout documentation after validation.

### Action Plan

1. Narrow object-typed values before `int(...)` conversion in synthetic service.
2. Adjust audit emit payload typing to `dict[str, object]` or a compatible mapping shape.
3. Convert sets to stable sequences when calling `normalize_canvas_design`.
4. Add or restore CI workflow coverage for pytest, ruff, mypy, web tsc, web eslint, and OpenAPI check.
5. Remove ignored local artifacts such as `docs/.DS_Store` during cleanup.

### Exit Criteria

- `cd apps/api && ./.venv/bin/python -m mypy app --ignore-missing-imports --no-error-summary` passes.
- Full local quality gate set passes.
- CI workflow files exist or an explicitly documented alternative gate is present.
- `docs/.DS_Store` no longer exists in the working directory.

## M6 — Schedule Dependency Maintenance Wave

**Tier:** Medium
**Status:** deferred

### Problem

The dependency set is materially behind current releases across both backend and frontend. There are no production npm vulnerabilities today, so this is not the first blocker, but the drift is broad enough to require a planned wave.

### Root Cause

Feature and remediation work has advanced faster than dependency maintenance. Major frontend upgrades are available and may require migration effort.

### Target State

- Dependencies are grouped into safe upgrade batches.
- Framework-major upgrades are handled deliberately after runtime blockers are closed.
- Security and compatibility gates pass after each batch.

### Files Most Likely To Change

- `apps/api/requirements.txt` - backend dependency pins.
- `apps/web/package.json` and lockfile - frontend dependency pins.
- `docs/reports/dependency-maintenance-plan-*.md` - upgrade sequencing.

### Action Plan

1. Defer until M1-M5 are complete and quality gates are green.
2. Split backend patch/minor upgrades from frontend major framework upgrades.
3. Upgrade one batch at a time and run full backend/frontend gates after each batch.
4. Re-enter the wave immediately if a security advisory appears or if platform support requires it.

### Exit Criteria

- A dependency maintenance plan exists with upgrade batches and rollback notes.
- Each completed batch passes tests, lint, type checks, and runtime smoke.
- No production vulnerability remains unresolved.

## Debt Markers

The grep scan for `TODO`, `FIXME`, `HACK`, `PLACEHOLDER`, `XXX`, and `NOCOMMIT` in non-test Python/TypeScript/TSX files returned no matches.

Hygiene note: `docs/.DS_Store` exists locally but is ignored and not tracked. `.gitignore` contains `.DS_Store`.

## Recommended Next Actions

1. Execute M1 immediately so the validated catalog/graph recovery becomes branch state rather than a local-only fix.
2. Execute M2 next because stubbed routers are now the largest product correctness gap.
3. Execute M3 before declaring calculation readiness.
4. Execute M4 after route fixes so OpenAPI reflects final API behavior.
5. Execute M5 to turn manual validation into enforced validation.
6. Defer M6 until runtime and quality gates are clean.

## Dependency Drift

Backend examples from `pip list --outdated`:

```text
Package            Version   Latest    Type
------------------ --------- --------- -----
aiosqlite          0.20.0    0.22.1    wheel
alembic            1.13.1    1.18.4    wheel
asyncpg            0.29.0    0.31.0    wheel
boto3              1.34.101  1.43.0    wheel
botocore           1.34.162  1.43.0    wheel
celery             5.4.0     5.6.3     wheel
fastapi            0.111.0   0.136.1   wheel
httpx              0.27.0    0.28.1    wheel
openpyxl           3.1.2     3.1.5     wheel
```

Frontend examples from `npm outdated --depth=0`:

```text
Package                 Current    Wanted   Latest
@tanstack/react-query    5.99.0   5.100.6  5.100.6
next                    15.5.15   15.5.15   16.2.4
react                    18.3.1    18.3.1   19.2.5
react-dom                18.3.1    18.3.1   19.2.5
eslint                   8.57.1    8.57.1   10.2.1
tailwindcss              3.4.19    3.4.19    4.2.4
typescript                5.9.3     5.9.3    6.0.3
zod                     3.25.76   3.25.76    4.4.1
```

Current production npm audit is clean:

```text
info=0, low=0, moderate=0, high=0, critical=0, total=0
```
