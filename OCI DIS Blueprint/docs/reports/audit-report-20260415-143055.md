# Project Audit Report

**Generated:** 2026-04-15 14:30:55 CST
**Repository:** `oci`
**Codebase:** `OCI DIS Blueprint`
**Branch:** `codex/codex-active-work`
**Commit:** `5c481ba docs: record post-audit remediation progress`
**Auditor:** Codex (automated)

---

## Executive Summary

This audit covers the `OCI DIS Blueprint` codebase inside the shared `oci` repository, not the sibling `pricing/` project.

Execution and write-path resolution for this run:

- Execution context: `/Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint`
- Branch root: `/Users/javierchan/.codex/worktrees/b840/oci`
- Branch-backed codebase path used for output: `/Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint`

Reason: `git worktree list --porcelain` shows `codex/codex-active-work` materialized in exactly one linked worktree, so this worktree is the safe branch-backed destination for the report.

Current audit conclusion:

- Total milestones assessed: 15
- Complete: 15
- Partial: 0
- In progress: 0
- Not started: 0
- Critical blockers: 0

Quality and release posture in this branch are now clean:

- Backend API integration tests are committed and passing.
- Calc-engine parity tests are passing.
- Ruff, mypy, TypeScript, and OpenAPI sync checks are green.
- The local Docker stack is healthy (`6/6` containers up).
- The missing project patch audit path, committed OpenAPI artifact, and CI validation workflow flagged in the prior audit are now present in source control.

The only remaining notes are operational rather than code blockers:

- The current local dev database contains multiple smoke and parity projects, so the "first project" is no longer a reliable benchmark target.
- API integration tests emit upstream `openpyxl` deprecation warnings, but the endpoint behavior remains correct and the warnings originate from library internals.

---

## Repository Profile

Selected codebase root: `OCI DIS Blueprint/`

Project profile:

- Backend: FastAPI in `apps/api/`
- Frontend: Next.js 14 / TypeScript in `apps/web/`
- Calc engine: pure Python in `packages/calc-engine/`
- Docker stack: `docker-compose.yml`
- Migrations: `apps/api/migrations/` and `apps/api/alembic.ini`
- Backend API integration tests: `apps/api/app/tests/`
- Calc-engine parity tests: `packages/calc-engine/src/tests/`
- CI validation workflow: `.github/workflows/api-validation.yml`
- Source-controlled OpenAPI artifact: `docs/api/openapi.yaml`

Repo-shape detection:

```text
Python:
apps/api/requirements.txt
Node:
apps/web/package.json
package.json
Docker:
docker-compose.yml
Migrations:
apps/api/alembic.ini
apps/api/migrations
Tests:
packages/calc-engine/src/tests
CI:
.github/workflows/api-validation.yml
```

Git context:

```text
git rev-parse --show-toplevel
/Users/javierchan/.codex/worktrees/b840/oci

git branch --show-current
codex/codex-active-work

git worktree list --porcelain
worktree /Users/javierchan/Documents/GitHub/oci
HEAD 155b2036e0dd687521645b16595303fc84f411ce
branch refs/heads/vscode/codex-active-work

worktree /Users/javierchan/.codex/worktrees/7ea6/oci
HEAD 6990ea595854c3cdd8faf67948cef375a07f7a23
detached

worktree /Users/javierchan/.codex/worktrees/b840/oci
HEAD 5c481bad0d9db3b2ce2037f6d9b9b79c6df218a4
branch refs/heads/codex/codex-active-work
```

Interpretation:

- The audited branch is correctly materialized in this Codex worktree.
- No branch switching was required for the audit.
- The branch was clean at the start of the audit run; after report generation, this report file becomes the only new uncommitted artifact.

Debt markers:

```text
No TODO/FIXME/XXX markers found in apps/api, apps/web, packages, or docs (excluding prior reports and lockfiles).
```

---

## Milestone Status

### M1 — Schema + Migrations
**Status:** ✅ Complete

Evidence:

- Alembic configuration and migration history exist in `apps/api/alembic.ini` and `apps/api/migrations/versions/`.
- Governance seed artifacts and reference models are present in the backend codebase.
- This area remains documented as complete in `README.md` and `docs/progress.md`.

### M2 — Import Engine
**Status:** ✅ Complete

Evidence:

- Import router, service, worker, and schemas are present in `apps/api/app/routers/imports.py`, `apps/api/app/services/import_service.py`, `apps/api/app/workers/import_worker.py`, and related schemas.
- Calc-engine parity suite passes: `26 passed`.
- Multiple live projects in the current dev DB still show benchmark-scale imported catalogs (`144` and `148` rows), confirming the import path remains functional.

### M3 — Catalog Grid API
**Status:** ✅ Complete

Evidence:

- Catalog list, detail, bulk patch, lineage, estimate, duplicate detection, and system autocomplete surfaces are present in the backend and wired in the web app.
- Live probe against the largest current project returned `selected_catalog_total=148`, `selected_catalog_page_size=50`, and `selected_lineage_column_names=True`.

### M4 — Calculation Engine
**Status:** ✅ Complete

Evidence:

- Recalculation service, schemas, and worker flow are present and were remediated earlier in this branch.
- mypy is clean across `apps/api/app`, including recalculation service typing fixes.
- Calc-engine parity suite remains green.

### M5 — Dashboard API
**Status:** ✅ Complete

Evidence:

- Dashboard routes and service remain present in the API.
- Source-controlled OpenAPI includes the dashboard contract.
- No code-level gaps were confirmed in this milestone during this rerun.

Note:

- The current local DB does not have persisted dashboard snapshots for the inspected smoke projects, so this rerun relied on passing tests and contract presence rather than a fresh live snapshot probe.

### M6 — Justification Narratives
**Status:** ✅ Complete

Evidence:

- Justification router, service, and schemas are present.
- Live probe on a representative imported integration returned `selected_justification_state=draft`.

### M7 — Exports
**Status:** ✅ Complete

Evidence:

- Export router and service remain present.
- Template export endpoint responds successfully and generated a valid workbook artifact during this audit run (`/tmp/oci_capture_template.xlsx`, `6.2K`).
- OpenAPI artifact includes the export contract.

### M8 — Admin + Governance
**Status:** ✅ Complete

Evidence:

- Governance models, schemas, services, and routers are committed.
- Milestone status is recorded as complete in `README.md` and `docs/progress.md`.

### M9 — Integration Capture Wizard
**Status:** ✅ Complete

Evidence:

- Capture flow pages and components remain present in the web app.
- Supporting catalog endpoints for create, duplicate check, estimate, and systems autocomplete are present in the API.

### M10 — System Dependency Map
**Status:** ✅ Complete

Evidence:

- Graph service and graph schemas are present.
- OpenAPI publishes `/api/v1/catalog/{project_id}/graph`.
- Live graph probe on the representative project returned `graph_nodes=8` and `graph_edges=4`.

### M11 — Navigation + Theme
**Status:** ✅ Complete

Evidence:

- Breadcrumb, theme toggle, theme hook, and global tokenized styling are present in the web app.
- TypeScript remains clean after the post-audit remediation commits.

### M12 — Source Lineage + Template
**Status:** ✅ Complete

Evidence:

- Lineage schema/service support `column_names`.
- Live lineage probe confirms named lineage data is returned.
- Template export endpoint remains healthy.

### M13 — Integration Design Canvas
**Status:** ✅ Complete

Evidence:

- `apps/web/components/integration-canvas.tsx` remains present and wired into the detail experience.
- No regression signals were found in current frontend checks.

### M14 — Map Pan + Visual Improvements
**Status:** ✅ Complete

Evidence:

- `apps/web/components/graph-controls.tsx` and `apps/web/components/integration-graph.tsx` remain present.
- The graph API contract and rendering stack are both committed and type-checked.

### Browser QA — Bug Fixes + UX Enhancements
**Status:** ✅ Complete

Evidence:

- The Browser QA remediation remains documented in `docs/progress.md`.
- The affected components and pages are present in source control.
- Frontend quality gates remain green.

---

## Milestone Index

This rerun did not confirm any open remediation milestones. The prior audit findings are now closed in this branch.

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [A1](#a1--restore-audited-project-patch-path) | Restore audited project patch path | High | closed |
| [A2](#a2--add-committed-backend-api-validation) | Add committed backend API validation | High | closed |
| [A3](#a3--restore-openapi-traceability) | Restore OpenAPI traceability | High | closed |
| [A4](#a4--rerun-clean-head-audit) | Rerun clean-head release audit | Medium | closed |

### A1 — Restore Audited Project Patch Path
**Status:** closed

Closed by commit `f141ad3 feat(api): add audited project patch, integration tests, and OpenAPI sync`.

### A2 — Add Committed Backend API Validation
**Status:** closed

Closed by the committed backend integration harness in `apps/api/app/tests/` and `.github/workflows/api-validation.yml`.

### A3 — Restore OpenAPI Traceability
**Status:** closed

Closed by the committed exporter script in `apps/api/scripts/export_openapi.py` and the restored artifact in `docs/api/openapi.yaml`.

### A4 — Rerun Clean-Head Release Audit
**Status:** closed

Closed by this report, generated after re-verifying the branch state and quality gates.

---

## Verification Results

### Backend

```text
./.venv/bin/python -m pytest apps/api/app/tests -q
...                                                                      [100%]
3 passed, 4 warnings in 0.15s
```

```text
./.venv/bin/python -m pytest packages/calc-engine/src/tests -q
..........................                                               [100%]
26 passed in 0.03s
```

```text
./.venv/bin/python -m ruff check apps/api/app apps/api/app/tests apps/api/scripts
All checks passed!
```

```text
./.venv/bin/python -m mypy apps/api/app --ignore-missing-imports --no-error-summary
[no output]
```

### Frontend

```text
cd apps/web && npx tsc --noEmit --skipLibCheck
[no output]
```

### Contract / Traceability

```text
./.venv/bin/python apps/api/scripts/export_openapi.py --check
OpenAPI artifact is up to date: /Users/javierchan/.codex/worktrees/b840/oci/OCI DIS Blueprint/docs/api/openapi.yaml
```

```text
git ls-files docs/api/openapi.yaml .github/workflows/api-validation.yml apps/api/app/tests/test_projects_api.py apps/api/app/tests/test_catalog_api.py apps/api/app/tests/test_exports_api.py
.github/workflows/api-validation.yml
apps/api/app/tests/test_catalog_api.py
apps/api/app/tests/test_exports_api.py
apps/api/app/tests/test_projects_api.py
docs/api/openapi.yaml
```

### Docker / Live Stack

```text
docker compose ps
NAME                       IMAGE                    COMMAND                  SERVICE   CREATED        STATUS                  PORTS
ocidisblueprint-api-1      ocidisblueprint-api      "uvicorn app.main:ap…"   api       25 hours ago   Up 25 hours (healthy)   0.0.0.0:8000->8000/tcp, [::]:8000->8000/tcp
ocidisblueprint-db-1       postgres:16-alpine       "docker-entrypoint.s…"   db        39 hours ago   Up 39 hours (healthy)   0.0.0.0:5432->5432/tcp, [::]:5432->5432/tcp
ocidisblueprint-minio-1    minio/minio:latest       "/usr/bin/docker-ent…"   minio     39 hours ago   Up 39 hours (healthy)   0.0.0.0:9000-9001->9000-9001/tcp, [::]:9000-9001->9000-9001/tcp
ocidisblueprint-redis-1    redis:7-alpine           "docker-entrypoint.s…"   redis     39 hours ago   Up 39 hours (healthy)   0.0.0.0:6379->6379/tcp, [::]:6379->6379/tcp
ocidisblueprint-web-1      ocidisblueprint-web      "docker-entrypoint.s…"   web       25 hours ago   Up 25 hours             0.0.0.0:3000->3000/tcp, [::]:3000->3000/tcp
ocidisblueprint-worker-1   ocidisblueprint-worker   "celery -A app.worke…"   worker    39 hours ago   Up 37 minutes           8000/tcp
```

```text
curl -sf http://localhost:8000/health
{"status":"ok","version":"1.0.0"}
```

### Representative Runtime Probes

```text
project_count=10
selected_project_id=31bb8146-0e66-4bc3-958a-d8d643617379
selected_project_name=UI Smoke 1776177714678
selected_catalog_total=148
selected_catalog_page_size=50
selected_lineage_column_names=True
selected_justification_state=draft
graph_nodes=8
graph_edges=4
```

```text
curl -s http://localhost:8000/api/v1/exports/template/xlsx -o /tmp/oci_capture_template.xlsx && ls -lh /tmp/oci_capture_template.xlsx
-rw-r--r--@ 1 javierchan  wheel   6.2K Apr 15 14:34 /tmp/oci_capture_template.xlsx
```

---

## Pending Tasks

### CRITICAL

None confirmed.

### INCOMPLETE

None confirmed.

### DEFERRED

- Optional: replace or suppress upstream `openpyxl` deprecation warnings once the dependency path is intentionally updated.
- Optional: seed or recalculate a benchmark project in the local dev DB if you want live dashboard snapshot probes to be part of future audits, rather than relying on committed tests and contract checks.

---

## Remediation Plans For Confirmed Findings

No open code defects were confirmed in this rerun.

Closed since the prior audit:

1. Stubbed project patch route replaced with typed service-backed update flow and audit emission.
2. Backend API integration test harness committed under `apps/api/app/tests/`.
3. CI validation workflow committed under `.github/workflows/api-validation.yml`.
4. Source-controlled OpenAPI artifact restored and checked in sync.
5. Clean-head audit rerun completed from the branch-backed Codex worktree.

---

## Dependency Drift

- No required package upgrades were identified for release readiness in this rerun.
- The only observed dependency-related signal is the `openpyxl` deprecation warning emitted during API export-template tests.

---

## Recommended Next Actions

1. Commit this audit report if you want the refreshed clean-head assessment preserved in git history.
2. Push `codex/codex-active-work` when you want the validated remediation commits and this audit artifact available remotely.
3. If you want stronger live-environment evidence in future audits, keep one benchmark project with recalculated snapshots in the local dev DB so dashboard probes can be included alongside import, catalog, lineage, graph, and export checks.
