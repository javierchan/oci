# Project Audit Report

**Generated:** 2026-08-14 02:32 America/Mexico_City  
**Repository:** https://github.com/javierchan/oci.git  
**Selected codebase:** `OCI DIS Blueprint`  
**Execution and write location:** `/Users/javierchan/Documents/GitHub/oci/OCI DIS Blueprint`  
**Branch:** `main`  
**Commit:** `ca91c3438321998066bfe5fc5d3867a77077f6f4` (`feat(app): add governed identity and decision workflows`)  
**Auditor:** Codex (automated)

---

## Executive Summary

The application is functionally broad and the user-requested session scope is implemented and locally validated. The repository defines 79 milestone sections: 78 are supported by passing evidence, and M77 is intentionally deferred pending explicit authorization to provision OCI. The retained runtime contains the 350-integration Aurora reference project, the 240-row ADN Import Correction QA workspace, complete 512-dimensional OCI provider embeddings, governed local authentication, granular read-only API tokens, User Management, and first-install identity onboarding.

The checkout is clean and versioned in one local `main` worktree, but it is one commit ahead of `origin/main`. The public GitHub workflow badge is passing for the previous remote commit `689ccac`; it is not evidence for local commit `ca91c34`. Direct publication of the local default branch requires explicit user authorization.

Five findings were identified. The three locally actionable findings are resolved; remote publication and the explicitly deferred OCI rollout remain:

1. Resolved: the exact backend mypy gate now passes after narrowing optional audit evidence in the bootstrap test.
2. Resolved: README and current-state architecture evidence now include M78, 142 frontend tests, and 307 provider vectors.
3. Resolved: the App Knowledge Governance Agent completed against source hash `f0459e...`, with 307/307 valid provider vectors.
4. Open external gate: the local commit is not published and therefore has no remote CI execution.
5. Deferred by design: M77 remains an OCI/OKE delivery plan with no authorized cloud mutation.

## Repository Profile

| Attribute | Evidence |
| --- | --- |
| Product shape | Next.js 15 web, FastAPI API, PostgreSQL 16, Redis/Celery, MinIO/Object Storage adapter, pure calc and pricing engines |
| Source size | 447 Python/TypeScript source files; 140,435 lines by `wc -l` |
| Git history | 390 commits; first recorded date 2026-01-07; latest 2026-08-14 |
| Worktrees | One: `/Users/javierchan/Documents/GitHub/oci`, branch `main` |
| Branch state | Clean; `main...origin/main [ahead 1]` |
| Runtime | Eight expected Compose services running; API, DB, Redis, and MinIO healthy |
| Database schema | Alembic current/head `20260814_0057` |
| Served API | 170 paths, 203 operations |
| Canonical CI | `.github/workflows/oci-dis-blueprint-quality.yml` |

## Milestone Status

The formal repository plan contains 79 milestone sections and 510 checklist items. The audit initially reopened one M78 exit item after the executable mypy gate failed; the closeout correction and exact rerun restored M78 to complete.

| Status | Count | Notes |
| --- | ---: | --- |
| Complete | 78 | All implemented milestones, including M78 authentication and installation onboarding |
| Partial / in progress | 0 | No local implementation milestone remains partial |
| Deferred / planned | 1 | M77 OCI OKE horizontal scale and observability; planning complete, provisioning not authorized |
| Not started outside planned scope | 0 | No additional undocumented application milestone was inferred |

Definition-of-done items: 510 total; 499 currently supported; 11 intentionally open in M77.

Session-request mapping:

| User request area | Governed milestone/evidence | Assessment |
| --- | --- | --- |
| Reasoning-led Import Correction, formula removal, per-line rationale, human authorization | M72-M73; 240 ADN drafts analyzed, 0 promoted, 0 `Table357` persistence | Complete |
| OCI embeddings, no assistant fallback, hidden inferred memory, persistent explicit contexts, automatic knowledge ownership | M74-M75; 307/307 provider vectors at 512 dimensions; 39 delivered provider-space assistant runs | Complete with current-run closeout pending |
| Docker cleanup and naming | One Compose project, eight aligned service names, all running | Complete |
| Ideal 3-year demo | M76; Aurora has 350 integrations, 4 scenarios, 12 environment plans, 4 BOM snapshots | Complete |
| BOM environment behavior and Rollout Explorer slider | M46; focused browser spec covers environment tabs, add/remove, all/individual scopes, and two-handle Recharts brush | Complete |
| Queretaro OKE and observability plan | M77 architecture and roadmap documents | Planned as requested; deployment deferred |
| Professional current-vs-target architecture documentation | Architecture portal, current-state audit, roadmap, decision register, OKE plan | Complete, with current evidence drift pending |
| Local auth, project isolation, API tokens, User Management, top-right account menu | M78, ADR-003, migration `20260814_0057`, focused API/UI tests | Functionally complete; type gate pending |
| New-host and future OCI installation onboarding | `scripts/bootstrap_installation.py`, README, installation runbook | Complete locally; OKE Job packaging deferred to M77 |
| One worktree, clean Git, versioned work | One clean local `main` worktree at `ca91c34` | Local complete; remote publication pending |

## Milestone Index

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [M1](#m1--repair-installation-bootstrap-typing) | Repair Installation Bootstrap Typing | Critical | complete |
| [M2](#m2--repeat-exact-quality-contract) | Repeat Exact Quality Contract | Critical | complete |
| [M3](#m3--reconcile-current-evidence-documentation) | Reconcile Current Evidence Documentation | High | complete |
| [M4](#m4--validate-documentation-and-diagrams) | Validate Documentation and Diagrams | High | complete |
| [M5](#m5--run-current-knowledge-governance-cycle) | Run Current Knowledge Governance Cycle | High | complete |
| [M6](#m6--verify-provider-space-assistant-evidence) | Verify Provider-space Assistant Evidence | High | complete |
| [M7](#m7--authorize-local-main-publication) | Authorize Local Main Publication | Critical | blocked |
| [M8](#m8--verify-remote-ci-for-exact-commit) | Verify Remote CI for Exact Commit | Critical | blocked |
| [M9](#m9--re-enter-oci-deployment-scope) | Re-enter OCI Deployment Scope | Medium | deferred |
| [M10](#m10--implement-queretaro-platform) | Implement Queretaro Platform | Medium | deferred |
| [M11](#m11--prove-scale-failure-and-rollback) | Prove Scale, Failure, and Rollback | Medium | deferred |

## Verification Results

| Verification | Result |
| --- | --- |
| Rebuilt backend quality image from current checkout | Pass |
| API tests | `340 passed in 47.30s` |
| Calc-engine tests | `99 passed` |
| Pricing-engine tests | `35 passed` |
| Frontend unit tests | `142 passed` across 24 files |
| Ruff | Pass |
| Backend mypy | Pass after explicit optional-value narrowing |
| TypeScript and ESLint Docker target | Pass |
| Next.js 15.5.23 production build | Pass, 17 static pages generated |
| npm audit | `found 0 vulnerabilities` |
| OpenAPI artifact | Up to date |
| App Knowledge artifact | Consistent with source hash `f0459e5b68fe`; 307 provider vectors, each 512 dimensions |
| Runtime health/readiness | `status=ready`; migrations and Object Storage ready |
| Aurora reference data | 350 catalog integrations; 4 scenarios; 12 environment plans; 4 BOM snapshots |
| ADN Import Correction data | 1 capture session; 240 staged/analyzed rows; 0 promotions; 0 formula-string persistence |
| Assistant operational evidence | 39 delivered provider-space runs; configured paths fail closed rather than substituting answers |
| Current assistant closeout | Run `1fba79ea...`: provider retrieval, delivered/completed, Guardrails completed, `grounding_fallback=false` |
| Authenticated Dashboard regression | Aurora renders 350/350 QA after transaction-boundary fix; fresh browser tab has zero warnings/errors |
| Remote canonical workflow | Public badge passing for `origin/main` at prior commit only |

The attempted host `npm test` returned `vitest: command not found`. This is not a product failure: repository policy forbids host dependencies. The canonical Docker test target subsequently passed 142 tests.

## Pending Tasks

### CRITICAL

- Git `main` / release governance — the final local closeout is not published, so exact-commit remote quality, browser, and image-scan jobs do not exist.

### INCOMPLETE

- None after local remediation and validation.

### DEFERRED

- M77 / `docs/architecture/oci-oke-horizontal-scale-deployment-plan.md` — OCI infrastructure, IAM integration, shared knowledge publication, and observability are intentionally unprovisioned until explicit user authorization.

## Finding Remediation Plans

### Finding: Exact M78 type gate fails
- Classification: `CRITICAL`
- Impact: Prevents the exact local commit from passing canonical CI.
- Area: M78 authentication and installation onboarding.
- Evidence:
  - `apps/api/app/tests/test_installation_bootstrap.py:66` — `event.new_value` is typed `dict | None` and is indexed without a narrowing assertion.
  - `docker run ... mypy app ...` — exits 1 with `[index]`.
- Issue: A test-only optional JSON value is not narrowed before indexing.
- Remediation Milestones:
  - `M1` — Repair Installation Bootstrap Typing (`Critical`, `complete`).
  - `M2` — Repeat Exact Quality Contract (`Critical`, `complete`).

### Finding: Architecture evidence is stale
- Classification: `INCOMPLETE`
- Impact: Professional documentation does not exactly describe the validated commit.
- Area: Architecture governance and M78.
- Evidence:
  - `README.md` milestone table — ends at M77 and omits completed M78.
  - `docs/architecture/current-state-architecture.md` — records 135 frontend tests and old App Knowledge hash `02796f...`; current evidence is 142 and `f0459e...`.
- Issue: The implementation and validation advanced after the architecture evidence table was written.
- Remediation Milestones:
  - `M3` — Reconcile Current Evidence Documentation (`High`, `complete`).
  - `M4` — Validate Documentation and Diagrams (`High`, `complete`).

### Finding: Current knowledge artifact lacks a matching persisted maintenance run
- Classification: `INCOMPLETE`
- Impact: The artifact is valid, but operational ownership evidence does not yet close the latest change cycle.
- Area: M75 App Knowledge Governance Agent.
- Evidence:
  - `derived_app_knowledge.json` — source hash `f0459e...`, 307/307 provider vectors at 512 dimensions.
  - `knowledge_maintenance_jobs` — completed jobs carry older source hashes only.
- Issue: The daily scheduled agent has not yet persisted a completion record for the latest application contract.
- Remediation Milestones:
  - `M5` — Run Current Knowledge Governance Cycle (`High`, `complete`).
  - `M6` — Verify Provider-space Assistant Evidence (`High`, `complete`).

### Finding: Exact commit has no remote CI execution
- Classification: `CRITICAL`
- Impact: Remote quality, browser E2E, and image security cannot be claimed for `ca91c34`.
- Area: Repository publication and CI/CD.
- Evidence:
  - `git status --branch` — `main...origin/main [ahead 1]`.
  - GitHub workflow badge — passing for the current remote branch, whose local tracking ref is `689ccac`.
- Issue: The locally versioned changes have not been published to GitHub.
- Remediation Milestones:
  - `M7` — Authorize Local Main Publication (`Critical`, `blocked`).
  - `M8` — Verify Remote CI for Exact Commit (`Critical`, `blocked`).

### Finding: OCI horizontal-scale implementation is intentionally deferred
- Classification: `DEFERRED`
- Impact: The App cannot yet claim production OKE scale-out or OCI-native observability.
- Area: M77 deployment.
- Evidence:
  - `AGENTS.md` M77 — 0/11 implementation items checked.
  - `docs/architecture/current-vs-target-roadmap.md` — authorization is planning/repository work only.
- Issue: The plan is complete, but OCI discovery and resource creation require a future explicit authorization and tenancy decisions.
- Remediation Milestones:
  - `M9` — Re-enter OCI Deployment Scope (`Medium`, `deferred`).
  - `M10` — Implement Queretaro Platform (`Medium`, `deferred`).
  - `M11` — Prove Scale, Failure, and Rollback (`Medium`, `deferred`).

## M1 — Repair Installation Bootstrap Typing

**Tier:** Critical  
**Status:** complete

### Problem

The canonical backend type gate fails in an onboarding test.

### Root Cause

SQLAlchemy correctly types `AuditEvent.new_value` as optional, while the test assumes it is always populated after asserting only that the event exists.

### Target State

- The test narrows `new_value` before reading the bootstrap flag.
- Runtime behavior remains unchanged.

### Files Most Likely To Change

- `apps/api/app/tests/test_installation_bootstrap.py` — add a precise non-`None` assertion.

### Action Plan

1. Assert `event.new_value is not None` immediately before indexing.
2. Rerun focused bootstrap tests and exact mypy.
3. Confirm no runtime or API contract change.

### Exit Criteria

- Focused bootstrap tests pass.
- `python -m mypy app --ignore-missing-imports --no-error-summary` exits 0.

## M2 — Repeat Exact Quality Contract

**Tier:** Critical  
**Status:** complete

### Problem

The commit cannot be closed from partial or stale-image evidence.

### Root Cause

An earlier quality image predated the final App Knowledge artifact and the current exact mypy run failed.

### Target State

- All canonical backend/frontend/drift/runtime gates pass from newly built images.
- Evidence is attached to the final repository state.

### Files Most Likely To Change

- No additional source file is expected.

### Action Plan

1. Rebuild quality and production targets after all fixes.
2. Run API, calc, pricing, Ruff, mypy, OpenAPI, App Knowledge, frontend test/lint/build, and npm audit gates.
3. Recheck Compose health, migrations, readiness, and focused browser behavior.

### Exit Criteria

- Every canonical local gate exits 0.
- Git diff is clean after versioning the closeout.

## M3 — Reconcile Current Evidence Documentation

**Tier:** High  
**Status:** complete

### Problem

The architecture portal understates the latest authentication and validation state.

### Root Cause

M78 and its final validation ran after the prior architecture audit snapshot.

### Target State

- README lists M78 truthfully.
- Architecture evidence uses final counts, hash, auth/onboarding results, and Git state.

### Files Most Likely To Change

- `README.md` — milestone and validation snapshot.
- `docs/architecture/current-state-architecture.md` — observed evidence and limitations.

### Action Plan

1. Replace stale counts and hashes with final executable evidence.
2. Add M78 to the milestone table and authentication validation summary.
3. Preserve M77 as planned rather than implying OCI deployment.

### Exit Criteria

- No current-state document reports obsolete test/vector evidence.
- M78 and M77 statuses are mutually consistent across README, AGENTS, and architecture docs.

## M4 — Validate Documentation and Diagrams

**Tier:** High  
**Status:** complete

### Problem

Documentation changes require structural and Mermaid validation.

### Root Cause

Architecture documentation is executable delivery evidence and cannot rely on visual inspection alone.

### Target State

- Internal links, milestone references, and Mermaid blocks remain valid.
- The audit report is retained as versioned closeout evidence.

### Files Most Likely To Change

- `docs/reports/audit-report-20260814-023244.md` — final status reconciliation.

### Action Plan

1. Run repository documentation/Mermaid validators when present.
2. Check Markdown links and milestone references with repository scripts or focused searches.
3. Update this report with final statuses and residual external blocker.

### Exit Criteria

- All available documentation validators pass.
- Every indexed remediation milestone has one matching detailed section.

## M5 — Run Current Knowledge Governance Cycle

**Tier:** High  
**Status:** complete

### Problem

No completed maintenance job records the current App Knowledge source hash.

### Root Cause

The latest contract was deployed inside the normal schedule interval.

### Target State

- The repository-owned App Knowledge Governance Agent records a terminal run against the current artifact.
- It does not require manual approval for derived App knowledge or provider-vector publication.

### Files Most Likely To Change

- No source file is expected; this is operational validation against the live stack.

### Action Plan

1. Dispatch the existing scheduled knowledge-maintenance task.
2. Wait for terminal completion and verify the source hash and embedding-maintenance result.
3. Confirm no incomplete vector space was activated.

### Exit Criteria

- A completed job/run records the current source hash.
- Provider vector count remains 307/307 at 512 dimensions.

## M6 — Verify Provider-space Assistant Evidence

**Tier:** High  
**Status:** complete

### Problem

Knowledge maintenance must not regress assistant delivery or introduce fallback.

### Root Cause

The assistant depends on the active vector artifact and remote provider boundary.

### Target State

- Provider-space retrieval remains active after maintenance.
- Any provider failure is terminal and visible, never substituted with fallback prose.

### Files Most Likely To Change

- No source file is expected.

### Action Plan

1. Inspect the terminal maintenance result and active runtime artifact.
2. Execute or inspect a governed assistant case using provider retrieval.
3. Verify `retrieval_embedding_space=provider`, delivered state, citations, and no fallback.

### Exit Criteria

- A current provider-space assistant case completes with governed citations.
- No answer fallback is reported.

## M7 — Authorize Local Main Publication

**Tier:** Critical  
**Status:** blocked

### Problem

The clean local commit is one ahead of the remote default branch.

### Root Cause

Direct push to `main` is an external mutation and the approval boundary rejected the prior attempt without a fresh explicit authorization.

### Target State

- The user explicitly authorizes publication of the reviewed local `main` commit.
- Push is fast-forward only and targets the confirmed remote.

### Files Most Likely To Change

- No repository source file; Git remote state only.

### Action Plan

1. Present the final local commit IDs and clean status.
2. Obtain explicit authorization to push local `main` to `origin/main`.
3. Fetch/prune, verify fast-forward, and push without force.

### Exit Criteria

- `origin/main` resolves to the final local commit.
- No force push or unrelated branch mutation occurs.

## M8 — Verify Remote CI for Exact Commit

**Tier:** Critical  
**Status:** blocked

### Problem

The current public passing badge covers only the prior remote commit.

### Root Cause

GitHub Actions cannot run the new commit until it is published.

### Target State

- Quality, Browser E2E, and Production Image Security jobs pass for the exact final SHA.

### Files Most Likely To Change

- `.github/workflows/oci-dis-blueprint-quality.yml` only if the exact run exposes a workflow defect.

### Action Plan

1. Wait for the workflow triggered by the authorized push.
2. Inspect every job conclusion and failure artifact.
3. Correct any genuine regression, republish, and repeat until all jobs are green.

### Exit Criteria

- GitHub reports all canonical jobs successful for the final SHA.
- The public workflow badge and SHA-specific run agree.

## M9 — Re-enter OCI Deployment Scope

**Tier:** Medium  
**Status:** deferred

### Problem

OCI tenancy discovery and resource creation are not currently authorized.

### Root Cause

The user explicitly requested planning first and will indicate when deployment should begin.

### Target State

- Authorization, tenancy identity, compartments, network posture, managed database choice, RPO/RTO, and SLOs are recorded before mutation.

### Files Most Likely To Change

- `docs/adr/` — approved identity, topology, DR, and observability decisions.
- `docs/architecture/oci-oke-horizontal-scale-deployment-plan.md` — tenancy-backed reconciliation.

### Action Plan

1. Re-enter only on explicit user authorization.
2. Authenticate with this workspace's isolated OCI identity and discover Queretaro capabilities.
3. Convert open decisions into ADRs and reviewed resource estimates.

### Exit Criteria

- Required decisions and permissions are approved with current tenancy evidence.
- No credentials or assumptions cross workspace boundaries.

## M10 — Implement Queretaro Platform

**Tier:** Medium  
**Status:** deferred

### Problem

No Terraform, Helm, or provisioned observability platform currently exists.

### Root Cause

Implementation correctly waits for M9 authorization and decisions.

### Target State

- Primary resources run in `mx-queretaro-1`; OCI GenAI remains a governed Chicago dependency.
- Identity, shared knowledge, data, queue recovery, probes, secrets, and observability meet M77.

### Files Most Likely To Change

- Future `infra/terraform/` and `deploy/helm/` paths after authorization.
- Application readiness, telemetry, and queue configuration.

### Action Plan

1. Implement application-side M77 prerequisites in independently testable slices.
2. Provision reviewed OCI resources through IaC and immutable image digests.
3. Deploy migrations, seed, first-Admin bootstrap Job, and workloads in governed order.

### Exit Criteria

- IaC and Helm are reproducible and reviewed.
- Queretaro non-production deployment is healthy with no secret or telemetry leakage.

## M11 — Prove Scale, Failure, and Rollback

**Tier:** Medium  
**Status:** deferred

### Problem

Horizontal-scale readiness is a behavioral claim, not a container-count claim.

### Root Cause

The required multi-replica and failure environment does not exist yet.

### Target State

- Aurora passes scale, pod loss, queue/database/cache failure, shared-hash, no-fallback, alarm, backup, and rollback tests.

### Files Most Likely To Change

- Future load/failure test suites, runbooks, dashboards, and release evidence.

### Action Plan

1. Execute multi-replica load and controlled failure scenarios.
2. Validate OpenTelemetry correlation, fixed cardinality, alarms, and seven-day cost baseline.
3. Exercise rollback, restore, and secret rotation before production promotion.

### Exit Criteria

- Every M77 exit test is retained and passing.
- Production rollout requires a separate explicit authorization.

## Closeout Reconciliation

- `apps/api/app/tests/test_installation_bootstrap.py` now narrows optional audit
  evidence before indexing; exact mypy exits 0.
- `apps/api/app/core/auth.py` now commits authenticated session usage and closes
  the read-only membership transaction before route-owned transaction scopes.
  The regression is covered by an authenticated Dashboard snapshot request.
- `README.md` and `docs/architecture/current-state-architecture.md` now report
  M78, 340 API tests, 142 frontend tests, and 307/307 provider vectors.
- Knowledge Governance AgentRun `2decf77e...` completed and persisted source hash
  `f0459e5b68fe...`, zero findings, model `OpenAI gpt-oss-120b`, and a complete
  307-vector Cohere Embed v4 space at 512 dimensions.
- Post-maintenance Assistant AgentRun `1fba79ea...` answered the Aurora project
  question with `retrieval_embedding_space=provider`, delivery and provider
  status `completed`, Guardrails `completed`, and `grounding_fallback=false`.
- Final local gates pass: API 340, calc 99, pricing 35, frontend 142, Ruff,
  mypy, TypeScript, ESLint, production build, npm audit, OpenAPI drift, App
  Knowledge drift, migration/readiness, Compose health, and fresh-tab browser
  console validation.
- The only unresolved release gate is external: publish the final local commit
  after explicit authorization and require all SHA-specific GitHub jobs to pass.

## Debt Markers

The only non-test source matches for `TODO|FIXME|HACK|PLACEHOLDER|XXX|NOCOMMIT` are intentional placeholder-detection regular expressions in the agent output and support grounding services. No actionable debt marker was found.

## Recommended Next Actions

1. Obtain explicit authorization for M7, then monitor M8 until all exact-SHA jobs pass.
2. Keep M9-M11 deferred until the user explicitly opens OCI deployment scope.

## Dependency Drift

`npm outdated --depth=0` reports no direct workspace drift and `npm audit` reports zero vulnerabilities. Python has several newer releases available, including major-version jumps for Redis, pytest/pytest-asyncio, mypy, and other runtime libraries. These upgrades are not required to close a known security finding and should be handled in isolated compatibility batches with full CI rather than mixed into the authentication/onboarding closeout.
