{
  "name": "oci-dis-blueprint",
  "version": "1.0.0",
  "private": true,
  "description": "OCI Integration Design Blueprint — monorepo",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "dev:infra": "docker compose up db redis minio -d",
    "dev:api": "docker compose up api worker -d",
    "dev:web": "docker compose up web -d",
    "build": "docker compose build",
    "down": "docker compose down",
    "test": "docker compose run --rm api pytest app/tests /calc-engine/src/tests -v",
    "test:web": "docker build --target test --output type=cacheonly -f apps/web/Dockerfile .",
    "logs": "docker compose logs -f",
    "db:migrate": "docker compose run --rm api alembic upgrade head",
    "db:rollback": "docker compose run --rm api alembic downgrade -1",
    "db:seed": "docker compose run --rm api python -m app.migrations.seed",
    "lint:api": "docker compose run --rm api ruff check app /calc-engine/src",
    "lint:web": "docker build --target lint --output type=cacheonly -f apps/web/Dockerfile ."
  },
  "overrides": {
    "postcss": "8.5.12",
    "next": {
      ".": "$next",
      "postcss": "8.5.12"
    }
  },
  "engines": {
    "node": "26.0.0"
  }
}
