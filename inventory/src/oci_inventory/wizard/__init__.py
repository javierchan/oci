"""Interactive UX wrappers for `oci-inv`.

This package is intentionally optional. The base CLI (`oci-inv`) remains the
source of truth; the wizard collects user intent and executes existing command
handlers.
"""
