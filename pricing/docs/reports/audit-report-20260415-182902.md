# Project Audit Report

**Generated:** 2026-04-15 18:29 CST
**Repository:** https://github.com/javierchan/oci.git
**Branch:** `vscode/codex-active-work`
**Commit:** `155b203 feat(session-store): add new test clients and sessions for quote exports and events`
**Auditor:** Codex (automated)

---

## Executive Summary

The `pricing` codebase is in a strong implementation state locally, but the audit found one remaining documented milestone gap and one operational rollout gap.

- The local backend verification is strong: `npm test` from `pricing/server` passed with `898/898` tests.
- The documented milestone index in `pricing/docs/IMPROVEMENT_MILESTONES.md` marks all ten milestones `complete`, but **M1 is not actually complete in source**: `pricing/server/dependency-resolver.js` still performs widespread `request.source` reparsing, which directly violates M1 exit criteria.
- The current source tree contains working implementations for M8, M9, and M10, including versioned routes, auth/rate limiting, and Prometheus metrics, and the local tests covering those areas pass.
- The running Docker container at `localhost:8742` is **not serving the current checkout**. Runtime probes showed legacy `/api/health` working while `/api/v1/health`, `/api/v1/openapi.json`, and `/api/metrics` all fell through to the SPA HTML shell. This is a deployment drift issue, not source-of-truth evidence against the checkout.
- The active branch is dirty and includes mutable runtime artifacts plus local dependency-tree churn under `pricing/server/node_modules/`, which raises release hygiene risk.

**Completion summary:** `9/10` documented milestones are complete in the audited source checkout. `1/10` is partial. There are `2` critical blocking findings: the unresolved M1 contract gap and the stale runtime deployment.

## Repository Profile

- Selected codebase: `pricing`
- Branch root: `/Users/javierchan/Documents/GitHub/oci`
- Codebase path: `/Users/javierchan/Documents/GitHub/oci/pricing`
- Execution location: `/Users/javierchan/Documents/GitHub/oci`
- Write location: `/Users/javierchan/Documents/GitHub/oci/pricing/docs/reports/audit-report-20260415-182902.md`
- Worktree mode: canonical checkout for `vscode/codex-active-work`

### Stack And Shape

- Backend stack: Node.js + Express in `pricing/server`
- Frontend stack: static HTML/JS app in `pricing/app`
- Containerization: `pricing/docker-compose.yml`, `pricing/Dockerfile`
- Test framework: Node built-in test runner via `node --test ./test/*.test.js`
- CI/CD evidence: `.github/` exists in the repo and `pricing/server/test/api-versioning-openapi.test.js` validates the contract artifact
- No Python backend, database migrations, or separate TypeScript app were detected inside the selected codebase

### Repository Summary

- First commit date in repo history: `2026-01-07`
- Latest commit date in repo history: `2026-04-15`
- Total commits in repository: `228`
- Commits in prior 7-day window: `2`
- Commits in last 7 days: `39`
- Approximate selected-codebase source files scanned: `146`
- `cloc` summary: unavailable in this environment (`cloc` not installed)

### Current Branch State

The active checkout is dirty:

```text
 M pricing/data/session-store.json
 M pricing/docs/IMPROVEMENT_MILESTONES.md
 M pricing/server/assistant.js
 M pricing/server/genai.js
 M pricing/server/index.js
 M pricing/server/node_modules/.package-lock.json
 M pricing/server/package-lock.json
 M pricing/server/package.json
?? .github/
?? pricing/docs/openapi.yaml
?? pricing/docs/reports/
?? pricing/server/auth.js
?? pricing/server/metrics.js
?? pricing/server/node_modules/ajv-formats/
?? pricing/server/node_modules/ajv/
?? pricing/server/node_modules/fast-deep-equal/
?? pricing/server/node_modules/fast-uri/
?? pricing/server/node_modules/json-schema-traverse/
?? pricing/server/node_modules/lodash.merge/
?? pricing/server/node_modules/openapi-schema-validator/
?? pricing/server/node_modules/openapi-types/
?? pricing/server/node_modules/require-from-string/
?? pricing/server/test/api-versioning-openapi.test.js
?? pricing/server/test/auth-rate-limit.test.js
?? pricing/server/test/metrics-observability.test.js
```

This is materially relevant to release readiness because it includes mutable session data and local dependency tree churn.

## Documented Milestone Status

### `pricing M1` — Service Detection Metadata Migration
**Status:** ⚠ Partial

Evidence:
- `pricing/server/dependency-resolver.js:46-75` does dispatch through `FAMILY_RESOLVERS`.
- `pricing/server/dependency-resolver.js:466-1887` still contains extensive `request.source` reparsing and free-text matching.
- `rg -n "request\\.source" pricing/server/dependency-resolver.js` returned many live matches.

Gaps:
- Exit criterion “`resolveRequestDependencies()` contains no string matching against `request.source`” is not met.
- The resolver is still partly text-driven instead of fully metadata-driven.

Deviation:
- `pricing/docs/IMPROVEMENT_MILESTONES.md` marks M1 `complete`, but source evidence does not support that state yet.

### `pricing M2` — GenAI Intent Output Validation
**Status:** ✅ Complete

Evidence:
- `pricing/server/intent-schema.js:148-175` defines structured validation.
- `pricing/server/intent-extractor.js:5,76-87` validates parsed model output and logs schema violations.
- `pricing/server/test/intent-validation-fallback.test.js` is included in the passing suite.

### `pricing M3` — Session Store Async and Hardening
**Status:** ✅ Complete

Evidence:
- `pricing/server/session-store.js:137` uses `fs.promises.writeFile`, not synchronous writes.
- `pricing/server/session-store.js:160-184` prunes expired sessions.
- `pricing/server/index.js:68,819` ignores request-body session context in favor of stored session state.
- `pricing/server/test/session-store-hardening.test.js` and `pricing/server/test/assistant-session-context-hardening.test.js` cover concurrency, TTL, LRU, and request-body hardening.

### `pricing M4` — GenAI Configuration Externalization
**Status:** ✅ Complete

Evidence:
- `pricing/server/genai-profiles.js` defines named request profiles.
- `pricing/server/test/genai-profiles.test.js` verifies profile resolution without OCI calls.
- `pricing/server/genai.js` resolves model/runtime settings from config and environment instead of hardcoded OCID fallbacks.

### `pricing M5` — Structured Logging
**Status:** ✅ Complete

Evidence:
- `pricing/server/logger.js:9-133` provides structured logging helpers and JSON/human-readable formatting.
- `pricing/server/index.js:722-918` emits request lifecycle logs for `/api/v1/chat` and `/api/v1/assistant`.
- `pricing/server/test/structured-logging.test.js:77-155` verifies JSON output and assistant completion traces.

Deviation:
- `console.log` still exists for startup/catalog bootstrap messages in `pricing/server/index.js`; the milestone only required removal of `console.warn`, which is satisfied.

### `pricing M6` — Prompt Caching and Token Optimization
**Status:** ✅ Complete

Evidence:
- `pricing/server/intent-extractor.js:52` reads `OCI_GENAI_INTENT_HISTORY_TURNS`.
- `pricing/server/genai.js:23-106,290` enforces token-budget warnings, logs usage metadata, and documents current SDK prompt-cache limits.
- `pricing/server/test/prompt-optimization.test.js` covers truncation, overrides, warnings, and usage logging.

### `pricing M7` — Semantic Error Handling and Error Codes
**Status:** ✅ Complete

Evidence:
- `pricing/server/errors.js:3-104` defines structured error classes and shared serialization.
- `pricing/server/index.js` consistently routes failures through `handleError(...)`.
- `pricing/server/test/http-error-handling.test.js` verifies semantic error codes and non-leakage of stack traces.

### `pricing M8` — API Versioning and Contract Hardening
**Status:** ✅ Complete

Evidence:
- `pricing/server/index.js:934,1358` exposes versioned metrics and legacy-compatible routes.
- `pricing/docs/openapi.yaml` exists.
- `pricing/server/test/api-versioning-openapi.test.js:87-96` verifies OpenAPI 3.1 validity and contract shape.

Deviation:
- The live container at `localhost:8742` is not serving the current versioned API surface. This is treated as operational deployment drift, not a source milestone failure.

### `pricing M9` — Authentication and Rate Limiting
**Status:** ✅ Complete

Evidence:
- `pricing/server/auth.js:13-86` defines shared-key auth and rate limiting.
- `pricing/server/index.js:158-194` enforces `AUTH_UNAUTHORIZED` and `RATE_LIMITED`.
- `pricing/server/test/auth-rate-limit.test.js:144-201` verifies shared-secret enforcement and 429 behavior.

### `pricing M10` — Observability and Cost Metrics
**Status:** ✅ Complete

Evidence:
- `pricing/server/metrics.js:1-209` defines counters, histogram, and Prometheus rendering.
- `pricing/server/genai.js:75-106,343-509` reads SDK usage fields and records metrics.
- `pricing/server/assistant.js:456-471` records assistant outcomes.
- `pricing/server/index.js:170-178,934,1358` exposes metrics handlers.
- `pricing/server/test/metrics-observability.test.js` verifies the endpoint and counter behavior.

Deviation:
- The running container at `localhost:8742` did not expose `/api/metrics`, indicating deployment drift from the audited checkout.

## Progress Metrics

Derived from `pricing/docs/IMPROVEMENT_MILESTONES.md` and audit verification:

- Total milestones: `10`
- Complete: `9`
- Partial: `1`
- Not started: `0`
- In progress: `0`

Definition-of-done metrics:

- Definition-of-done items total: `38`
- Items verified: `37`
- Items with gaps: `1`

## Audit Remediation Milestone Index

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [M11](#m11--resolver-source-text-elimination) | Resolver Source-Text Elimination | Critical | open |
| [M12](#m12--m1-closeout-and-status-realignment) | M1 Closeout and Status Realignment | High | open |
| [M13](#m13--runtime-deployment-provenance-and-refresh) | Runtime Deployment Provenance and Refresh | Critical | open |
| [M14](#m14--post-deploy-contract-and-metrics-smoke-validation) | Post-Deploy Contract and Metrics Smoke Validation | High | open |

## Verification Results

### Git Context

```text
$ git rev-parse --show-toplevel
/Users/javierchan/Documents/GitHub/oci

$ git branch --show-current
vscode/codex-active-work

$ git worktree list --porcelain
worktree /Users/javierchan/Documents/GitHub/oci
HEAD 155b2036e0dd687521645b16595303fc84f411ce
branch refs/heads/vscode/codex-active-work

worktree /Users/javierchan/.codex/worktrees/7ea6/oci
HEAD 6990ea595854c3cdd8faf67948cef375a07f7a23
detached

worktree /Users/javierchan/.codex/worktrees/b840/oci
HEAD 54c107179414e5ea97418acd4c2ce637032fad96
branch refs/heads/codex/codex-active-work
```

### Local Test Suite

```text
$ (cd pricing/server && npm test)
ℹ tests 898
ℹ pass 898
ℹ fail 0
ℹ duration_ms 14404.740417
```

### Docker Runtime

```text
$ (cd pricing && docker compose ps)
NAME                IMAGE               COMMAND                  SERVICE     CREATED      STATUS      PORTS
oci-pricing-agent   pricing-oci-agent   "docker-entrypoint.s…"   oci-agent   5 days ago   Up 2 days   0.0.0.0:8742->8742/tcp, [::]:8742->8742/tcp

$ (cd pricing && docker compose ps --format json ...)
Containers: 1/1 running
  oci-agent            Up 2 days
```

### Live API Probes

```text
$ curl -i -s http://localhost:8742/api/health | head -20
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
...
{"ok":true,"catalogsLoaded":4,...}

$ curl -i -s http://localhost:8742/api/v1/health | head -20
HTTP/1.1 200 OK
Content-Type: text/html; charset=UTF-8
...
<!DOCTYPE html>

$ curl -i -s http://localhost:8742/api/metrics | head -20
HTTP/1.1 200 OK
Content-Type: text/html; charset=UTF-8
...
<!DOCTYPE html>
```

Interpretation:
- The container is healthy enough to serve the app.
- It is not serving the same API surface as the audited checkout.
- Runtime deployment evidence is therefore not used to downgrade source milestone completeness, but it is elevated as an operational blocker.

### Dependency Drift Probe

```text
$ (cd pricing/server && npm outdated --depth=0)
npm error code EPERM
npm error syscall open
npm error path /Users/javierchan/.npm/_cacache/tmp/***
npm error errno EPERM
...
npm error Your cache folder contains root-owned files
```

Interpretation:
- Dependency drift could not be audited safely from this environment because the local npm cache is permission-corrupted.
- This is an audit-environment limitation, not direct evidence of package freshness.

## Pending Tasks

### `CRITICAL`

- `pricing/server/dependency-resolver.js` — `pricing M1` — eliminate remaining `request.source` reparsing so the resolver actually satisfies the documented metadata-driven contract.
- `pricing/docker-compose.yml` / live `oci-agent` container on `localhost:8742` — `pricing M8/M10 rollout` — rebuild or redeploy from the current branch so runtime behavior matches the validated checkout.

### `INCOMPLETE`

- `pricing/docs/IMPROVEMENT_MILESTONES.md` — `pricing M1` — the status field should not remain `complete` until the remaining source-text resolver logic is removed and revalidated.
- `pricing/data/session-store.json` — release hygiene — mutable runtime data is currently mixed into the dirty working tree and should be excluded from release-ready state handling.

### `DEFERRED`

- `pricing/server` dependency audit — environment blocker — rerun `npm outdated --depth=0` after npm cache ownership is repaired so package drift can be assessed cleanly.

## Finding Remediation Plans

### Finding: M1 is still incomplete in source
- Classification: `CRITICAL`
- Impact: deterministic routing integrity and documentation trustworthiness
- Area: documented milestone `pricing M1`
- Evidence:
  - `pricing/server/dependency-resolver.js:466-1887` — many active `request.source` reparsing sites remain.
  - `pricing/docs/IMPROVEMENT_MILESTONES.md` — M1 is marked `complete`.
  - `rg -n "request\\.source" pricing/server/dependency-resolver.js` — confirms the exit criterion is still violated.
- Issue: the codebase declares M1 finished even though the resolver still depends on free-text reparsing in core dependency resolution.
- Remediation Milestones:
  - `M11` — Resolver Source-Text Elimination (`Critical`, `open`): remove the remaining text-driven dependency logic from the resolver path.
  - `M12` — M1 Closeout and Status Realignment (`High`, `open`): validate the migration and only then realign milestone status and supporting docs.

### Finding: Local runtime deployment does not match the audited checkout
- Classification: `CRITICAL`
- Impact: release confidence, operational validation, and on-box debugging accuracy
- Area: deployment/runtime provenance for `pricing`
- Evidence:
  - `curl http://localhost:8742/api/health` — returns JSON from a legacy route.
  - `curl http://localhost:8742/api/v1/health` — returns SPA HTML, not the versioned API response expected from current source.
  - `curl http://localhost:8742/api/metrics` — returns SPA HTML, not the Prometheus metrics route present in current source.
  - `docker compose ps` — container has been up for 2 days, consistent with a stale image relative to current local changes.
- Issue: runtime smoke checks are exercising an older build than the checkout that passed local tests, so operational verification is currently misleading.
- Remediation Milestones:
  - `M13` — Runtime Deployment Provenance and Refresh (`Critical`, `open`): make the running container provenance obvious and refresh it from the current branch.
  - `M14` — Post-Deploy Contract and Metrics Smoke Validation (`High`, `open`): perform focused live contract checks after redeploy and document the deployed state.

## Debt Markers

```text
$ grep TODO/FIXME/HACK/... in pricing
pricing/server/genai.js:78:    // TODO: keep this aligned with the OCI SDK usage object. The current Node SDK
```

Observed debt count from the selected scan: `1`

## Recommended Next Actions

1. Treat `pricing M1` as the next engineering priority and revert its reported status to reality until the resolver no longer reparses `request.source`.
2. Rebuild or restart the `pricing` Docker container from the current branch before relying on any more live API checks.
3. Clean the branch state before merge or push by separating source changes from mutable runtime artifacts and local dependency installs.
4. After runtime refresh, re-run a focused smoke set: `/api/v1/health`, `/api/metrics`, and one versioned assistant/chat path.
5. Repair local npm cache ownership and rerun dependency drift checks so package freshness can be audited cleanly.

## Dependency Drift

- Node dependency drift audit command was attempted from `pricing/server` but failed because the local npm cache contains root-owned files.
- The codebase does show explicit package evolution in the dirty worktree:
  - `pricing/server/package.json`
  - `pricing/server/package-lock.json`
  - `pricing/server/node_modules/.package-lock.json`
- Because `npm outdated` did not complete successfully, this audit does not make freshness claims beyond that environment limitation.

## M11 — Resolver Source-Text Elimination

**Tier:** Critical
**Status:** open

### Problem

`pricing/server/dependency-resolver.js` still reparses free-text `request.source` in the dependency path, which keeps the system in a double-classification state and leaves M1 objectively unfinished.

### Root Cause

The codebase migrated the initial dispatch into `FAMILY_RESOLVERS`, but many family-specific resolution branches still carry legacy text parsing that was never fully moved behind stable metadata or normalized inputs.

### Target State

- `resolveRequestDependencies()` and the family-specific resolution path no longer depend on raw `request.source` string matching for supported families.
- Resolver behavior is driven by normalized intent/family metadata and explicit extracted inputs.
- The remaining source-text usage, if any, is isolated to clearly temporary or out-of-scope families and documented explicitly.

### Files Most Likely To Change

- `pricing/server/dependency-resolver.js` — remove or isolate remaining text-driven resolution logic
- `pricing/server/service-families.js` — extend family metadata where resolver inputs are still implicit
- `pricing/server/intent-extractor.js` — ensure resolver-facing fields are emitted explicitly when needed

### Action Plan

1. Inventory each remaining `request.source` use in `pricing/server/dependency-resolver.js` and group them by family and resolution reason.
2. Move supported-family dependency inputs behind metadata or normalized extracted inputs so resolver entry points no longer need raw source reparsing.
3. Add or extend targeted tests for the migrated families so the new contract is enforced by regression coverage.
4. Re-run the full backend suite and a focused search proving the targeted source-text usages are removed from resolver-critical paths.

### Exit Criteria

- `pricing/server/dependency-resolver.js` no longer performs resolver-critical string matching against `request.source` for supported families.
- The resolver continues to pass the full `pricing/server` test suite.
- A code search and focused tests demonstrate that the M1 contract violation has been removed, not just moved around.

## M12 — M1 Closeout and Status Realignment

**Tier:** High
**Status:** open

### Problem

The milestone tracker currently overstates completion by marking M1 `complete` before its exit criteria are actually satisfied.

### Root Cause

Status tracking appears to have been updated incrementally while the underlying resolver migration remained unfinished.

### Target State

- Documentation reflects implementation truth at all times.
- M1 is only marked `complete` after code evidence and tests confirm every exit criterion is met.
- Supporting audit artifacts and roadmap references are synchronized with the validated implementation state.

### Files Most Likely To Change

- `pricing/docs/IMPROVEMENT_MILESTONES.md` — realign status after validation
- `pricing/docs/reports/` — preserve audit evidence and follow-up verification artifacts

### Action Plan

1. Keep or revert M1 status to a non-complete state until M11 is validated.
2. After M11 lands, verify each M1 exit criterion explicitly with code search and tests.
3. Update the milestone index and any supporting progress artifacts only after successful validation.
4. Capture a short closeout note or audit follow-up showing the exact evidence that justified the status change.

### Exit Criteria

- `pricing/docs/IMPROVEMENT_MILESTONES.md` reflects the true implementation state of M1.
- Each M1 exit criterion is backed by fresh validation evidence.
- The audit trail clearly shows when and why the milestone status changed.

## M13 — Runtime Deployment Provenance and Refresh

**Tier:** Critical
**Status:** open

### Problem

The local Docker runtime is serving an API surface that does not match the audited checkout, so live validation currently exercises stale code.

### Root Cause

The running container has not been refreshed from the current branch state, and there is no visible deployment provenance signal confirming which source revision is actually running.

### Target State

- The local runtime is rebuilt or restarted from the current validated checkout.
- The running service exposes an obvious provenance signal through logs, image metadata, or startup output.
- Live probes can be trusted to reflect the current branch instead of an older image.

### Files Most Likely To Change

- `pricing/docker-compose.yml` — deployment/build behavior if provenance or refresh wiring is needed
- `pricing/Dockerfile` — image metadata or startup provenance output if needed
- `pricing/server/index.js` — optional startup/version logging if provenance needs to be surfaced in-app

### Action Plan

1. Confirm the intended deployment source for the local `oci-agent` container and rebuild or restart it from the current checkout.
2. Add a lightweight provenance indicator if the runtime cannot currently reveal its source revision clearly.
3. Verify that the refreshed runtime now serves the versioned API and metrics surface present in source.
4. Capture the refreshed container state and runtime command outputs as rollout evidence.

### Exit Criteria

- `docker compose ps` shows the refreshed `oci-agent` container running from the current local deployment cycle.
- `curl http://localhost:8742/api/v1/health` returns the expected JSON API response.
- `curl http://localhost:8742/api/metrics` returns Prometheus text instead of the SPA HTML shell.

## M14 — Post-Deploy Contract and Metrics Smoke Validation

**Tier:** High
**Status:** open

### Problem

Even after a runtime refresh, there is no captured post-deploy smoke evidence yet proving that the deployed API and observability surface match the validated source checkout.

### Root Cause

The current workflow relies heavily on source-level tests, but the live deployment state has not been revalidated after the recent milestone implementations.

### Target State

- A small live smoke suite verifies versioned routes, metrics exposure, and one representative assistant/backend path after deployment.
- The deployed contract surface is recorded in project docs or deployment notes.
- Operational validation is lightweight, repeatable, and easy to rerun after future milestone changes.

### Files Most Likely To Change

- `pricing/docs/reports/` — store rollout verification notes or updated audit evidence
- `.github/` or local scripts if a repeatable smoke command is formalized later

### Action Plan

1. Define a minimal post-deploy smoke set for `pricing`: `/api/v1/health`, `/api/metrics`, and one representative request path.
2. Run the smoke checks against the refreshed runtime and capture exact outputs.
3. Record the verified deployed state in a short report or operational note under `pricing/docs/reports/`.
4. Decide whether the smoke set should stay manual or be promoted into a repeatable script or CI-adjacent check.

### Exit Criteria

- The refreshed runtime passes the agreed smoke probes without falling back to the SPA shell.
- The deployed API/metrics evidence is written down in project-local documentation.
- Future operators can identify the minimal smoke set needed to confirm deployment parity.
