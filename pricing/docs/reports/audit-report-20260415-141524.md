# Project Audit Report

**Generated:** 2026-04-15 14:15 CST
**Repository:** https://github.com/javierchan/oci.git
**Branch:** `vscode/codex-active-work`
**Commit:** `155b203 feat(session-store): add new test clients and sessions for quote exports and events`
**Auditor:** Codex (automated)

---

## Executive Summary

The `pricing` codebase is materially functional and well-tested, but its documented completion state is ahead of the actual code in several important places.

- The local backend test suite is strong and currently green: `892` tests passed from `pricing/server/`.
- The largest audit deviation is **M1**: it is marked `complete` in `pricing/docs/planning/IMPROVEMENT_MILESTONES.md`, but `pricing/server/dependency-resolver.js` still contains widespread `request.source` reparsing and composite text matching, which directly violates its exit criteria.
- **M8** is only partially complete in practice. The backend now exposes `/api/v1/*`, validates `pricing/docs/contracts/openapi.yaml`, and has a CI workflow, but the frontend and project docs still call legacy `/api/*` routes, and the running Docker container is clearly serving the old unversioned API.
- **M9** and **M10** remain effectively unimplemented. There is no `auth.js`, no `metrics.js`, no `x-api-key` enforcement, no rate limiting, and no `/api/metrics` endpoint.
- The active branch is dirty and currently includes mutable runtime artifacts and local dependency trees under `pricing/server/node_modules/`, which is an operational hygiene risk for the next push or merge.

**Completion summary:** `6/10` documented milestones are currently complete by audit evidence. `2` are partial. `2` are not started. There are `2` critical blocking findings: the unresolved M1 contract violation and the incomplete M8 rollout/deployment alignment.

## Repository Profile

- Selected codebase: `pricing`
- Branch root: `/Users/javierchan/Documents/GitHub/oci`
- Codebase path: `/Users/javierchan/Documents/GitHub/oci/pricing`
- Execution location: `/Users/javierchan/Documents/GitHub/oci`
- Write location: `/Users/javierchan/Documents/GitHub/oci/pricing/docs/reports/audit-report-20260415-141524.md`
- Worktree mode: canonical checkout for `vscode/codex-active-work`

### Stack And Shape

- Backend stack: Node.js server in `pricing/server`
- Frontend stack: static HTML/JS app in `pricing/app`
- Containerization: `pricing/docker-compose.yml`, `pricing/Dockerfile`
- CI: `.github/workflows/pricing-server-contract.yml`
- Test framework: Node built-in test runner via `node --test ./test/*.test.js`
- No Python backend, migrations, TypeScript build, or DB service configuration were detected in the selected codebase

### Repository Summary

- First commit date in repo history: `2026-01-07`
- Latest commit date in repo history: `2026-04-15`
- Total commits: `228`
- Top authors:
  - `223  javierchan`
- Commits in prior 7-day window: `2`
- Commits in last 7 days: `39`
- Approximate selected-codebase tracked source/doc files scanned: `169`
- `cloc` summary: unavailable in this environment (`cloc` not installed)

### Current Branch State

The active checkout is dirty:

```text
 M pricing/data/session-store.json
 M pricing/docs/planning/IMPROVEMENT_MILESTONES.md
 M pricing/server/index.js
 M pricing/server/node_modules/.package-lock.json
 M pricing/server/package-lock.json
 M pricing/server/package.json
?? .github/workflows/pricing-server-contract.yml
?? pricing/docs/contracts/openapi.yaml
?? pricing/server/node_modules/ajv-formats/
?? pricing/server/node_modules/ajv/
?? pricing/server/node_modules/fast-deep-equal/
?? pricing/server/node_modules/fast-uri/
?? pricing/server/node_modules/json-schema-traverse/
?? pricing/server/node_modules/lodash.merge/
?? pricing/server/node_modules/openapi-schema-validator/
?? pricing/server/node_modules/openapi-types/
?? pricing/server/node_modules/require-from-string/
?? pricing/server/test/api-versioning-openapi.test.js
```

This is not just normal code churn. It includes mutable session data and vendored package directories under `pricing/server/node_modules/`, which increases merge and commit risk.

## Milestone Status

### `pricing M1` — Service Detection Metadata Migration
**Status:** ⚠ Partial

Evidence:
- `pricing/server/dependency-resolver.js:429-461` still enumerates many `resolveComposite*` functions that operate by reparsing free text.
- `pricing/server/dependency-resolver.js:466-1887` still contains many `request.source` reads and pattern matches, including `findCompositeSegment(request.source, ...)`, `splitWorkloadSegments(request.source)`, and metric extraction directly from source text.

Gaps:
- Exit criterion “`resolveRequestDependencies()` contains no string matching against `request.source`” is not met.
- The resolver surface is still execution-order-driven and text-driven instead of fully family-metadata-driven.

Deviation:
- `pricing/docs/planning/IMPROVEMENT_MILESTONES.md` marks M1 as `complete`, but the implementation still violates the milestone’s central contract.

### `pricing M2` — GenAI Intent Output Validation
**Status:** ✅ Complete

Evidence:
- `pricing/server/intent-schema.js:148-175` defines `InvalidIntentPayloadError` and `validateIntentPayloadOrThrow`.
- `pricing/server/intent-extractor.js:5,76,87` imports the validator, handles invalid payloads, and validates the parsed model output.
- `pricing/server/test/intent-validation-fallback.test.js` exists and is included in the passing suite.

Gaps:
- No material gaps observed in the current audit slice.

### `pricing M3` — Session Store Async and Hardening
**Status:** ✅ Complete

Evidence:
- `pricing/server/session-store.js:137-155` uses async queued writes via `fs.promises.writeFile`; no production `fs.writeFileSync` remains in the module.
- `pricing/server/session-store.js:12,160-194` implements TTL pruning and periodic prune behavior.
- `pricing/server/test/session-store-hardening.test.js` covers concurrent writes, TTL pruning, and LRU eviction.
- `pricing/server/test/session-store.test.js:36-60` covers versioned writes and bounded event log behavior.

Gaps:
- No material gaps observed in the current audit slice.

### `pricing M4` — GenAI Configuration Externalization
**Status:** ✅ Complete

Evidence:
- `pricing/server/genai-profiles.js:6-31` defines named profiles with per-profile budgets and temperatures.
- `pricing/server/genai.js:121-136` resolves model IDs and defaults from environment/config.
- `pricing/server/intent-extractor.js:129,151` and `pricing/server/index.js:273,690-695` route callers through named profile resolution.

Gaps:
- No hardcoded OCID fallback was found in the audited server files.

### `pricing M5` — Structured Logging
**Status:** ✅ Complete

Evidence:
- `pricing/server/logger.js:9-131` provides the logger module, request logger helpers, and dev/JSON formatting behavior.
- `pricing/server/index.js:25,673-856` uses `buildRequestLogger`, `createTrace`, and request lifecycle logging.
- `pricing/server/test/structured-logging.test.js` verifies both human-readable and JSON log formats.
- `rg "console\\.warn\\(" pricing/server --glob '!pricing/server/node_modules/**'` returned no matches.

Gaps:
- No material gaps observed in the current audit slice.

### `pricing M6` — Prompt Caching and Token Optimization
**Status:** ✅ Complete

Evidence:
- `pricing/server/intent-extractor.js:52` reads `OCI_GENAI_INTENT_HISTORY_TURNS`.
- `pricing/server/genai.js:22,46-85` reads `OCI_GENAI_TOKEN_BUDGET`, warns on prompt size, and logs usage metadata.
- `pricing/server/test/prompt-optimization.test.js` covers conversation truncation, env override, token budget warning, and token usage logging.

Gaps:
- No material gaps observed for the implemented scope.

Deviation:
- The optimization path is based on truncation and budget hardening, not SDK-level prompt caching. This is an intentional implementation constraint rather than an untracked regression.

### `pricing M7` — Semantic Error Handling and Error Codes
**Status:** ✅ Complete

Evidence:
- `pricing/server/errors.js:3-104` defines structured pricing error types and shared error serialization.
- `pricing/server/index.js:725,857,1072,1088,1116,1162,1179,1198,1228,1234,1251,1257,1268` consistently routes failures through `handleError(...)`.
- `pricing/server/test/http-error-handling.test.js` verifies semantic error shapes and non-leakage of stack traces.

Gaps:
- No material gaps observed in the current audit slice.

### `pricing M8` — API Versioning and Contract Hardening
**Status:** ⚠ Partial

Evidence:
- `pricing/server/index.js:668-1286` registers canonical `/api/v1/*` routes and legacy aliases.
- `pricing/docs/contracts/openapi.yaml` exists and is validated by `pricing/server/test/api-versioning-openapi.test.js`.
- `.github/workflows/pricing-server-contract.yml` runs `npm test` for `pricing/**` changes.

Gaps:
- `pricing/app/index.html:292-1366` still calls legacy `/api/*` endpoints, including `/api/health`, `/api/catalog/*`, `/api/sessions`, `/api/assistant`, and `/api/excel/estimate`.
- `pricing/README.md:46-48,98-99` still documents legacy unversioned routes.
- `pricing/docs/core/ARCHITECTURE.md:389-545` still documents the old unversioned API surface.
- Live runtime probe shows deployment drift:
  - `curl http://localhost:8742/api/health` returns JSON
  - `curl http://localhost:8742/api/v1/health` returns the SPA HTML shell
  - `curl http://localhost:8742/openapi.json` returned HTML, not an OpenAPI document

Deviation:
- `pricing/server/index.js:56` uses `301` for `GET/HEAD` and `307` for non-idempotent legacy redirects. This is safer for `POST` bodies, but it does not match the milestone text literally.

### `pricing M9` — Authentication and Rate Limiting
**Status:** ❌ Not Started

Evidence:
- `sed pricing/server/auth.js` failed with `No such file or directory`.
- Search for `x-api-key`, `Authorization`, `express-rate-limit`, and `rateLimit` returned no implementation matches in the server.
- `pricing/server/index.js:36-40` still resolves identity from `x-client-id` or body `clientId` with a development trust-model comment only.
- `pricing/server/index.js:862-864` still returns provider details including `region`, `endpoint`, `compartment`, `modelId`, and `profile`.

Gaps:
- No shared-secret boundary exists for sensitive endpoints.
- No per-client throttling exists for `/api/v1/assistant` or `/api/v1/chat`.
- `/api/providers` is still more detailed than the milestone target state.

### `pricing M10` — Observability and Cost Metrics
**Status:** ❌ Not Started

Evidence:
- `sed pricing/server/metrics.js` failed with `No such file or directory`.
- Search for `/api/metrics`, `assistant_requests_total`, `genai_calls_total`, and `genai_tokens_input_total` returned no implementation matches.
- The runtime probe for `/openapi.json` failed to produce a machine-readable spec, and the running container does not expose visible versioned/metrics surface from the checkout being audited.

Gaps:
- No in-process metrics module exists.
- No Prometheus-compatible endpoint exists.
- No counters or histograms are wired into assistant or GenAI execution paths.

## Progress Metrics

Derived from `pricing/docs/planning/IMPROVEMENT_MILESTONES.md` and audit verification:

- Total milestones: `10`
- Complete: `6`
- Partial: `2`
- Not started: `2`
- In progress: `0`

Definition-of-done items:

- Total documented exit-criteria items: `38`
- Items verified: `25`
- Items with gaps: `13`

Interpretation:

- Documented milestone status currently overstates completion.
- The largest gap concentration is now in hardening and rollout rather than in deterministic quote correctness.

## Milestone Index

| ID | Title | Tier | Status |
|----|-------|------|--------|
| [M1](#m1--re-baseline-resolver-contract-and-status) | Re-baseline Resolver Contract and Status | Critical | open |
| [M2](#m2--finish-metadata-driven-composite-resolution) | Finish Metadata-Driven Composite Resolution | Critical | open |
| [M3](#m3--parity-and-documentation-closeout-for-resolver-hardening) | Parity and Documentation Closeout for Resolver Hardening | High | open |
| [M4](#m4--migrate-clients-and-docs-to-apiv1) | Migrate Clients and Docs to `/api/v1` | Critical | open |
| [M5](#m5--align-runtime-rollout-and-redirect-policy) | Align Runtime Rollout and Redirect Policy | High | open |
| [M6](#m6--retire-legacy-route-dependence) | Retire Legacy Route Dependence | Medium | open |
| [M7](#m7--introduce-auth-boundary-for-sensitive-operations) | Introduce Auth Boundary for Sensitive Operations | Critical | open |
| [M8](#m8--add-rate-limiting-and-provider-sanitization) | Add Rate Limiting and Provider Sanitization | Critical | open |
| [M9](#m9--instrument-assistant-and-genai-metrics) | Instrument Assistant and GenAI Metrics | High | open |
| [M10](#m10--validate-observability-and-security-rollout) | Validate Observability and Security Rollout | Medium | open |

## Verification Results

### Git And Checkout

```text
$ git rev-parse --show-toplevel
/Users/javierchan/Documents/GitHub/oci

$ git branch --show-current
vscode/codex-active-work

$ git worktree list --porcelain
worktree /Users/javierchan/Documents/GitHub/oci
HEAD 155b2036e0dd687521645b16595303fc84f411ce
branch refs/heads/vscode/codex-active-work

worktree /Users/javierchan/.codex/worktrees/7ea6/oci
HEAD 6990ea595854c3cdd8faf67948cef375a07f7a23
detached

worktree /Users/javierchan/.codex/worktrees/b840/oci
HEAD 4df5499efcbb255f2d83aba812573cc1a40bcee5
branch refs/heads/codex/codex-active-work
```

### Backend Validation

```text
$ (cd pricing/server && npm test)
...
ℹ tests 892
ℹ pass 892
ℹ fail 0
```

### Docker Runtime

```text
$ (cd pricing && docker compose ps)
NAME                IMAGE               COMMAND                  SERVICE     CREATED      STATUS      PORTS
oci-pricing-agent   pricing-oci-agent   "docker-entrypoint.s…"   oci-agent   5 days ago   Up 2 days   0.0.0.0:8742->8742/tcp, [::]:8742->8742/tcp

$ (cd pricing && docker compose config --services)
oci-agent
```

### Live HTTP Probes

```text
$ curl -i http://localhost:8742/api/health | head
HTTP/1.1 200 OK
...
{"ok":true,"catalogsLoaded":4,...}

$ curl -i http://localhost:8742/api/v1/health | head
HTTP/1.1 200 OK
Content-Type: text/html; charset=UTF-8
<!DOCTYPE html>

$ curl -sf http://localhost:8742/openapi.json
HTML returned instead of JSON; local runtime does not expose a machine-readable OpenAPI document

$ curl -sf http://localhost:8742/health && echo "API: UP"
HTML returned; runtime still exposes SPA shell on root health path
```

### Dependency Drift / Tooling

```text
$ (cd pricing/server && npm outdated --depth=0)
[no output]

$ cloc pricing ...
command unavailable in this environment
```

Interpretation:

- No package outdated signal was produced from `npm outdated`.
- This does not offset the current local dependency drift from newly materialized `pricing/server/node_modules/*` changes in the worktree.

## Pending Tasks

### CRITICAL

- `pricing/server/dependency-resolver.js` — `pricing M1` — still reparses `request.source`, so the resolver contract is not metadata-driven yet.
- `pricing/app/index.html` — `pricing M8` — still uses legacy `/api/*` routes, so client rollout to `/api/v1` is incomplete.
- `pricing/server/index.js` — `pricing M9` — no authentication or rate limiting is applied to `/api/v1/assistant`, `/api/v1/chat`, or sensitive config/admin surfaces.
- Live container `pricing-oci-agent` — `pricing M8` — running runtime does not match the checked-out versioned API behavior, blocking trustworthy deployment validation.

### INCOMPLETE

- `pricing/README.md` — `pricing M8` — operational docs still instruct users to call unversioned endpoints.
- `pricing/docs/core/ARCHITECTURE.md` — `pricing M8` — stable API surface section still lists unversioned routes.
- `pricing/server/index.js` — `pricing M8` — legacy redirect policy intentionally uses `307` for `POST`, which is safer but not reflected in milestone text/documentation.
- `pricing/server/index.js` — `pricing M9` — `/api/providers` still returns region/endpoint/compartment/model/profile details instead of connectivity-only status.
- `pricing/server/metrics.js` — `pricing M10` — metrics module is absent.
- `pricing/server/index.js` — `pricing M10` — `/api/metrics` route is absent.
- `pricing/data/session-store.json` and `pricing/server/node_modules/*` — operational hygiene — mutable runtime artifacts are currently mixed into the branch worktree.

### DEFERRED

- Full removal of legacy `/api/*` aliases — `pricing M8` — should happen only after frontend migration, runtime rollout, and compatibility validation are complete.

## Finding Remediation Plans

### Finding: Resolver contract drift and incorrect milestone completion
- Classification: `CRITICAL`
- Impact: deterministic routing correctness and documentation trust
- Area: `pricing M1`
- Evidence:
  - `pricing/server/dependency-resolver.js:429-461` — composite resolution still fans out through many manual source-text parsers
  - `pricing/server/dependency-resolver.js:466-1887` — many direct `request.source` reads remain
  - `pricing/docs/planning/IMPROVEMENT_MILESTONES.md` — M1 is marked `complete`
- Issue: The implementation still violates M1’s core exit criteria, so the documented architecture hardening status is inaccurate.
- Remediation Milestones:
  - `M1` — Re-baseline Resolver Contract and Status (`Critical`, `open`): align documented completion state with actual code.
  - `M2` — Finish Metadata-Driven Composite Resolution (`Critical`, `open`): remove remaining free-text composite reparsing from resolver paths.
  - `M3` — Parity and Documentation Closeout for Resolver Hardening (`High`, `open`): validate behavior and then re-mark the milestone honestly.

### Finding: API versioning rollout is incomplete across code, docs, and runtime
- Classification: `CRITICAL`
- Impact: deployment safety, client/server contract consistency, and backward compatibility management
- Area: `pricing M8`
- Evidence:
  - `pricing/app/index.html:292-1366` — frontend still calls legacy `/api/*`
  - `pricing/README.md:46-48,98-99` — operational docs still instruct legacy routes
  - `pricing/docs/core/ARCHITECTURE.md:389-545` — architecture doc still lists unversioned API surface
  - Live probe — `/api/health` works, `/api/v1/health` returns SPA HTML from the running container
- Issue: The versioned backend exists in source, but the rest of the system has not migrated to it and the running container does not reflect the audited checkout.
- Remediation Milestones:
  - `M4` — Migrate Clients and Docs to `/api/v1` (`Critical`, `open`): move the app and docs to the versioned surface.
  - `M5` — Align Runtime Rollout and Redirect Policy (`High`, `open`): rebuild/redeploy the runtime and document the intentional redirect semantics.
  - `M6` — Retire Legacy Route Dependence (`Medium`, `open`): remove or hard-deprecate aliases after compatibility validation.

### Finding: Authentication and rate limiting are still absent
- Classification: `CRITICAL`
- Impact: security, abuse resistance, and OCI GenAI quota exposure
- Area: `pricing M9`
- Evidence:
  - `pricing/server/index.js:36-40` — client identity still trusts `x-client-id` or body `clientId`
  - `pricing/server/auth.js` — file absent
  - Search for `x-api-key`, `Authorization`, `express-rate-limit`, and `rateLimit` — no implementation matches
  - `pricing/server/index.js:862-864` — `/api/providers` still returns detailed config metadata
- Issue: The server still operates in a trust-based internal mode with no real authentication or throttling boundary.
- Remediation Milestones:
  - `M7` — Introduce Auth Boundary for Sensitive Operations (`Critical`, `open`): add shared-secret enforcement and central auth policy.
  - `M8` — Add Rate Limiting and Provider Sanitization (`Critical`, `open`): add in-memory throttling and reduce exposed provider detail.

### Finding: Metrics and Prometheus scrape surface are missing
- Classification: `INCOMPLETE`
- Impact: observability, cost tracking, and production troubleshooting
- Area: `pricing M10`
- Evidence:
  - `pricing/server/metrics.js` — file absent
  - Search for `/api/metrics`, `assistant_requests_total`, `genai_calls_total`, `genai_tokens_input_total` — no implementation matches
  - Current runtime probes expose no observable metrics contract
- Issue: Logging exists, but the codebase still lacks quantitative counters and a scrape endpoint for assistant and GenAI behavior.
- Remediation Milestones:
  - `M9` — Instrument Assistant and GenAI Metrics (`High`, `open`): add counters/histograms and wire them into request execution.
  - `M10` — Validate Observability and Security Rollout (`Medium`, `open`): expose the scrape endpoint and verify metrics/security interplay in runtime.

## M1 — Re-baseline Resolver Contract and Status

**Tier:** Critical
**Status:** open

### Problem

The documented completion state for resolver hardening no longer matches the code. This creates planning risk because downstream work assumes a metadata-driven contract that is not actually enforced yet.

### Root Cause

M1 was marked complete after partial cleanup, but broad `request.source` composite parsing and routing heuristics remained in `dependency-resolver.js`.

### Target State

- The milestone status reflects verified implementation reality.
- The remaining M1 contract violations are explicitly tracked and bounded before more hardening layers build on top of them.

### Files Most Likely To Change

- `pricing/docs/planning/IMPROVEMENT_MILESTONES.md` — re-baseline milestone status and notes
- `pricing/docs/planning/EXECUTION_PLAN.md` — reflect actual hardening sequence
- `pricing/server/dependency-resolver.js` — identify remaining contract-violating paths

### Action Plan

1. Audit every remaining `request.source` use in `pricing/server/dependency-resolver.js` and classify it by purpose: routing, segmentation, quantity extraction, or composite decomposition.
2. Update milestone/documentation status so M1 is no longer reported complete until the remaining contract-violating paths are closed.
3. Produce a bounded implementation map showing which residual paths must move to metadata or typed inputs before M1 can be re-closed.

### Exit Criteria

- The docs no longer overstate M1 as complete if contract-violating resolver paths remain.
- Every residual `request.source` dependency in the resolver is inventoried with an owner path to closure.
- The next implementation milestone can proceed without ambiguity about what still belongs to M1.

## M2 — Finish Metadata-Driven Composite Resolution

**Tier:** Critical
**Status:** open

### Problem

Composite and mixed-service resolution still depends on reparsing free text inside the deterministic resolver, which undermines the architectural split between intent interpretation and deterministic pricing.

### Root Cause

Composite support was expanded incrementally in the resolver for speed, and the metadata model did not absorb those paths afterward.

### Target State

- Composite resolution dispatches from structured family metadata and typed parsed inputs.
- `dependency-resolver.js` no longer decides family ownership by scanning free-text user prompts.

### Files Most Likely To Change

- `pricing/server/dependency-resolver.js` — remove remaining text-driven family/composite dispatch
- `pricing/server/service-families.js` — add remaining declarative ownership and composite capability metadata
- `pricing/server/assistant.js` or related request-shaping helpers — ensure required typed inputs are preserved before deterministic resolution

### Action Plan

1. Move composite family detection and segment ownership rules into `service-families.js` or companion metadata structures.
2. Refactor resolver entrypoints to consume structured family/segment inputs instead of scanning `request.source`.
3. Keep low-level quantity parsing only where it is truly deterministic input shaping, not routing or family detection.
4. Add focused regression tests for mixed bundles that previously relied on source-text matching.

### Exit Criteria

- Resolver family selection no longer depends on `request.source`.
- Composite decomposition is traceable from metadata rather than execution-order helper lists.
- Existing parity/regression coverage remains green and covers migrated mixed-service bundles.

## M3 — Parity and Documentation Closeout for Resolver Hardening

**Tier:** High
**Status:** open

### Problem

Even after resolver refactoring, the codebase still needs proof that parity and deterministic behavior did not regress and that milestone status is trustworthy again.

### Root Cause

Resolver hardening affects high-coverage pricing paths; without closeout validation, the project risks trading architectural cleanliness for silent parity regressions.

### Target State

- Resolver hardening is validated by targeted and broad regression suites.
- M1 can be marked complete again with evidence, not assumption.

### Files Most Likely To Change

- `pricing/server/test/*.test.js` — add or extend targeted resolver tests
- `pricing/docs/planning/IMPROVEMENT_MILESTONES.md` — close M1 only after verification
- `pricing/docs/planning/COVERAGE_ROADMAP.md` — note the hardened resolver boundary if validated

### Action Plan

1. Add regression tests for representative composite bundles previously resolved through text reparsing.
2. Run the full server suite and compare key parity-sensitive cases before and after the refactor.
3. Update docs only after the refactor and validation are both complete.

### Exit Criteria

- Targeted composite/regression tests cover the migrated resolver paths.
- The full `pricing/server` test suite passes after the M1 hardening slice.
- M1 is re-marked complete only after its documented exit criteria are actually true.

## M4 — Migrate Clients and Docs to `/api/v1`

**Tier:** Critical
**Status:** open

### Problem

The backend now exposes a versioned API, but the frontend and primary operator docs still target legacy `/api/*` endpoints.

### Root Cause

M8 implementation focused on the backend contract and spec first, but the client/docs migration was left incomplete.

### Target State

- New client code uses `/api/v1/*` consistently.
- Operator-facing documentation describes the versioned contract as the source of truth.

### Files Most Likely To Change

- `pricing/app/index.html` — migrate all fetch calls to `/api/v1/*`
- `pricing/README.md` — update operational examples and architecture summary
- `pricing/docs/core/ARCHITECTURE.md` — update stable API surface references

### Action Plan

1. Replace legacy frontend endpoint calls with `/api/v1/*` equivalents.
2. Update README and architecture docs so examples and endpoint inventories match the versioned API.
3. Add regression coverage, if needed, for frontend/session flows that now call versioned routes.

### Exit Criteria

- No active frontend fetch path points at legacy `/api/*` routes for supported flows.
- README and architecture docs describe `/api/v1/*` as the canonical API surface.
- Existing server and route tests continue to pass after client/doc migration.

## M5 — Align Runtime Rollout and Redirect Policy

**Tier:** High
**Status:** open

### Problem

The running Docker container does not reflect the versioned API behavior in the current checkout, and redirect semantics are implemented more safely than documented but not explained anywhere.

### Root Cause

Source changes landed without a verified rebuild/redeploy of the local runtime, and the milestone text still assumes literal `301` redirects for all methods.

### Target State

- The running container matches the audited checkout behavior.
- Redirect semantics for legacy `POST` routes are documented intentionally.

### Files Most Likely To Change

- `pricing/docker-compose.yml` or related runtime docs — align rebuild/run expectations
- `pricing/README.md` — document rollout/rebuild steps and redirect behavior
- `pricing/docs/planning/IMPROVEMENT_MILESTONES.md` — clarify safe redirect semantics if retained

### Action Plan

1. Rebuild/restart the local runtime from the active checkout and re-run live probes against `/api/v1/*`.
2. Decide whether the project formally endorses `307` for non-GET legacy redirects, then document that choice.
3. Add one explicit deployment verification note or checklist for post-M8 local rollout.

### Exit Criteria

- Live container responses match the checked-out versioned route behavior.
- Redirect semantics are documented and no longer ambiguous.
- Runtime probe evidence can be trusted as validation of the current branch.

## M6 — Retire Legacy Route Dependence

**Tier:** Medium
**Status:** open

### Problem

Legacy route aliases still carry real production value because the client and docs still depend on them.

### Root Cause

The migration is incomplete, so the legacy layer cannot yet be safely reduced or removed.

### Target State

- Legacy routes are transitional only.
- The codebase has a clear removal or hard-deprecation path for unversioned routes.

### Files Most Likely To Change

- `pricing/server/index.js` — tighten or remove legacy aliases when safe
- `pricing/docs/contracts/openapi.yaml` — reflect steady-state public contract only
- `pricing/README.md` — publish the deprecation timeline

### Action Plan

1. Confirm no first-party client depends on legacy endpoints.
2. Decide whether the next phase is `410 Gone`, removal, or long-lived compatibility.
3. Update server behavior and docs to match the chosen deprecation policy.

### Exit Criteria

- First-party code no longer depends on unversioned routes.
- A documented deprecation policy exists for legacy aliases.
- The server behavior matches the chosen policy and is covered by tests.

## M7 — Introduce Auth Boundary for Sensitive Operations

**Tier:** Critical
**Status:** open

### Problem

Any caller can choose a client ID, mutate sessions, and trigger expensive assistant paths without authentication.

### Root Cause

The server still runs in the original trusted-internal access model and lacks an enforcement module.

### Target State

- Sensitive operations require a shared secret or equivalent lightweight boundary.
- Auth behavior is centralized in a dedicated module rather than embedded ad hoc in routes.

### Files Most Likely To Change

- `pricing/server/auth.js` — new auth module
- `pricing/server/index.js` — protect sensitive endpoints and centralize auth checks
- `pricing/server/test/*.test.js` — add auth enforcement coverage

### Action Plan

1. Add a lightweight auth module that validates `x-api-key` or equivalent server-side secret configuration.
2. Protect high-risk endpoints first: catalog reload, provider inspection, and any privileged mutation surface.
3. Document the dev-vs-production trust model boundary in one place and wire tests around it.

### Exit Criteria

- `pricing/server/auth.js` exists and is used by route handlers.
- Sensitive endpoints reject missing or invalid shared-secret requests.
- Auth behavior is covered by automated tests.

## M8 — Add Rate Limiting and Provider Sanitization

**Tier:** Critical
**Status:** open

### Problem

Even with a shared-secret boundary, the assistant endpoints can still be spammed and `/api/providers` currently exposes more configuration detail than the target state allows.

### Root Cause

The server has no in-memory request throttling layer and the provider route was left developer-oriented.

### Target State

- `/api/v1/assistant` and `/api/v1/chat` enforce per-client limits.
- `/api/providers` returns connectivity posture, not operator-sensitive config detail.

### Files Most Likely To Change

- `pricing/server/index.js` — add throttling and sanitize `/api/providers`
- `pricing/server/auth.js` — share config and policy decisions with auth layer
- `pricing/server/test/*.test.js` — add rate-limit and provider-response tests

### Action Plan

1. Implement simple in-memory per-client counters for assistant/chat request windows.
2. Reduce `/api/providers` response fields to connectivity-safe information.
3. Add tests for throttle responses and provider response shape.

### Exit Criteria

- Assistant/chat endpoints enforce configured per-client request ceilings.
- `/api/providers` no longer leaks region/endpoint/compartment/model/profile detail unless explicitly intended and documented.
- Rate-limit configuration is environment-driven and covered by tests.

## M9 — Instrument Assistant and GenAI Metrics

**Tier:** High
**Status:** open

### Problem

The system has structured logs but no counters or histograms for requests, fallbacks, token consumption, or quote/discovery outcomes.

### Root Cause

Observability work stopped at logging and never advanced into measurable in-process instrumentation.

### Target State

- The codebase exposes in-process metrics for assistant and GenAI usage.
- Quantitative runtime behavior is observable without log scraping.

### Files Most Likely To Change

- `pricing/server/metrics.js` — new metrics module
- `pricing/server/genai.js` — instrument call counts and token counters
- `pricing/server/assistant.js` and `pricing/server/index.js` — instrument request outcome counters

### Action Plan

1. Create a metrics module that manages counters/histograms in-process.
2. Instrument assistant and GenAI paths with labeled counters for call type, outcome, and token volume.
3. Add focused tests that verify metrics are incremented under representative flows.

### Exit Criteria

- `pricing/server/metrics.js` exists and is used by assistant/GenAI code paths.
- Assistant and GenAI execution increment the expected counters.
- Automated tests verify the key metric names and increments.

## M10 — Validate Observability and Security Rollout

**Tier:** Medium
**Status:** open

### Problem

Security and observability hardening will remain brittle unless they are validated together in real runtime conditions and documented for operators.

### Root Cause

The codebase currently lacks a closeout milestone that proves auth, throttling, metrics, and rollout behavior all work together from the deployed surface.

### Target State

- `/api/metrics` is exposed intentionally and documented.
- Runtime validation proves the security and observability layers coexist without breaking assistant functionality.

### Files Most Likely To Change

- `pricing/server/index.js` — expose `/api/metrics`
- `pricing/docs/core/ARCHITECTURE.md` — describe the operational surface
- `pricing/README.md` — add operator-facing validation commands

### Action Plan

1. Expose `/api/metrics` in Prometheus text format and decide whether it is open or shared-secret-protected by configuration.
2. Validate end-to-end behavior in local runtime: auth, rate limiting, assistant requests, and metrics scrape.
3. Document the operational validation path in architecture and README.

### Exit Criteria

- `/api/metrics` returns a valid scrape response in runtime.
- Security controls do not break expected assistant or admin flows.
- Operator docs include reproducible validation steps for the hardening stack.

## Debt Markers

Source scan for `TODO|FIXME|HACK|PLACEHOLDER|XXX|NOCOMMIT` under `pricing` returned no matches in non-`node_modules` source files.

Interpretation:

- The current debt is structural and operational, not hidden behind inline debt comments.

## Recommended Next Actions

1. Re-open `pricing M1` in the project docs and treat the current resolver state as a live critical hardening item.
2. Migrate `pricing/app/index.html`, `pricing/README.md`, and `pricing/docs/core/ARCHITECTURE.md` to `/api/v1/*` before calling M8 complete in practice.
3. Rebuild the local Docker runtime from the active checkout and re-run the `/api/v1/health` probe so runtime validation matches source.
4. Start `pricing M9` immediately after the M1/M8 corrections, because the current trust-based `x-client-id` model remains the main exposure.
5. Clean the branch worktree before the next push: remove mutable `session-store.json` churn and avoid carrying `pricing/server/node_modules/*` into source control decisions.

## Dependency Drift

- `npm outdated --depth=0` in `pricing/server` produced no package drift output during this audit.
- The current branch still has **local dependency drift**:
  - `pricing/server/package.json`
  - `pricing/server/package-lock.json`
  - `pricing/server/node_modules/*`
- This drift appears tied to recent M8 work and local install activity rather than to upstream version lag.

The audit report and milestone plan are ready. Do you want me to start working through the identified milestones in order?
